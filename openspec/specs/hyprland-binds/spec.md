# hyprland-binds Specification

## Purpose

Горячие клавиши сессии Hyprland: раскладка, построенная от понятий демона workspaced, и явный список сочетаний, которые занимать нельзя.

## Requirements

### Requirement: Скриншоты в Wayland
Скрипты скриншотов для Hyprland MUST использовать grim, slurp и wl-copy, сохранять файл в каталог скриншотов с именем из даты, размера и суффикса `grim`, копировать снимок в буфер обмена и показывать уведомление dunst.

#### Scenario: Снимок области
- **WHEN** нажато Shift+Print и выделена область экрана
- **THEN** появляется файл снимка в каталоге скриншотов, снимок есть в буфере обмена, показано уведомление

### Requirement: Привязки из конфига демона
Конфиг Hyprland MUST при каждой загрузке выполнять код привязок, который печатает `workspaced keys --lua`: прямые привязки одиночных сочетаний и подкарты цепочек workspace, приложений, служебных действий и записей `[[binds]]`. Цепочка MUST работать как последовательность: первое сочетание открывает подкарту, последующие ведут глубже, последнее выполняет действие и закрывает подкарту; `Escape` в подкарте MUST закрывать её без действия. Если команда завершилась ошибкой или недоступна, конфиг Hyprland MUST загрузить копию кода из `~/.local/state/workspaced/keys.lua`, записать причину в журнал и показать уведомление; если копии нет, конфиг MUST загрузиться без привязок с уведомлением об этом. Сочетания из списка `reserved` MUST NOT появляться в `hyprctl binds`.

#### Scenario: Цепочка workspace
- **WHEN** в конфиге демона у `dev-front` задано `chain = "SUPER+W f"` и нажато Super+W, затем f
- **THEN** `dev-front` поднят на активном столе, подкарта закрыта, следующее нажатие f печатает букву в окне

#### Scenario: Escape закрывает подкарту
- **WHEN** нажато Super+W, затем Escape
- **THEN** ничего не поднимается, `hyprctl submap` (или эквивалентная проверка) показывает отсутствие активной подкарты

#### Scenario: Ошибка конфига демона
- **WHEN** в конфиг демона внесена ошибка и выполнена `hyprctl reload`
- **THEN** перезагрузка завершается, все привязки из таблицы раскладки работают как до правки, на экране уведомление об ошибке, в журнале Hyprland сообщение от `workspaced keys`

#### Scenario: Демон отсутствует и копии нет
- **WHEN** `workspaced` нет в `PATH`, файла `keys.lua` нет и выполнена `hyprctl reload`
- **THEN** перезагрузка завершается, `hyprctl binds` пуст, на экране уведомление о том, что привязки не загружены

### Requirement: Раскладка клавиш сессии
Конфиг демона workspaced (`~/.config/workspaced/config.toml`: разделы `[keys]`, цепочки workspace и приложений, записи `[[binds]]`) MUST задавать все привязки сессии; `hyprland.lua` MUST NOT содержать собственных привязок, кроме загрузки кода привязок от демона. Раскладка строится от понятий демона: префикс Super+Tab для действий с workspace, слой Alt+Super для геометрии окна, Super+буква для приложений workspace, действий с окном и программ:

| Сочетание | Действие |
|---|---|
| Super+Tab w | поднять workspace work на текущем столе (цепочка workspace) |
| Super+Tab s | поднять workspace surf (браузеры из контейнера fedora-box на шаблоне overlay: Яндекс.Браузер по центру, главный, Chrome профиля Default слева, профиля AI.dev2026 справа) на текущем столе |
| Super+Tab Super+S | сохранить активный workspace в конфиг |
| Super+Tab Tab | поднять следующий workspace из списка текущего стола |
| Super+T / C / E | сделать главным приложение herdr (wezterm с herdr) / Chromium / neovide (цепочки приложений из одного сочетания) |
| Super+S | окно выбора сессии |
| Super+R | лаунчер `rofi -show drun` |
| Super+Delete | закрыть активное окно |
| Super+B | окно Google Chrome профиля Default (приложение `chrome`): поднять surf и сделать окно главным |
| Super+V | окно Google Chrome профиля AI.dev2026 (приложение `chrome-ai`), так же |
| Super+Y | окно Яндекс.Браузера из контейнера fedora-box (приложение `yandex-browser`), так же |
| Super+Insert | меню буфера обмена (история cliphist через rofi, выбранная запись вставляется в буфер) |
| Super+Z | спрятать активное окно в специальный рабочий стол без перехода за ним |
| Super+Shift+Z | показать или скрыть специальный рабочий стол со спрятанными окнами |
| Super+стрелки | перенести фокус на соседнее окно в направлении стрелки |
| Alt+Super+Влево / Вправо | активное окно занимает левую / правую половину рабочей области командой демона `half` с отступом 10 px от краёв и между окнами, тем же, что между плитками панели |
| Alt+Super+Вверх / Вниз | активное окно занимает верхнюю / нижнюю половину рабочей области с теми же отступами |
| Alt+Super+Home / PageDown / End | активное окно в половину ширины и высоты рабочей области в верхнем ряду слева / по центру / справа командой демона `place` с теми же отступами |
| Ctrl+Alt+Super+Z / X / V | то же в нижнем ряду слева / по центру / справа |
| Ctrl+Alt+Super+C | активное окно в половину ширины на всю высоту по центру |
| Alt+Super+PageUp | переключить развёртывание окна командой демона `maximize`: окно занимает рабочую область с отступом 10 px от краёв экрана и панели, тем же, что у половин и плиток панели; повторное нажатие возвращает прежние положение и размер |
| Alt+Super+Menu | переключить полноэкранный режим |
| Super+1…8 | перейти на рабочий стол 1…8 |
| Super+Shift+1…8 | перенести активное окно на стол 1…8 и перейти вслед за ним |
| Alt+Tab, Alt+Shift+Tab | переключить фокус на следующее / предыдущее окно текущего стола с подъёмом окна наверх |
| Super+L | заблокировать сессию командой `loginctl lock-session` |
| Ctrl+Alt+Super+Escape | выход из сессии |
| Print, Ctrl+Print, Shift+Print | скриншот экрана, с обратным отсчётом, области |
| Ctrl+Escape, Ctrl+Return, Ctrl+Пробел, Ctrl+Shift+Пробел | dunstctl history-pop, context, close, close-all |
| XF86AudioRaiseVolume / LowerVolume / Mute | `~/.local/bin/handmade-scripts/change-volume.sh` с аргументами `+`, `-`, `0` |
| XF86MonBrightnessUp / Down | `~/.local/bin/handmade-scripts/change-brightness.sh` с аргументами `+`, `-` |
| XF86AudioPlay / Stop / Prev / Next | playerctl play-pause, stop, previous, next |
| Super+левая кнопка мыши | перемещение окна перетаскиванием |
| Super+правая кнопка мыши | изменение размера перетаскиванием |

Сочетания Super+Shift+стрелки MUST оставаться свободными. Рабочая область — площадь монитора за вычетом зон, зарезервированных слоями (панелью).

#### Scenario: Префикс workspace
- **WHEN** нажато Super+Tab, затем w
- **THEN** на текущем столе поднимается workspace work, подкарта закрывается; `hyprctl binds` содержит подкарту, открываемую Super+Tab, и не содержит одиночного действия на Super+Tab

#### Scenario: Следующий workspace
- **WHEN** на столе два workspace и нажато Super+Tab, затем Tab
- **THEN** поднимается следующий workspace из списка стола

#### Scenario: Окно в половину рабочей области
- **WHEN** при активном плавающем окне нажато Alt+Super+Влево, зарезервированных зон нет, `gap = 5`
- **THEN** окно получает положение 10,10 и размер 1905×2140 на мониторе 3840×2160

#### Scenario: Окно в позицию рабочей области
- **WHEN** при активном плавающем окне нажато Ctrl+Alt+Super+V, затем Ctrl+Alt+Super+C, панель резервирует 330 px слева, `gap = 5`
- **THEN** окно получает положение 2090,1085 и размер 1740×1065, затем 1215,10 и 1740×2140

#### Scenario: Развёрнутое окно с отступами
- **WHEN** при активном плавающем окне нажато Alt+Super+PageUp, панель резервирует 330 px слева на мониторе 3840×2160, `gap = 5`
- **THEN** окно получает положение 340,10 и размер 3490×2140, а повторное нажатие Alt+Super+PageUp возвращает ему прежние положение и размер

#### Scenario: Приложение одним сочетанием
- **WHEN** нажато Super+C при поднятом workspace work
- **THEN** окно Chromium становится главным в центральной ячейке, а прежнее главное окно занимает его ячейку; Super+Delete закрывает активное окно

#### Scenario: Перенос окна на стол
- **WHEN** при активном окне нажато Super+Shift+3
- **THEN** окно оказывается на столе «3», стол «3» становится активным, окно остаётся в фокусе

#### Scenario: Блокировка
- **WHEN** нажато Super+L
- **THEN** экран закрывает hyprlock, после ввода пароля сессия продолжается со всеми окнами

#### Scenario: Привязок в конфиге Hyprland нет
- **WHEN** выполнен поиск `hl.bind` в `~/.config/hypr/hyprland.lua`
- **THEN** совпадений нет, а `hyprctl binds` содержит все сочетания из таблицы

### Requirement: Зарезервированные сочетания
Список `reserved` раздела `[keys]` конфига демона MUST содержать сочетания, которые ни одна привязка занимать не может, с комментарием о причине: сочетания карты XKB Super+Пробел, Alt+E и Alt+R, которые композитор перехватил бы раньше XKB, и намеренно свободное Alt+Super+Пробел. Проверка конфига MUST отклонять привязку на любое из этих сочетаний, а конфиг MUST NOT назначать их действиям без отдельного решения.

#### Scenario: Сочетание без действия
- **WHEN** нажато Alt+Super+Пробел
- **THEN** ничего не происходит, `hyprctl binds` не содержит этого сочетания

#### Scenario: Попытка занять зарезервированное сочетание
- **WHEN** в `[[binds]]` добавлена запись с `chain = "SUPER+D"` и выполнена `workspaced check`
- **THEN** команда завершается с ошибкой, называющей `SUPER+D` как зарезервированное, демон продолжает работать с прежним конфигом
