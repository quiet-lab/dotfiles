## 1. Демон workspaced (`~/work/pets/workspaced`)

- [ ] 1.1 `hypr.rs`: разбор тегов состава `ws:<имя>` (с отбрасыванием `*`, который добавляет правило композитора) — `Client::workspaces()` и `Client::in_ws(ws)`; диспетчеры `d_tag`/`d_untag` годятся без изменений. Проверка: модульный тест `ws_tag_forms`
- [ ] 1.2 `daemon.rs`: окна workspace по тегам (решение D1) — помощник `ws_windows(clients, ws)`; `placed_windows`, `ws_app_of`, `next_app_window`, `anchor_window`, `on_focus`, `remember_geometry`, `arrange` (запоминание прямоугольников), `detach_step`, парковка в `raise_on` и `remove` переходят с «приложение описано в workspace» на «окно несёт тег `ws:`». Проверка: модульные тесты `ws_windows_by_tags`, прежние тесты цикла и расстановки проходят
- [ ] 1.3 `daemon.rs`: тег состава при входе окна в workspace (решение D2) — `adopt` (по `Pending.workspace`), `capture_new` и `join` (workspace, активный на столе окна), `adopt_untagged` (workspace, для которого идёт захват); запись о месте для приложения, которого в workspace нет, — прежним путём `remember_extra`. Проверка: модульный тест `joined_window_gets_ws_tag`, проверка 3.1
- [ ] 1.4 `daemon.rs`: сверка состава с эффективным конфигом (решение D2) рядом с `free_stale_tagged` — чистая функция `stale_ws_tags(cfg, clients)` (тег `ws:W`, чей `W` исчез или не описывает приложение окна) и `derive_membership(cfg, st, clients)` (состав для окон с тегом экземпляра без тегов состава: активный workspace стола или все неактивные workspace списков столов для окна на `special:pool`); вызов после перечитывания конфига, при старте и после загрузки сессии (`session.rs`: `restore_default`, `load`). Проверка: модульные тесты `stale_ws_tags_follow_effective_config`, `membership_is_derived_for_old_windows`, проверка 3.8
- [ ] 1.5 `daemon.rs`: правило размещения (решение D4) — чистая функция `window_home(members, active_by_desktop, current, desktop_of_window) -> Home { Desktop(n), Pool, Keep }` с четырьмя шагами и неприкосновенностью `special:hidden`. Проверка: модульный тест `window_home_rules` на все четыре шага, на стол пользователя без workspace окна и на скрытое окно
- [ ] 1.6 `daemon.rs`: `adopt_plan` и `raise_on` — захват свободных окон (в том числе с тегом экземпляра без состава) и отдача в общее пользование окон других workspace приложению, у которого в поднимаемом workspace окон нет (решение D3); перенос окон workspace и вытеснение чужих по `window_home` после `assign_desktop`; поднятие без перехода (`focus = false`, преемник) не уводит общее окно со стола композитора. Проверка: модульные тесты `raise_shares_windows_only_when_missing`, `displaced_shared_window_goes_to_its_other_desktop`, проверки 3.2, 3.5
- [ ] 1.7 `daemon.rs`: следование за столом (решения D5, D6) — в `on_event` для `Event::Workspace` после ленивого поднятия: уточнить стол у композитора (`sync_current`), для активного workspace стола перенести его окна с других столов и `special:pool`, в режиме обмена ячеек поставить в ячейку (`State::rect_for`), в режиме `stack` — в `st.geom[W]` или на место из конфига (`raise_target`); перед уходом окна со стола, где активен другой его workspace режима `stack`, запомнить там прямоугольник; окно с фокусом поднять наверх; одна последовательность диспетчеров. Чистая функция `follow_plan` для теста. Проверка: модульный тест `follow_plan_moves_shared_windows`, проверки 3.3, 3.4
- [ ] 1.8 `daemon.rs`: `Daemon::app` — порядок случаев (решение D7): описано в активном `W` → цикл; признак `pull` → перетаскивание; описано в запущенном `V` → поднять `V`; есть активный `W` → открыть в `W` по правилам захвата при поднятии (свободные окна, без них окна других workspace в общее пользование, без окон — запуск с `Pending.workspace = W` и записью о месте при появлении окна); иначе цикл вне workspace. Ветвь «3б» (поднятие workspace, которого нет ни в одном списке столов) убирается. Чистая функция `app_route` для теста. Проверка: модульный тест `app_route_cases`, проверки 3.2, 3.6
- [ ] 1.9 `daemon.rs`: перетаскивание (решение D8) — `Daemon::pull(app)`: окна приложения (семейства или варианта, кроме скрытых) из любых workspace и свободные получают тег `ws:W`, `W` получает запись о месте (`Join::AppPlace` для приложения файла, полная копия записи для дополнительного приложения сессии другого workspace — по правилу `join_window`), окна встают на текущий стол на место по `open_rect`, без места — `center_rect`, затем `cycle(W, app, true)`. Проверка: модульный тест `pull_record_kind`, проверка 3.2
- [ ] 1.10 `daemon.rs`: отделение окна (решение D9) — `detach_step` возвращает `Leave { app, home, drop_extra }` для окна с другими тегами состава и `Close { … }` для окна без них; `detach` снимает тег `ws:W`, уводит окно по `window_home` или закрывает; `drop_extra_app` различает два случая: окна приложения остались в других workspace — запись снимается только в `W`; окно закрыто и окон нет нигде — запись с командой снимается во всех workspace, как сейчас. Проверка: модульные тесты `detach_leaves_shared_window`, `detach_closes_last_membership`, `drop_extra_keeps_records_of_other_members`, проверка 3.5
- [ ] 1.11 `daemon.rs`: перенос и преемник (решение D10) — `move_desktop` и `raise_on(…, false)` опираются на `window_home`; `remove` уводит окна по нему же. Проверка: модульный тест `successor_keeps_shared_window_with_user`, проверка 3.7
- [ ] 1.12 `daemon.rs`: состояние для панели (решение D11) — `state_json` строит состав по тегам, `status_json` отдаёт у окна поле `workspaces`. Проверка: проверка 3.2 (`workspaced status --json`)
- [ ] 1.13 `config.rs`, `keys.rs`, `main.rs`: ключ `pull` у действия `{ app = … }` в `[[binds]]` (только с `app` и без `workspace`, иначе отказ с цепочкой), генерация вызова `workspaced app <имя> --pull`, признак `--pull` у подкоманды `app` и поле `pull` в запросе сокета. Проверка: модульный тест `pull_action_in_binds`, `workspaced keys --list` на временном конфиге
- [ ] 1.14 `save.rs`: `save_workspace` берёт окна по тегам состава, записывает приложение общего окна под своим именем с `rect` первого экземпляра, входящего в workspace, и ставит записанному окну тег `ws:`; `update_extra_rects` для окна не на столе своего workspace берёт `st.geom[W]` или оставляет запись (решение D12). Проверка: модульные тесты `save_workspace_writes_shared_window`, `extra_rect_of_shared_window_elsewhere`
- [ ] 1.15 Сборка и проверки: `cargo test`, `cargo clippy --all-targets -- -D warnings`, `cargo build --release` — чисто

## 2. Конфиг сессии

- [ ] 2.1 `dot_config/workspaced/config.toml` не меняется (решение D8: клавиши перетаскивания нет). Проверка: `workspaced check` новым бинарником — конфиг корректен, число привязок прежнее
- [ ] 2.2 Подменить бинарник и перезапустить `workspaced.service`. Проверка: `systemctl --user is-active` — active; в журнале строка о выданном составе окнам прежней версии; `hyprctl clients -j` показывает у окон `work` тег `ws:work`, у окон `surf` — `ws:surf`

## 3. Проверка командами (агент)

- [ ] 3.1 На столе 1 (`work`) открыть второе окно Chromium (`chromium --new-window`) — теги `app:chromium#2` и `ws:work`, окно в ячейке `left`
- [ ] 3.2 `workspaced app chrome-ai --pull` на столе 1 — окно `chrome-ai` несёт теги `ws:surf` и `ws:work`, стоит на столе 1 в центре экрана со своим размером, фокус у него; `workspaced status --json` показывает `"workspaces": ["surf", "work"]`, в составе обоих workspace адрес окна; `surf` активен на столе 2, второй Chrome AI не запущен
- [ ] 3.3 `HYPRLAND_INSTANCE_SIGNATURE=… hyprctl eval 'hl.dispatch(hl.dsp.focus({ workspace = 2 }))'` — окно `chrome-ai` переехало на стол 2 в прямоугольник, запомненный для `surf`; обратно на стол 1 — встало на место `chrome-ai` в `work`; при переходе на стол 4 окно остаётся там, где было
- [ ] 3.4 Сдвинуть `chrome-ai` на столе 2 (`hl.dsp.window.move` с адресом), перейти на стол 1 и обратно — на столе 2 окно в сдвинутом прямоугольнике, на столе 1 — на месте из записи `work`
- [ ] 3.5 На столе 1 при активном `chrome-ai` — `workspaced detach`: окно не закрыто, тег `ws:work` снят, окно ушло на стол 2, запись о месте `chrome-ai` у `work` снята; `workspaced raise work` окно не возвращает. Затем `workspaced detach` над вторым окном Chromium — окно закрыто, как на шаге 3
- [ ] 3.6 `surf` убрать из списков столов (`workspaced remove surf --desktop 2`: его окна уходят на `special:pool`), на столе 1 вызвать `workspaced app chrome-ai` — `surf` не поднимается, второй Chrome AI не запускается, окно `chrome-ai` с `special:pool` становится общим для `surf` и `work` и встаёт на столе 1 в центр экрана. Вернуть `surf` на стол 2 (`workspaced raise surf --desktop 2`) и убрать окно из `work` (`workspaced detach` на столе 1)
- [ ] 3.7 Сделать `chrome-ai` общим для `work` и `surf`, поднять на столе 1 `surf`, затем `work` (история стола: `surf`) и выполнить `workspaced move-desktop 5`: окно `chrome-ai` на столе 5 вместе с `work`, преемник `surf` активен на столе 1 без него; переход на стол 1 — окно пришло туда. Вернуть исходное расположение (`work` на столе 1, `surf` на столе 2)
- [ ] 3.8 Сверка: скопировать `~/.local/state/workspaced/sessions/default.toml` в scratchpad; при общем `chrome-ai` перезапустить `workspaced.service` без сохранения сессии — тег `ws:work` снят (записи о месте в снимке нет), окно остаётся в `surf`, в журнале строка о снятом теге; снова сделать окно общим, выполнить `workspaced save-session`, перезапустить демон — окно осталось общим; вернуть сохранённую копию `default.toml` и убрать окно из `work`
- [ ] 3.9 Состояние после проверок вернулось к исходному: активен стол 1, фокус у окна herdr, окна `work` в своих ячейках, окна `surf` на столе 2, у `work` нет записей дополнительных приложений, оставшихся от проверок, `default.toml` совпадает с копией, сделанной в 3.8

## 4. Проверка вживую (пользователь)

- [ ] 4.1 Переход между столами клавишами Super+1 и Super+2 при общем окне `chrome-ai` — окно появляется на столе без заметного мелькания и встаёт на своё место; если запаздывание видно, сказать: переключение столов можно перевести на демон (решение D5)
- [ ] 4.2 Переход на стол кликом по плитке панели — окно `chrome-ai` приходит так же, как при переходе клавишей
- [ ] 4.3 Super+Backspace над общим окном `chrome-ai` на столе 1 — окно исчезает со стола 1 и оказывается на столе 2 в `surf`, открытым; Super+Backspace над ним же на столе 2 — окно закрывается
- [ ] 4.4 Плитки панели после перетаскивания и следования: значок `chrome-ai` виден в плитке того стола, где окно стоит, и не пропадает из неё при переключениях (показ в обеих плитках — шаг 6)

## 5. Спецификации и документация

- [ ] 5.1 Дельты: ws-daemon (новые «Состав workspace» и «Следование общего окна за столом»; «Поднятие workspace на столе», «Цепочка приложения», «Расстановка окон», «Идентификация окон», «Состояние для панели», «Захват открытых окон приложения», «Перенос workspace на стол», «Отделение окна»), ws-config («Цепочки клавиш и привязки», «Запись по команде сохранения»), ws-sessions («Дополнительные приложения сессии»)
- [ ] 5.2 `openspec validate shared-windows --strict` проходит
- [ ] 5.3 `AGENTS.md`, раздел о сессии Hyprland: теги состава `ws:<имя>`, общее окно следует за пользователем между столами, Super+Backspace убирает окно из активного workspace и закрывает только последнее, `workspaced app <имя> --pull`, клавиша приложения, не описанного в активном workspace, открывает его там, если его workspace не запущен
- [ ] 5.4 `docs/window-model.md`: раздел 2.5 («Общие окна», «Следование за столом», Super+Backspace — выполнено шагом 4, правило размещения), раздел 3 (строки ws-daemon «Поднятие», «Захват», «Цепочка приложения», «Состояние для панели», «Идентификация окон»; ws-config «Генерация привязок» остаётся, «Цепочки клавиш и привязки» изменено; ws-sessions «Дополнительные приложения сессии»), раздел 5 — шаг 4 выполнен со ссылкой на архив, в списке спецификаций шага поправлены расхождения, перечисленные в `design.md` (D8)
- [ ] 5.5 Отдельный проход по формулировкам: артефакты изменения, изменённые комментарии в коде демона, правки `AGENTS.md` и `docs/window-model.md`
