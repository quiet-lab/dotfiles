## 1. Демон workspaced (`~/work/pets/workspaced`)

- [x] 1.1 `config.rs`: поле `dialog_title` у приложения (проверка: без `class` отклоняется, выражение компилируется), список `ignore_classes` в корне конфига с помощником `Config::ignored_class`, действие `detach` в записи `[[binds]]`. Проверка: модульные тесты `dialog_title_and_ignore_classes`, `detach_action_in_binds`
- [x] 1.2 `keys.rs`: действие `detach` разворачивается в команду `workspaced detach`. Проверка: `workspaced keys --list` показывает для `SUPER+BackSpace` команду `detach`
- [x] 1.3 `daemon.rs`: чистая функция `join_window` — решение о принятии окна (решения D1—D4), перечисления `Join` и `Free` с причиной для журнала, помощники `taken_names` и `app_name` (последний переехал из `save.rs`). Проверка: модульные тесты `join_takes_new_window_into_the_active_workspace`, `dialogs_and_ignored_classes_stay_free`, `extra_app_names_do_not_repeat`
- [x] 1.4 `daemon.rs`: обработчик `openwindow` выполняет решение — захват приложения конфига, запись о месте для приложения не из workspace, новое дополнительное приложение сессии, свободное окно с причиной (`Daemon::join`, `remember_extra`); `capture_new` принимает готовое место. Проверка: `cargo test`, живая проверка 3.1—3.3
- [x] 1.5 `daemon.rs`: команда `detach` и чистая функция `detach_step` (решение D5) — закрытие окна и снятие записи дополнительного приложения вместе с последним его окном. Проверка: модульные тесты `detach_closes_window_of_the_active_workspace`, `detach_drops_the_last_window_of_a_session_app`, живая проверка 3.5
- [x] 1.6 `daemon.rs`: окно, класс которого подходит `ignore_classes`, не захватывается и при поднятии workspace (`adopt_plan`)
- [x] 1.7 `daemon.rs`: снимок сессии больше не пишется по событию и по команде (решение D6) — вызовы записи убраны из обработчика событий, обработчика запросов, обработчика выхода процесса и старта демона; функция `changes_state` и её тест удалены. Проверка: живая проверка 3.6
- [x] 1.8 `save.rs`: `save-session` проходит по всем столам с активным workspace (`accept_desktop`), принимает окна по `join_window`, обновляет места дополнительных приложений по нынешнему положению окон (`update_extra_rects`) и пишет снимок; ответ команды и вывод клиента — число workspace, окон и принятых окон. Проверка: модульные тесты `session_plan_names_windows`, `session_plan_uniquifies_names`, живая проверка 3.6
- [x] 1.9 `main.rs`: подкоманда `workspaced detach`, новый вывод `save-session`
- [x] 1.10 Сборка и проверки: `cargo test` (72 теста), `cargo clippy --all-targets -- -D warnings`, `cargo build --release` — чисто
- [x] 1.11 `daemon.rs` и `hypr.rs`: порядок окон по глубине при поднятии и переносе workspace (решение D8) — чистая функция `depth_plan`, диспетчер `hypr::d_raise` (`alter_zorder` с явным адресом вместо `bring_to_top`, который действует только на активное окно), признак переноса `moving_ws` в `Daemon::raise`. Проверка: модульный тест `depth_plan_keeps_order_and_lifts_main`, живая проверка 3.8

## 2. Конфиг сессии

- [x] 2.1 `dot_config/workspaced/config.toml`: список `ignore_classes` (`termfilechooser`, `xdg-desktop-portal-gtk`, `pinentry.*`, `zenity`) с пояснением, `dialog_title` у `chromium`, `chrome`, `chrome-ai` и `yandex-browser` (диспетчер задач и отстыкованное окно средств разработки, русский и английский варианты), запись `[[binds]]` `SUPER+BackSpace` → `detach`, новый смысл цепочки `save_session` в комментарии к `[keys]`. Проверка: `workspaced check` — конфиг корректен, 83 привязки
- [x] 2.2 Применить конфиг (`chezmoi apply --force ~/.config/workspaced/config.toml`) и перезапустить `workspaced.service` с новым бинарником. Проверка: `systemctl --user is-active` — active, журнал без ошибок, `hyprctl binds` содержит `SUPER+BackSpace` (modmask 64)

## 3. Проверка командами (агент)

- [x] 3.1 На столе 1 (`work`) `setsid alacritty` — окно принято: в журнале «окно … принято в workspace work дополнительным приложением сессии alacritty», тег `app:alacritty#1`, положение 1520,780 при размере 800×600 (центр экрана 3840×2160), в `workspaced status --json` `alacritty` входит в состав `work`
- [x] 3.2 Второе окно Alacritty — тег `app:alacritty#2`, то же место 1520,780: окна одного приложения стоят стопкой
- [x] 3.3 `workspaced raise work --desktop 3` и обратно на стол 1 — оба окна Alacritty переехали вместе с workspace и вернулись, прямоугольник тот же
- [x] 3.4 `zenity --info` — окно не принято: в журнале «свободно: класс окна в списке ignore_classes», тега нет
- [x] 3.5 `workspaced detach` при активном втором окне Alacritty — окно закрыто, запись приложения осталась; та же команда на последнем окне — окно закрыто и в журнале «дополнительное приложение сессии alacritty снято вместе с последним его окном», состав `work` вернулся к трём приложениям конфига
- [x] 3.6 Снимок сессии: после перезапуска демона, переноса workspace, открытия и закрытия окон время изменения `~/.local/state/workspaced/sessions/default.toml` не менялось; `workspaced save-session` напечатал «снимок сессии записан: workspace — 1, окон в них — 4, принято окон — 0» и обновил время файла
- [x] 3.7 Состояние после проверок вернулось к исходному: активен стол 1, фокус у окна herdr, окна `work` в своих ячейках, окна `surf` на `special:pool`
- [x] 3.8 Порядок окон по глубине: на столе 2 в `surf` боковое окно Chrome поднято поверх главного; `workspaced move-desktop 3` и обратно `move-desktop 2` — порядок `chrome-ai → yandex-browser → chrome` (снизу вверх) тот же, что до переноса, фокус у Яндекс.Браузера; `workspaced raise surf` на том же столе возвращает главное окно наверх; состояние стола после проверки возвращено к исходному

## 4. Проверка вживую (пользователь)

Пункты 4.1—4.6 и 4.8 пользователь проверил 22.09.2026: всё работает.

- [x] 4.1 На столе 1 (`work`) открыть программу из лаунчера (Super+R) — окно должно встать в центр экрана, войти в состав `work` и показаться значком в плитке стола; Super+Tab s и обратно Super+Tab w — окно ездит вместе с workspace
- [x] 4.2 Super+Backspace при активном таком окне — окно закрывается и уходит из состава `work`; Super+Backspace при активном окне Chromium — окно Chromium закрывается, запись `chromium` в конфиге не меняется; Super+Backspace на столе без активного workspace (стол 4 с Firefox) — ничего не происходит
- [x] 4.3 Диспетчер задач Chrome (Shift+Esc в окне Chrome) — окно остаётся там, где его открыл браузер, в ячейку `left` не встаёт и в состав `surf` не входит. Если оно всё-таки принято, значит заголовок диспетчера задач в этой сборке другой: сказать, какой именно, и `dialog_title` в конфиге будет поправлен
- [x] 4.4 Отстыкованное окно средств разработки (F12, затем «открыть в отдельном окне») — так же остаётся свободным
- [x] 4.5 Диалог «Сохранить как» в браузере и выбор файла из yazi — остаются свободными по `ignore_classes`
- [x] 4.6 Ctrl+Super+S, Ctrl+Super+S — в журнале демона строка «снимок сессии записан: workspace — N, окон в них — M, принято окон — K», `config.toml` не изменился (`chezmoi status` пуст)
- [ ] 4.7 Восстановление после перезагрузки: расставить окна как удобно, нажать Ctrl+Super+S дважды, перезагрузиться — состояние должно вернуться к сохранённому вместе с принятыми окнами. Проверка требует закрытия сессии, поэтому её делает пользователь: агент проверить её не может, так как его терминал живёт в этой же сессии
- [x] 4.8 Изменения после последнего снимка не переживают перезапуск: перенести workspace на другой стол, не сохраняя, и выполнить `systemctl --user restart workspaced.service` — workspace вернётся на стол из снимка. Если такое поведение неудобно, сказать: правило принято 22.09.2026 и меняется отдельным решением
- [ ] 4.9 Порядок окон по глубине при переносе: в `surf` на столе 2 щелчком поднять боковое окно Chrome поверх главного, нажать Ctrl+Super+3 и обратно Ctrl+Super+2 — Chrome должен остаться верхним окном, а фокус уйти к главному окну Яндекс.Браузера; Super+Tab s на том же столе возвращает главное окно наверх

## 5. Спецификации и документация

- [x] 5.1 Дельты: ws-daemon («Посторонние окна», «Захват открытых окон приложения», «Состояние для панели», «Поднятие workspace на столе», новое «Отделение окна»), ws-config («Файл конфига и его разделы», «Цепочки клавиш и привязки», «Запись по команде сохранения»), ws-sessions («Файлы сессий», «Дополнительные приложения сессии»), hyprland-binds («Раскладка клавиш сессии»)
- [x] 5.2 `openspec validate windows-join-workspace --strict` проходит
- [x] 5.3 `AGENTS.md`: принятие окон автоматическое, Super+Backspace, новый смысл Ctrl+Super+S, `dialog_title`, `ignore_classes`
- [x] 5.4 `docs/window-model.md`: разделы 2.5 и 2.7, таблица раздела 3, состояние шага 3 в разделе 5
- [x] 5.5 `docs/open-questions/hyprland-window-parent.md` — признак диалога у композитора, с условием пересмотра; черновики обращений в Hyprland и Chromium в `docs/upstream/` (не отправлены)
- [x] 5.6 Отдельный проход по формулировкам: артефакты изменения, комментарии в `config.toml` и в изменённом коде демона

## 6. Порядок архивации

- [ ] 6.1 Изменение `qs-notifications` правит то же требование hyprland-binds «Раскладка клавиш сессии». Дельта этого изменения — надмножество его дельты, поэтому архивировать надо сначала `qs-notifications`, затем `windows-join-workspace`. Если порядок окажется обратным, после архивации `qs-notifications` вернуть в требование строку про Super+Backspace, сценарий «Отделение окна» и новый смысл Ctrl+Super+S
