## 1. Демон workspaced (`~/work/pets/workspaced`)

- [ ] 1.1 `config.rs`: поля `family` и `rect` у `App`, `cmd` становится необязательным; проверка требует хотя бы одно из `cmd` и `class`, отклоняет `family`, называющий отсутствующее приложение, вариант другого варианта и само приложение, и отклоняет имя приложения с двоеточием или `#`; прежние проверки (`title` без `class`, разбор выражений) сохраняются. Проверка: тесты на принятие правильного конфига и на каждую ошибку, `cargo test`
- [ ] 1.2 `daemon.rs`: тег экземпляра `app:<имя>#<номер>` — разбор тега (номер, тег без номера от прежней версии, звёздочка тега из правила композитора) и выбор наименьшего свободного номера среди живых окон приложения. Проверка: модульные тесты разбора и выбора номера; на живом окне `hyprctl dispatch tagwindow +app:probe#1 address:…` и `hyprctl clients -j` подтверждают, что композитор принимает такой тег (тег после проверки снять); при отказе взять разделитель `-` и записать это в `design.md`
- [ ] 1.3 `daemon.rs`: вместо поиска одного окна (`find_app_window`) — набор окон приложения по возрастанию номера экземпляра, где окна вариантов входят и в набор своего семейства. Проверка: модульные тесты на порядок экземпляров и на окно варианта в наборе семейства
- [ ] 1.4 `daemon.rs`: захват (`adopt_untagged`, `pick_candidate`) берёт все подходящие окна, выполняется и для приложения, у которого окна уже есть, сопоставляет сначала варианты, затем семейства, а при нескольких подходящих вариантах одного семейства отдаёт окно первому по имени с предупреждением в журнале. Проверка: модульные тесты (три окна Chromium получают номера 1—3; окно, подходящее варианту и семейству, достаётся варианту; окно на `special:hidden` не берётся)
- [ ] 1.5 `daemon.rs`: место окна по правилу «ячейка или `rect` приложения в workspace → то же у его семейства → `rect` приложения → центр экрана»; расставляются все окна приложения одним прямоугольником, порядок по глубине не меняется, наверх поднимается только окно, получающее фокус. Проверка: модульный тест правила места, `cargo test`
- [ ] 1.6 `daemon.rs`: обмен ячеек (`make_main`) переставляет стопки целиком, фокус получает первый экземпляр, второй экземпляр по цепочке не запускается; приложение без `cmd` и без окон оставляет место пустым и пишет строку в журнал вместо запуска. Проверка: модульные тесты, `cargo test`, `cargo build --release`
- [ ] 1.7 `save.rs`: у приложения с несколькими окнами в конфиг записывается `rect` первого экземпляра, остальные окна отдельными записями не появляются. Проверка: модульный тест записи, `cargo test`

## 2. Переезд профиля Chrome `AI.dev2026` (до задач, зависящих от класса варианта)

- [ ] 2.1 Сверить размещение профиля только чтением: каталог данных Chrome — `~/.config/google-chrome`, профилю `AI.dev2026` отвечает подкаталог `Profile 1` (по `profile.info_cache` в `Local State`), объём около 338 МиБ; `$HOME` общий с контейнером `fedora-box`, поэтому путь одинаков на хосте и в контейнере. Проверка: вывод `du -sh` и записи `info_cache` приложены к задаче, ничего не изменено
- [ ] 2.2 Закрыть все окна Chrome обоих профилей и убедиться, что процессов `google-chrome` в контейнере и на хосте нет. Проверка: `pgrep -af google-chrome` ничего не выводит
- [ ] 2.3 Скопировать `~/.config/google-chrome/Profile 1` в `~/.config/google-chrome-ai/Default` без изменения исходного каталога. Проверка: `du -sh` обоих каталогов совпадает, в копии есть `Preferences`, `Login Data`, `Extensions`, `History`
- [ ] 2.4 Запустить экземпляр вручную из контейнера командой с `--user-data-dir=$HOME/.config/google-chrome-ai --profile-directory=Default --class=google-chrome-ai --ozone-platform=wayland --enable-features=AcceleratedVideoDecodeLinuxGL,VaapiOnNvidiaGPUs,VaapiIgnoreDriverChecks`. Проверка: `hyprctl clients -j` показывает класс `google-chrome-ai` у окна и у второго окна, открытого пользователем по Ctrl+N
- [ ] 2.5 Пользователь проверяет в новом экземпляре вход в аккаунты, расширения, закладки и историю. Проверка: подтверждение пользователя
- [ ] 2.6 Ярлык варианта: обновить (или создать по образцу экспорта distrobox) `~/.local/share/applications/fedora-box-google-chrome-ai.desktop` с новыми ключами запуска; файл вне chezmoi, поэтому упомянуть его в `docs/host-state.md`. Проверка: запуск из лаунчера rofi открывает нужный экземпляр, `hyprctl clients -j` показывает класс `google-chrome-ai`
- [ ] 2.7 Только после подтверждения пользователя (задача 2.5) удалить прежний профиль `AI.dev2026` из каталога `~/.config/google-chrome` средствами Chrome (диспетчер профилей), чтобы `Local State` остался согласованным. Проверка: в диспетчере профилей экземпляра по умолчанию остался один профиль, каталог `Profile 1` исчез

## 3. Конфиг сессии и правила композитора

- [ ] 3.1 `dot_config/workspaced/config.toml`: семейство `[apps.wezterm]` (`cmd = "wezterm-gui"`, `class = "^org\\.wezfurlong\\.wezterm$"`, без `chain`, ни в одном workspace); `herdr` получает `family = "wezterm"`, аргумент `--class wezterm-herdr` в запуске и `class = "^wezterm-herdr$"` вместо `title`. Проверка: `workspaced check` без ошибок, `workspaced keys --list` показывает Super+T по-прежнему у `herdr`
- [ ] 3.2 `dot_config/workspaced/config.toml`: у `chrome-ai` команда запуска с `--user-data-dir` и `--class` из задачи 2.4 и `class = "(?i)^google-chrome-ai$"`; у `chrome` класс остаётся `(?i)^google-chrome$`. Каталог в аргументе писать как `--user-data-dir=$HOME/.config/google-chrome-ai`: `~` демон подставляет только в начале строки, внутри аргумента он остался бы знаком тильды. Проверка: `workspaced check` без ошибок, в журнале демона после запуска `chrome-ai` виден путь с раскрытым `$HOME`
- [ ] 3.3 `dot_config/hypr/hyprland.lua`: класс `google-chrome-ai` добавлен в правило непрозрачности браузеров `opaque-media`, класс `wezterm-herdr` — в правило размытия терминалов `blur-terminal-focused` (иначе окно herdr теряет размытие вместе с прежним классом). Проверка: после `chezmoi apply` и `hyprctl reload config-only` окно herdr размывается в фокусе, окно нового экземпляра Chrome непрозрачно
- [ ] 3.4 Применить конфиг (`chezmoi apply`), перезапустить `workspaced.service`, выполнить `hyprctl reload config-only`, поднять `work` и `surf`. Проверка: `hyprctl clients -j` показывает теги с номерами экземпляров у всех окон приложений, окна стоят по ячейкам `thirds` и `overlay`, повторных запусков в журнале демона нет

## 4. Проверка вживую (пользователь)

- [ ] 4.1 Super+T, Super+C, Super+E, Super+B, Super+V делают главным те же приложения, что и раньше, Super+Tab w и Super+Tab s поднимают `work` и `surf`
- [ ] 4.2 Второе окно Chromium, открытое по Ctrl+N: после Super+Tab w оба окна стоят в ячейке `left` одно поверх другого, Super+C выводит стопку в главную ячейку с фокусом у первого экземпляра, перенос `work` на другой стол уводит оба окна
- [ ] 4.3 Окна двух профилей Chrome не путаются: после перезапуска браузеров Super+B поднимает окно профиля Default, Super+V — окно `AI.dev2026`, и каждое стоит в своей ячейке `overlay`
- [ ] 4.4 Обычное окно wezterm (терминал агента) остаётся свободным: поднятие `work` его не переносит и в ячейку `center` не ставит

## 5. Спецификации и документация

- [ ] 5.1 `openspec validate app-families --strict` проходит
- [ ] 5.2 `AGENTS.md`: семейства и варианты в конфиге, теги экземпляров, несколько окон у приложения, класс окна herdr, отдельный каталог данных и класс окна профиля `AI.dev2026`; оговорка о неотличимых окнах двух профилей Chrome снимается
- [ ] 5.3 `docs/host-state.md`: каталог данных `~/.config/google-chrome-ai` и ярлык варианта вне chezmoi
- [ ] 5.4 Отдельный проход по формулировкам: артефакты изменения, комментарии в `config.toml`, `hyprland.lua` и в изменённом коде демона
