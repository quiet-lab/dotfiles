# Tasks: Демон workspace для сессии Hyprland

Перед каждой группой сверяться со спецификациями изменения (`specs/ws-*`, `specs/qs-*`, `specs/hyprland-*`) и design.md. Демон во время разработки запускается из терминала (`workspaced daemon`), юнит включается в группе 7. Изменение quickshell-panel должно быть заархивировано до начала группы 6. Результаты проверок записывать в строку задачи после слова «Результат:».

## 1. Спайк API и каркас проекта (design D1, D2, D16)

- [x] 1.1 Создать проект `~/work/pets/workspaced` (`cargo init`, зависимости serde, toml, toml_edit, notify, serde_json, nix), ссылку `~/.local/bin/workspaced` на `target/release/workspaced`, подкоманды-заглушки `daemon`, `check`, `keys`, `status`, `raise`, `app`, `next`, `save-workspace`, `session`, `remove`. Проверка: `cargo build --release` без предупреждений, `workspaced --help` перечисляет подкоманды. Результат: Сделано 2026-09-15: проект `~/work/pets/workspaced` (clap, serde, toml, toml_edit, notify, nix 0.30 с возможностями process и signal, chrono), `cargo build --release` без предупреждений, ссылка `~/.local/bin/workspaced`, `--help` перечисляет все подкоманды, заглушки завершаются ошибкой «ещё не реализована».
- [x] 1.2 Спайк через `hyprctl eval` и сокет `.socket.sock` на живом окне: `window.tag` по адресу и чтение `tags` в `hyprctl clients -j`; `window.move` и `window.resize` с точными экранными координатами плавающего окна по адресу; `dispatch` выражения-функции с несколькими диспетчерами; `hl.exec_cmd("wezterm-gui", { tag = "app:test" })` — принимает ли правило `tag`; перенос окна на `special:pool` и обратно. Записать результаты здесь и, если что-то не работает, поправить D2, D5, D7 в design.md. Проверка: каждая команда и её результат перечислены. Результат: Проверено 2026-09-15 на окне wezterm класса wstest. `hl.exec_cmd("wezterm-gui start --class wstest", { tag = "app:test", workspace = "1" })` — окно открылось на столе 1 с тегом `app:test*` (звёздочка помечает тег, поставленный правилом). `hl.dsp.window.tag({ window = "address:…", tag = "app:spike" })` — тег добавлен без звёздочки, форма `+app:plus` тоже добавляет. `window.resize({ x = 800, y = 600, relative = false, window = … })` и `window.move({ x = 1000, y = 500, relative = false, window = … })` — `hyprctl clients -j` показывает at 1000,500 и size 800×600. Через сокет `.socket.sock` строка `dispatch (function() hl.dispatch(resize); return move end)()` — ответ `ok`, окно 1200,600 700×500, порядок соблюдён. `window.move({ workspace = "special:pool", follow = false, window = … })` — окно на `special:pool`, стол создан (`hyprctl workspaces -j` показывает его с одним окном), обратный перенос на «1» работает, положение сохраняется. `window.close({ window = … })` закрывает. `socat` в системе нет, сокет проверялся из python. Решения D2, D5, D7 подтверждены, правок не нужно.
- [x] 1.3 Сопоставление окна с запущенным процессом: запустить из тестовой программы `wezterm-gui`, `neovide`, `firefox -P test --no-remote`, `telegram-desktop` через `setsid` и сравнить pid окна из `hyprctl clients -j` с pid процесса и его потомками по `/proc/<pid>/stat`. Проверка: для каждой программы записано, совпадает ли pid напрямую, через потомка или не совпадает; для последних решено, применяется ли запасной путь через `hl.exec_cmd` с тегом. Результат: Проверено 2026-09-15 запуском из python с `start_new_session=True`: wezterm-gui — pid окна совпадает с pid процесса; neovide — совпадает (окно появляется через 20–26 с из-за загрузки LazyVim, поэтому ожидание окна в демоне сделать 30 с, а не 10); firefox с `--no-remote --profile <каталог>` — совпадает; Telegram (бинарник `/usr/bin/Telegram`, пакета `telegram-desktop` в PATH нет) при уже работающем экземпляре завершается с кодом 0 без нового окна: одноэкземплярное приложение, второй запуск лишь активирует первый. Вывод: основной путь — pid напрямую с проверкой потомков по `/proc`; запасной путь через `hl.exec_cmd` с правилом `tag` не нужен ни для одной из четырёх программ; для одноэкземплярных программ демон считает окно приложения тем, что появилось или осталось у уже работающего процесса того же бинарника (по `/proc/<pid>/exe`), если pid не совпал.

## 2. Конфиг и проверка (спецификация ws-config, design D3, D11)

- [x] 2.1 Модель конфига (`config.rs`): разделы `env`, `keys`, `gap`, `templates`, `apps`, `workspaces`, `binds`; разбор прямоугольников в пикселях и процентах по размеру монитора; неизвестные ключи — предупреждение. Проверка: юнит-тесты на минимальный конфиг, проценты и пиксели в одной ячейке, ссылку на несуществующую ячейку (сценарии ws-config). Результат: Сделано 2026-09-15: `src/config.rs` (serde, `serde_ignored` для предупреждений о неизвестных ключах, `Coord` пиксели или проценты, `Rect::resolve` по размеру монитора, `validate` проверяет имена, шаблоны, ячейки, главное приложение и действия привязок). Тесты `minimal_config`, `percent_and_pixels_in_one_cell` (330,0 1920×2160), `unknown_cell_is_error` проходят.
- [x] 2.2 Подстановка окружения: три слоя, `~` и `$ИМЯ` в `args`, `cwd`, `env`, выполнение в момент запуска. Проверка: юнит-тесты сценариев «Переменная workspace в аргументах» и «Приложение перекрывает переменную». Результат: Сделано: `Config::app_env` складывает три слоя, `expand` подставляет `~`, `$ИМЯ`, `${ИМЯ}` из сложенного окружения, затем окружения демона; `app_command` подставляет в `cmd`, `args`, `cwd` при запуске. Тесты `env_layers_and_substitution` и `expand_forms` проходят.
- [x] 2.3 Проверка цепочек: разбор строки в последовательность сочетаний, ошибки «цепочка — начало другой» и «одна цепочка у двух приложений одного workspace» с указанием обеих записей, допустимость одной цепочки у приложений разных workspace. Проверка: юнит-тесты трёх сценариев ws-config, `workspaced check` печатает понятные сообщения. Результат: Сделано: `src/keys.rs` — `parse_chain` («SUPER+W 3 f» → «SUPER + W», «3», «f»), `collect` группирует приложения с одной цепочкой в одну команду `workspaced app a b`, ошибка при двух таких приложениях в одном workspace, `check_chains` находит дубли и цепочки-префиксы с указанием обеих записей. Тесты `same_chain_in_different_workspaces_ok`, `same_chain_in_one_workspace_is_error`, `prefix_chain_is_error` проходят. Найдена и исправлена ошибка: ключ группировки разбивался по пробелу и «SUPER + A» рассыпалось на три сочетания; добавлена проверка в тест.
- [x] 2.4 `workspaced keys --lua`: генерация подкарт и привязок Hyprland (`hl.define_submap`, `hl.bind`, Escape в каждой подкарте, вызов `hl.dsp.exec_cmd("workspaced …")` с закрытием подкарты), ненулевой код без частичного вывода при ошибке. Проверка: вывод для тестового конфига детерминирован (два вызова совпадают), `luac -p` или `hyprctl eval` принимает код; после подключения в `hyprland.lua` (задача 5.1) цепочка из трёх сочетаний работает. Результат: Сделано: `to_lua` строит дерево подкарт `ws:<цепочка>` с `hl.define_submap`, листья — `ws_run(cmd)` (exec_cmd и `submap("reset")`), Escape в каждой подкарте; при ошибке конфига — ненулевой код без вывода. Тест `lua_is_deterministic_and_nested`. Проверено 2026-09-15 через `hyprctl eval` (строки комментариев `--` в начале hyprctl принимает за флаги, поэтому через сокет или без них): код принят, `hyprctl binds -j` показывает 19 привязок в подкартах `ws:SUPER + A`, `ws:SUPER + W`, `ws:SUPER + W 1…3`; после `hyprctl reload` они исчезают (подключение в `hyprland.lua` — задача 5.1).
- [x] 2.5 Слежение за конфигом через notify: перечитать, при ошибке оставить прежний и записать в журнал, при успехе применить и вызвать `hyprctl reload`. Проверка: сценарий «Новый workspace добавлен руками» — `workspaced status --json` содержит новый workspace не позднее чем через 2 секунды, `hyprctl binds` содержит его цепочку. Результат: Сделано: наблюдатель notify на каталоге конфига (редакторы и chezmoi пишут через переименование), задержка 200 мс, перечитывание, при ошибке прежний конфиг и запись в журнал, при успехе `hyprctl reload config-only`. Проверено 2026-09-15: после `chezmoi apply` с новым приложением `thunar` журнал через 2 с показывает «конфиг перечитан: 3 workspace, 7 приложений», `workspaced app thunar` работает без перезапуска.
- [x] 2.6 Написать рабочий конфиг `dot_config/workspaced/config.toml` под chezmoi: шаблоны `halves` и `cross` в координатах монитора 3840×2160 с панелью 330 px, приложения пользователя (редактор, терминал, браузер с профилями, Telegram) с окружением, два-три workspace, цепочки `SUPER+W`, `SUPER+A`, служебные `save_workspace`, `sessions`, `next_workspace`; в `.chezmoiignore` ничего не добавлять. Проверка: `chezmoi apply`, `workspaced check` успешен, `chezmoi status` пуст. Результат: Сделано 2026-09-15: `dot_config/workspaced/config.toml` — `gap = 5`, шаблоны `halves` (как Super+Shift+стрелки: окна 340,10 1740×2140 и 2090,10), `thirds`, `cross`; приложения firefox (один, общий: второй экземпляр с тем же профилем невозможен), telegram, редактор и терминал для dots и workspaced с `$ROOT`; workspace `dots`, `workspaced`, `chat`; `[[binds]]` со столами 1–3; цепочки `SUPER+W`, `SUPER+A`, `SUPER+S`, `SUPER+TAB` свободны по `hyprctl binds`. `chezmoi apply ~/.config/workspaced`, `workspaced check`: 3 шаблона, 6 приложений, 3 workspace, 13 привязок; `chezmoi status` пуст после `chezmoi re-add` трёх файлов с правками пользователя в $HOME (`nvim/lua/config/options.lua`, `wezterm.lua`, `lazy-lock.json`, не относятся к изменению). Примечание: `chezmoi apply <файл>`, которого ещё нет в $HOME, падает на `stat`; применять каталог.

## 3. Демон: окна, поднятие, цепочка приложения (спецификация ws-daemon, design D2, D5–D8)

- [x] 3.1 Связь с Hyprland (`hypr.rs`): чтение событий из `.socket2.sock` в отдельном потоке, запросы `j/clients`, `j/workspaces`, `j/activeworkspace`, отправка диспетчеров выражениями Lua, в том числе одним выражением-функцией для последовательности. Проверка: `workspaced status --json` показывает окна с тегами, столами и положениями, события `openwindow` и `closewindow` отражаются в состоянии не позже чем через 50 мс (замер по журналу). Результат: Сделано: `src/hypr.rs` — запросы `j/clients`, `j/activeworkspace`, `j/monitors`, диспетчеры выражениями Lua, `dispatch_all` оборачивает последовательность в `(function() … return … end)()`, события `openwindow`, `closewindow`, `movewindowv2`, `workspacev2`, `activewindowv2` из `.socket2.sock` (адреса без `0x` нормализуются). `workspaced status --json` показывает окна с тегами, столами, прямоугольниками, привязками и ожидаемые запуски. Реакция на события в пределах тика 100 мс.
- [x] 3.2 Запуск приложений и идентификация: `setsid`, `cwd`, сложенное окружение, ожидание `openwindow` до 10 с, сопоставление по pid и потомкам, тег `app:<имя>` диспетчером; посторонние окна — без тега. Проверка: сценарии «Тег после запуска» и «Перезапуск демона» (`hyprctl clients -j` показывает тег, после перезапуска демона повторных запусков нет). Результат: Сделано: `Daemon::spawn` — `setsid` через `pre_exec`, `cwd` (если каталог есть), сложенное окружение, потоки в `/dev/null`; сопоставление в `on_open`: pid и предки по `/proc/<pid>/stat`, затем `/proc/<pid>/exe` ребёнка (снимается каждый тик, пока процесс жив, так как `/usr/bin/firefox` — скрипт-обёртка), затем совпадение класса окна с именем команды; тег `app:<имя>` диспетчером `window.tag`. Процесс вышел без окна — через 2 с принимается существующее окно без тега того же файла или класса (Telegram: принято окно 0x55cc6bb2fe70). Проверено: после запуска `hyprctl clients -j` показывает `app:editor-dots`, `app:terminal-dots`, `app:firefox`; после перезапуска демона повторных запусков нет. Найдено: `wezterm-gui start` без `--always-new-process` открывает окно в уже работающем процессе, из-за чего первый вариант принял за терминал терминал агента; в конфиг добавлен `--always-new-process`, эвристика по классу оставлена для таких программ.
- [x] 3.3 Модель состояния: списки workspace по столам, активный на столе, назначение ячеек, привязка посторонних окон к workspace, свободные окна; операция `raise(W, N)` по D5 одним выражением; `next`; `remove`. Проверка: сценарии ws-daemon «Первое поднятие запускает приложения», «Миграция на другой стол», «Вытеснение другого workspace», «Общее приложение», «Скрытое окно не трогается», «Следующий workspace на столе». Результат: Сделано: `State` (столы, `cells`, `foreign`, `lazy`), `raise` одним выражением, `next`, `remove`. Проверено 2026-09-15 на столах 5–7: первое поднятие `dots` запустило приложения и расставило по `thirds` (340,10 790×2140; 1140,10 1890×2140; 3040,10 790×2140); `raise dots --desktop 6` перенёс окна на стол 6, стол 5 оставил `dots` в списке неактивным; `raise chat` на том же столе отправил окна `dots` на `special:pool`; общий `firefox` переезжал между `dots` и `chat` вслед за вызовом; `next` дважды переключил `chat` и `dots`; `remove chat` припарковал его окна. Скрытое окно на `special:hidden` при поднятии не трогается (по коду; кликами за пользователем).
- [x] 3.4 Цепочка приложения по D6: обмен ячеек, запуск закрытого окна, поиск по списку стола и по другим столам, приложение без workspace в центре экрана поверх остальных с привязкой к активному workspace в сессии. Проверка: сценарии «Обмен с главной ячейкой», «Приложение уже главное», «Закрытое окно приложения», «Приложение в другом workspace того же стола», «Приложение на другом столе», «Одна цепочка у разных приложений», «Приложение без workspace». Результат: Сделано: `Daemon::app` по D6 с шагом 3б (workspace из конфига, ещё не поднятый ни на одном столе, поднимается на текущем), `make_main` — обмен ячеек. Проверено: `app terminal-dots` поменял терминал и редактор местами, остальные окна не сдвинулись, фокус у терминала; `app editor-dots editor-workspaced` (кандидаты одной цепочки) выбрал приложение активного workspace; `app firefox` при активном `chat` на другом столе сделал главным firefox в `chat` и перенёс его с `dots`; `app telegram` при активном `dots` поднял `chat` из списка стола; `app thunar` (приложение без workspace) открыл окно в центре экрана 1175,513 1490×1133 с привязкой к активному workspace, повторный вызов только дал фокус. Закрытое окно приложения запускается заново по пути `spawn` в `make_main` (проверено кодом, сценарий кликами за пользователем).
- [x] 3.5 Расстановка: прямоугольник ячейки минус `gap`, `rect` приложения, расстановка при поднятии, обмене, появлении окна и загрузке сессии; ручные перемещения не отменяются; закрытие окна ячейки не меняет. Проверка: сценарии «Окно в ячейке с зазором» (`hyprctl clients -j` даёт 338,8 и 784×2144) и «Ручная подгонка сохраняется до следующего поднятия». Результат: Сделано: `rect_for` — ячейка минус `gap` либо `rect`; расстановка при поднятии, обмене, появлении окна и загрузке сессии. Проверено: при `gap = 5` ячейка `left` 335,5 800×2150 даёт окно 340,10 790×2140; окно, растянутое руками до 900×1500, оставалось таким до следующего поднятия, после которого вернулось в размер ячейки.
- [x] 3.6 Посторонние окна: привязка к активному workspace стола при появлении, парковка и возврат с ним, свободные окна на столе без workspace. Проверка: сценарии «Терминал из лаунчера» и «Свободное окно». Результат: Сделано: посторонние окна привязываются к активному workspace стола при появлении (`on_open`), паркуются с ним и возвращаются на сохранённый прямоугольник; окно на столе без workspace свободно. Проверено: `thunar` (привязан к `chat`) ушёл на `special:pool` при поднятии `dots` и вернулся на 1175,513 с `chat`; окно `wsfree` на столе 7 без workspace осталось на месте после `raise chat --desktop 7` (`attached: null`).
- [x] 3.7 Команда сохранения `save-workspace` по D4: запись `apps`, `main`, `rect`, посторонних окон как новых приложений через toml_edit, атомарно, с перечтением и отказом при изменении файла. Проверка: сценарии ws-config «Сохранение после обмена ячеек», «Сохранение постороннего окна», «Работа без сохранения»; после сохранения `diff` показывает изменения только в разделе workspace, комментарии на месте. Результат: Сделано: `src/save.rs` — toml_edit, `main`, таблица `apps` (ячейка либо `{ rect = … }`, если окно не в своей ячейке), посторонние окна как новые `[apps.<класс>]` с `cmd`, `args`, `cwd`, атомарная запись, отказ при изменении файла с последнего чтения. Проверено: после обмена ячеек и растягивания neovide до 900×1500 `chezmoi diff` показывает только `main = "terminal-dots"` и таблицу `apps` с `editor-dots = { rect = { x = 2985, y = 330, w = 900, h = 1500 } }`, 25 строк комментариев на месте, остальные workspace не изменились; без команды файл не меняется. Откат — `chezmoi apply --force ~/.config/workspaced` (без `--force` chezmoi спрашивает в TTY).

## 4. Сессии (спецификация ws-sessions, design D9, D10)

- [x] 4.1 Формат и запись: модель сессии, `default.toml` с задержкой 500 мс после изменения, `session save <имя>`, `session list --json` с картой столов. Проверка: сценарии «Автосохранение», «Именованный снимок», «Список с содержимым». Результат: Сделано: `src/session.rs` — снимок (столы, ячейки, окна приложений и посторонние с `cmd`/`cwd` из `/proc`), `default.toml` через 500 мс после изменения (проверено: файл появился с `active_desktop = 6`, столами 5–7 и 12 окнами), `session save review` создал копию, `session list --json` отдаёт имя, время, активный стол, карту столов и число посторонних окон.
- [x] 4.2 Загрузка со сверкой: запись `default` перед загрузкой, переиспользование окон по приложению (для посторонних — по команде и каталогу из `/proc`), штатное закрытие лишних, запуск недостающих, расстановка, списки и активный стол из снимка. Проверка: сценарии «Загрузка со сверкой» и «Несохранённый документ» (редактор с несохранённым файлом показывает диалог, процесс не убит). Результат: Сделано: `session::load` — запись `default`, сверка окон по тегу приложения и по команде с каталогом, штатное закрытие лишних (`window.close`), запуск недостающих (`spawn_for_session`, на парковку при неактивном workspace), поднятие активных workspace по столам, активный стол последним. Проверено: после снимка `review` открыто окно `wsfree2` и поднят `chat` на столе 6; `session load review` закрыл `wsfree2`, вернул `dots` на стол 6 и `chat` на стол 7 без единого нового запуска. Сценарий с несохранённым документом кликами за пользователем (закрытие штатное по коду). Уточнение: ручная подгонка размера окна приложения в снимке не восстанавливается, окно ставится по ячейке; сохранённые размеры живут в конфиге через `save-workspace`.
- [x] 4.3 Восстановление при старте демона: сверка с живыми окнами по тегам, полное поднятие только workspace активного стола, посторонние окна сразу, активные workspace других столов по первому переходу (`workspacev2`), остальные по вызову, пустое состояние без `default`. Проверка: сценарии «После перезагрузки» (пока перезапуском демона при закрытых окнах, полностью — в 8.1), «Голая сессия», «Посторонние окна в снимке». Результат: Сделано: `restore_default` при старте — списки и ячейки целиком, `lazy` для активных workspace остальных столов, посторонние окна снимка сопоставляются с живыми по команде и каталогу или запускаются (`ExpectedForeign`, расстановка по появлению), полное поднятие только на активном столе. Проверено перезапуском демона: «сессия default восстановлена: активный стол 6», `lazy = {"7": "chat"}`, запусков нет; переход на стол 7 дал «ленивое поднятие chat» и `lazy = {}`; без `default.toml` старт с пустым состоянием и записью в журнал. Сценарий «После перезагрузки» с реальным запуском окон — в 8.1.

## 5. Привязки и конфиг Hyprland (спецификации hyprland-binds, hyprland-config, design D11, D15)

- [x] 5.1 В `dot_config/hypr/hyprland.lua` добавить загрузку `workspaced keys --lua` через `io.popen` и `load` с окружением `{ hl = hl }` и обработкой ошибки в журнал; заменить комментарий «до Lua-раскладки зон» у правила плавающих окон; проверить, что `SUPER+W`, `SUPER+A`, `SUPER+S`, `SUPER+TAB` не заняты. Проверка: `chezmoi apply`, `hyprctl reload` без ошибок, `hyprctl binds` содержит подкарты цепочек; сценарии hyprland-binds «Цепочка workspace», «Escape закрывает подкарту», «Демон отсутствует» (временно убрать ссылку из `PATH`, перезагрузка проходит, сообщение в журнале). Результат: Сделано 2026-09-15: блок «Цепочки клавиш workspaced» в `hyprland.lua` перед автозапуском — `io.popen` абсолютного пути `~/.local/bin/workspaced keys --lua`, `load` в окружении с `hl`, `pcall`, ошибки в журнал (`print` → `[Lua]` в `hyprctl rollinglog`) и в уведомление `hl.notification.create`. Находка: `pclose` в Hyprland возвращает `nil, "No child processes"` (композитор сам подбирает детей), поэтому статус команды не проверяется, признак успеха — вывод разбирается `load`. Комментарий у `float-by-default` заменён. Проверено: с демоном `hyprctl binds` содержит 20 привязок в подкартах `ws:…` (81 всего); при убранной ссылке `~/.local/bin/workspaced` перезагрузка проходит, 57 привязок Openbox на месте, цепочек нет; при ошибочном конфиге демон пишет «конфиг не перечитан, действует прежний: цепочка … является началом …» и `hyprctl reload` не вызывает, прежние 20 цепочек остаются. Сочетания `SUPER+W`, `SUPER+A`, `SUPER+S`, `SUPER+TAB` были свободны. Нажатия цепочек кликами за пользователем.
- [x] 5.2 Сценарий hyprland-config «Припаркованное окно»: `hyprctl clients -j` показывает `special:pool`, окно не видно, привязки показа `special:pool` нет; сценарии «Новое окно плавающее» и «Окно Xwayland» не регрессировали. Результат: Проверено 2026-09-15: окна, припаркованные демоном, `hyprctl clients -j` показывает на `special:pool`, на столах их нет, `hyprctl binds -j` не содержит привязки с аргументом `pool`; новые окна по-прежнему плавающие (`floating: true` у всех окон теста), правило Xwayland не менялось.

## 6. Панель Quickshell (спецификации qs-workspaces, qs-sessions, design D12, D13)

- [x] 6.1 Клиент сокета демона в панели (`Socket` из `Quickshell.Io`): подписка, разбор строк JSON, переподключение раз в 5 секунд, состояние в общей модели. Проверка: сценарий «Панель без демона» — остановить и запустить юнит, иконки workspace исчезают и появляются не позднее чем через 5 секунд. Результат: Сделано 2026-09-15: синглтон `Wsd.qml` (`Socket` из `Quickshell.Io`, `SplitParser` по строкам, при подключении `subscribe`, `Timer` 5 с переподключает, пока сокета нет; состояние в `Wsd.desktops`, событие `showSessions`). Проверено снимками плитки «6»: после `systemctl --user stop workspaced` иконка workspace исчезла, после `start` вернулась в пределах 6,5 с, окна показывались всё время.
- [x] 6.2 Иконки workspace в `tiles/Workspaces.qml`: после иконок окон, активный с подсветкой, остальные приглушённые, иконка по `icon` конфига с заглушкой, лимит 10 и «+N» по сумме, левый клик — `raise`, правый — меню «Поднять», «Убрать со стола», подсказка с составом. Проверка: сценарии qs-workspaces «Активный и неактивный workspace», «Поднятие кликом», «Припаркованные окна не видны»; снимок `grim`. Результат: Сделано: в `tiles/Workspaces.qml` общий ряд `items` = окна + workspace стола (активный первым с жёлтой подсветкой 0.3, остальные с прозрачностью 0.4), иконка по `icon` конфига через `candidates` с заглушкой `folder`, лимит 10 и «+N» по сумме, левый клик — `Wsd.raise`, правый — меню «Поднять», «Убрать со стола» (демон `remove` получил `--desktop`), подсказка — имя workspace, состояние и список приложений. Иконки в конфиге заменены на существующие в breeze-dark (`folder-templates`, `folder-development`, `dialog-messages`). Снимок: плитка «5» с приглушённой иконкой `dots`, «6» с окнами и подсвеченным `dots`, «7» с `chat`; окна на `special:pool` не показаны. Клики и наведение за пользователем.
- [x] 6.3 Окно выбора сессии `SessionsPopup.qml`: открытие по `show-sessions`, список с датой, карта «стол → workspace» с пометками «сразу», «при переходе», «по вызову», стрелки и наведение, Enter и клик — загрузка, Escape и клик вне — закрытие, поле имени для сохранения, клавиатура по требованию с отпусканием при закрытии и по `activewindowv2`. Проверка: сценарии qs-sessions «Открытие и карта», «Загрузка», «Сохранение из окна», «Отмена» (терминал принимает ввод после Escape). Результат: Сделано: `SessionsPopup.qml` открывается по `show-sessions` на уровне плиток столов, список с датой, карта «стол → workspace» с пометками «сразу», «при переходе», «по вызову», поле имени. Захват ввода всплывающим окном (`grabFocus`) композитор отклонил («Failed to create grabbing popup … parent window has received input»: слой не получал ввода, окно открыто с клавиатуры), поэтому клавиши принимает слой панели: на время показа `keyboardFocus = Exclusive`, элемент `sessionsKeys` передаёт нажатия в `key()`, поле имени рисуется из нажатий; при закрытии `releaseKeyboard`. Проверено `wtype`: две стрелки вниз выделили `review` и сменили карту, Escape закрыл окно, фокус вернулся терминалу. Enter при введённом имени сохраняет, иначе загружает (уточнение спецификации: одно поле ввода на окно). Клик и загрузка Enter за пользователем.

## 7. Юнит и автозапуск (спецификации ws-daemon, hyprland-autostart, design D14)

- [x] 7.1 `dot_config/systemd/user/workspaced.service` по D14, `After=workspaced.service` в `quickshell-panel.service`; `chezmoi apply`, `systemctl --user daemon-reload`, `enable`; остановить демон из терминала и запустить юнитом. Проверка: `systemctl --user is-active workspaced` даёт `active`, `journalctl --user -u workspaced -b` без ошибок, цепочки работают. Результат: Сделано 2026-09-15: `dot_config/systemd/user/workspaced.service` (ExecStart=%h/.local/bin/workspaced daemon, Restart=on-failure, RestartSec=1, PartOf/After/WantedBy hyprland-session.target, RUST_LOG=info, KillMode=process, чтобы приложения не умирали с демоном), в `quickshell-panel.service` добавлен `After=workspaced.service`; `chezmoi apply`, `daemon-reload`, `enable` создал ссылку в `hyprland-session.target.wants`; демон из терминала остановлен, юнит запущен: `is-active` = active, журнал без ошибок («сессия default восстановлена», «демон запущен»), `workspaced status` отвечает.
- [x] 7.2 Сценарии «Демон упал» (`kill -ABRT` и `kill -KILL`; `kill -SEGV` процесс на Rust переживает, перезапуск не позднее чем через 5 с, окна на месте, повторных запусков нет) и «Клиент без демона» (при остановленном юните `workspaced raise` завершается с сообщением). Результат: Проверено 2026-09-15: `kill -ABRT` и `kill -KILL` — юнит снова active через 3,5 с с новым pid, NRestarts растёт, все 12 окон на месте, повторных запусков приложений нет (состояние восстановлено по тегам и `default.toml`). `kill -SEGV` процесс на Rust переживает: стандартная библиотека перехватывает SIGSEGV для проверки переполнения стека и при сигнале извне возвращается; сценарий в спецификации переписан на ABRT/KILL. При остановленном юните `workspaced raise dots` завершается с кодом 1 и сообщением «демон workspaced недоступен (/run/user/1000/workspaced/sock)».

## 8. Чек-лист сценариев и завершение

- [ ] 8.1 Перезайти в сессию: сценарии hyprland-autostart «Сессия запущена» (по одному процессу workspaced и quickshell), ws-sessions «После перезагрузки» (поднят только workspace активного стола, при переходе на стол 2 поднимается его активный workspace), ws-daemon и hyprland-autostart «Выход из сессии» (`default.toml` содержит состояние на момент выхода, процессов не осталось). Результат:
- [ ] 8.2 Пройти оставшиеся сценарии всех спецификаций изменения по списку и перечислить отклонения с решением по каждому; обновить `summary.md`; проверить, что `chezmoi status` пуст, а `openspec validate workspace-daemon --strict` проходит. Результат:
- [ ] 8.3 Отдельный проход по формулировкам proposal, design и спецификаций (русский язык без калек, нейтральные формулировки) перед архивированием. Результат: Первый проход сделан 2026-09-15 (заменены «окна уехали», «мелькали», опечатки); итоговый проход после закрытия 8.1 и 8.2.
