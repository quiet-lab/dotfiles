## 1. Демон workspaced

- [x] 1.1 `src/config.rs`: раздел `sticky` — `IndexMap<String, StickyChain>` (порядок файла; тип назван `StickyState`: корень и дочерние состояния устроены одинаково), состояние с полями `title`, `apps`, `keys` (`IndexMap`, клавиша: `state`, `action`, `exec`, `dispatch`, `lua`, `exit`, `desc`), `states` (рекурсивно), `enter` у корня; поле `new` у `TargetAction`; раздел не даёт предупреждения о неизвестном ключе (решение D1) — 23.09.2026
- [x] 1.2 `src/keys.rs`: проверка цепочек с выходом — имена, клавиши без `+`, запрет `Escape`/`BackSpace`/мыши, ровно одно действие или дополнение унаследованной клавиши, `exit` без `state`, переход только в дочернее состояние, достижимость состояний, `apps` из `raise`/`spawn`; `enter` — одно звено в общей проверке совпадения, начала цепочки и `reserved` с источником `sticky.<имя>`; ключ `new` только с `app` без `workspace` и `pull` (решения D2, D3, D5, D8) — 23.09.2026
- [x] 1.3 `src/keys.rs`: наследование клавиш приложений — цепочки клавиш приложений из одного сочетания с модификатором ровно Super, клавиша в нижнем регистре, действие `workspaced key '<цепочка>'` или `workspaced key --new '<цепочка>'`; явная запись дополняет или заменяет (решения D5, D6) — 23.09.2026
- [x] 1.4 `src/keys.rs`, `to_lua`: прямая привязка входа, `hl.define_submap("ws-sticky:<путь>", …)` на каждое состояние, клавиши с `ignore_mods = true`, действие без закрытия подкарты, с `exit` — с закрытием, `BackSpace` к родителю (в корне — закрытие), `Escape`, последней — `catchall` с `hl.dsp.no_op()`; в конце кода — закрытие устаревшей подкарты `ws:`/`ws-sticky:` (решения D4, D9) — 23.09.2026
- [x] 1.5 `src/keys.rs`, `to_list`: строки путей `sticky <имя>`, `state <состояние>`, клавиши с действием, флаг `exit`, источник `sticky.<имя>[.<состояние>…]`; пути не участвуют в проверке цепочек (решение D10) — 23.09.2026
- [x] 1.6 `src/keys_help.rs`: группа «Цепочки с выходом» после «Приложения», описания путей и клавиш с « · » и « (выход)», раздел `sticky` в JSON (решение D10) — 23.09.2026
- [x] 1.7 `src/main.rs`, `src/daemon.rs`: ключ `--new` у `key` и `app`, поле `new` запроса сокета; `Daemon::key`/`Daemon::app` с `new` — по пути `AppRoute` новый экземпляр вместо цикла (в режиме обмена мест — сначала главное место), `Daemon::spawn` с признаком «новый»: отдельная запись ожидания без проверки незавершённого запуска (решения D6, D7) — 23.09.2026
- [x] 1.8 Тесты: разбор раздела и каждая ошибка проверки из сценариев ws-config; наследование и дополнение клавиш; код Lua (подкарты, `ignore_mods`, `catchall` последним, `BackSpace` корня и вложенного состояния, закрытие устаревшей подкарты) с проверкой синтаксиса `luac`; строки `--list` и `--json`; `key_route` с `new` для четырёх путей; два ожидания одного приложения и порядок захвата окон: 10 новых тестов (`sticky_*`, `new_instance_action` в `keys.rs`; `sticky_entries_and_section` в `keys_help.rs`; `new_instance_keeps_own_pending`, `new_instance_routes` в `daemon.rs`), всего 153 — 23.09.2026
- [x] 1.9 `cargo test`, `cargo clippy --all-targets -- -D warnings`, `cargo build --release`; `README.md` демона — раздел о цепочках с выходом и ключе `--new`; коммит и push в репозитории демона: clippy и сборка без замечаний, коммит `c142d97`, push; `workspaced.service` перезапущен — 23.09.2026

## 2. Конфиг демона

- [x] 2.1 `dot_config/workspaced/config.toml`: раздел `[sticky.apps]` на `SUPER+S` с состояниями `raise` и `spawn` и клавишей `e = { exit = true }` в обоих, комментарий о смысле раздела; `[keys] sessions` — `SHIFT+SUPER+S` с комментарием о переносе (решение D8) — 23.09.2026
- [x] 2.2 `chezmoi apply ~/.config/workspaced/config.toml`; `workspaced check` проходит; журнал демона: конфиг перечитан, `hyprctl reload config-only` выполнен: подкарты `ws-sticky:apps`, `…/raise`, `…/spawn` в `hyprctl binds -j` — 23.09.2026

## 3. Панель Quickshell

- [x] 3.1 `KeyChains.qml` (синглтон): подписка на `Hyprland.rawEvent` с именем `submap`, начальное чтение `hyprctl submap`, загрузка `workspaced keys --json` при входе в цепочку (разбор по трём событиям процесса, как в `KeysHelp.qml`), текущее состояние из раздела `sticky`, запасной путь из имени подкарты, IPC-цель `chains` с функцией `state` (решение D11) — 23.09.2026
- [x] 3.2 `KeyChainsWindow.qml`: окно layer-shell на слое Overlay, без клавиатуры, с пустой маской ввода, карточка в оформлении плиток (`Theme`, `Tile`) вверху по центру рабочей области: путь, строки клавиш, пометка выхода, нижняя строка; ширина по содержимому: карточка 524 px в корне, x = 1823 = 330 + (3510 − 524) / 2 — 23.09.2026
- [x] 3.3 `shell.qml`, `qmldir`; `chezmoi apply` файлов панели, перезапуск `quickshell-panel.service`, журнал без ошибок QML — 23.09.2026

## 4. Проверка командами (агент)

- [x] 4.1 `workspaced keys --list`: строки `SUPER+S`, `SUPER+S r`, `SUPER+S r v` — `key SUPER+V`, `SUPER+S s v` — `key --new SUPER+V`, `SUPER+S r e` и `SUPER+S s e` с флагом `exit`; `workspaced keys --json`: группа «Цепочки с выходом» после «Приложения», раздел `sticky` с тремя состояниями: все строки на месте, группа «Цепочки с выходом» четвёртой, после «Приложения»; раздел `sticky` — 3 состояния — 23.09.2026
- [x] 4.2 `workspaced keys --lua | luac -p -`; `hyprctl binds -j`: подкарты `ws-sticky:apps`, `ws-sticky:apps/raise`, `ws-sticky:apps/spawn`, в каждой `BackSpace`, `Escape` и одна привязка `catch_all: true`, у клавиш состояний `ignore_mods`; в `ws:SUPER + TAB` привязки `catch_all` нет: `luac -p` проходит; по 1 привязке `catch_all` в трёх подкартах `ws-sticky:`, в `ws:SUPER + TAB` и `ws:SUPER + CTRL + s` нет; поля `ignore_mods` в `hyprctl binds -j` Hyprland 0.56.2 нет, признак проверен пробной привязкой (`design.md`, D13) — 23.09.2026
- [x] 4.3 Проверки отказов `workspaced check` на копии конфига в каталоге `XDG_CONFIG_HOME=<временный каталог>` (рабочий конфиг не трогается): вход `SHIFT+SUPER+S` (совпадает с `keys.sessions`), вход из `reserved`, клавиша `Escape`, клавиша `SHIFT+v`, переход в чужое состояние, недостижимое состояние, дополнение неунаследованной клавиши, `{ workspace = "work", new = true }`: все восемь отказов с названием записи, состояния и клавиши — 23.09.2026
- [x] 4.4 Событие `submap`: запись `.socket2.sock` экземпляра Hyprland, затем `hyprctl eval 'hl.dispatch(hl.dsp.submap("ws-sticky:apps"))'`, то же для `ws-sticky:apps/raise`, затем `submap("reset")`: в записи `submap>>ws-sticky:apps`, `submap>>ws-sticky:apps/raise`, `submap>>`; после каждого шага `qs -c panel ipc call chains state` по сценарию qs-key-chains, слой индикатора в `hyprctl layers` только пока карточка показана: события `submap>>ws-sticky:apps`, `submap>>ws-sticky:apps/raise`, `submap>>`; `chains state`: rows=2, rows=6, hidden; слой карточки в `hyprctl layers` только пока показана; `ws:SUPER + TAB` — hidden — 23.09.2026
- [x] 4.5 Устаревшая подкарта: открыть `ws-sticky:apps/raise` через `hyprctl eval`, убрать состояние `raise` из конфига, дождаться перечитывания по журналу демона — `hyprctl submap` печатает `default`; вернуть конфиг `chezmoi apply --force ~/.config/workspaced/config.toml`: после удаления `raise` — `default`; при возврате конфига внутри `ws-sticky:apps/spawn` подкарта осталась открытой — 23.09.2026
- [x] 4.6 Перезапуск панели при открытой подкарте `ws-sticky:apps/spawn`: `chains state` начинается с «shown ws-sticky:apps/spawn»; затем `submap("reset")`: «shown ws-sticky:apps/spawn rows=6 …» — 23.09.2026
- [x] 4.7 По решению пользователя проверено без браузеров: во временно дополненном рабочем конфиге приложение `wev` (`class = "wev"`, клавиша `CTRL+ALT+SUPER+F10`) на пустом столе 6 — `workspaced app wev --new` дважды подряд (второй запуск до появления первого окна) и `workspaced key --new 'CTRL+ALT+SUPER+F10'`: три окна с тегами `app:wev#1`, `#2`, `#3`, каждое от своего pid, в центре рабочей области 1765,840; окна закрыты по PID, конфиг возвращён `chezmoi apply --force`. Сценарии с Chrome в `surf` и neovide в `work` проверяются нажатиями (5.1) — 23.09.2026

## 5. Проверка пользователем (нажатия)

- [ ] 5.1 Сценарий «Поднять и перейти к открытию» (hyprland-binds, «Цепочка приложений с выходом»): Super+S, R, V, Y, Backspace, S, V, V, E; индикатор показывает путь и клавиши на каждом шаге
- [ ] 5.2 Super не отпущен после Super+S: R и V действуют так же
- [ ] 5.3 Посторонние клавиши (Q, Alt+Tab, Super+Delete) в состоянии «Поднять» поглощаются; при русской раскладке V и R действуют так же, как при английской
- [ ] 5.4 Escape из состояния «Новое окно» и Backspace из корня закрывают цепочку, индикатор исчезает, Shift+Super+S открывает окно выбора сессии

## 6. Документация

- [x] 6.1 `AGENTS.md`, раздел «Сессия Hyprland»: абзац о цепочке Super+S (состояния, Backspace, Escape, индикатор) и о ключе `--new`: отдельный пункт после абзаца о привязках, окно сессий на Shift+Super+S, ссылки на спецификации ws-config, ws-daemon, hyprland-binds и qs-key-chains (подходящего документа в `docs/` нет); `todo.md` — вопрос о проверках раздела 5 и порядок архивации — 23.09.2026
- [x] 6.2 Шапка раздела привязок в `config.toml` — ссылка на раздел `[sticky]`; `openspec validate sticky-chains --strict`: шапка дополнена группой «Цепочки с выходом» и абзацем о разделе `[sticky.<имя>]`, `chezmoi apply`, демон перечитал конфиг без ошибок; проход по формулировкам дельт и `design.md`; проверка проходит — 23.09.2026
- [ ] 6.3 Архивация после `keys-help`, `session-instances`, `live-layout`, `work-area-center`, `askpass-only-quickshell` (порядок — в `todo.md`)
