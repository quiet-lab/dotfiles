## 1. Исследование

- [x] 1.1 Где центр считается по монитору: демон (`center_rect` — положение по умолчанию, `join`, `pull`, запуск без места, `arrange_plan`), окно подсказки (x = 960); композитор, `half`/`place`/`maximize` уже по рабочей области; `reserved` — `[330, 0, 0, 0]` (таблица в `design.md`) — 23.09.2026
- [x] 1.2 Решение о занятой зоне: остаётся 330 px (решение D1) — 23.09.2026

## 2. Демон workspaced

- [x] 2.1 `center_rect(c, area)` по рабочей области, `Daemon::work_area`, параметр `area` у `arrange_plan` и `arrange_layout` (решение D2) — 23.09.2026
- [x] 2.2 Тесты: `center_rect_in_work_area` (1920×1080 → 1125,540; 640×480 → 1765,840, как у композитора), `arrange_plan_places_apps_and_stacks_free_windows` с центром области; `cargo test` — 143 теста, `cargo clippy --all-targets -- -D warnings` и `cargo build --release` без замечаний — 23.09.2026
- [x] 2.3 Коммит `5b56928`, push; перезапуск `workspaced.service`, стол 1, фокус у herdr — 23.09.2026

## 3. Панель и конфиги

- [x] 3.1 `KeysHelpWindow.qml`: карточка по центру рабочей области (решение D3; изменение `keys-help`, D6) — 23.09.2026
- [x] 3.2 `shell.qml`: комментарий к `exclusiveZone` — почему 330 px — 23.09.2026
- [x] 3.3 `config.toml`: описание Alt+Super+Enter «по центру рабочей области», комментарий к расстановке; `hyprland.lua`: комментарий к правилу Postman; `chezmoi apply`, `hyprctl reload config-only`, перезапуск панели, `workspaced check` — 23.09.2026

## 4. Проверка командами (агент)

- [x] 4.1 `hyprctl monitors -j | jq '.[0].reserved'` — `[330,0,0,0]`; слои `panel` 330×2160 и `keys-help` 3840×2160 в 0,0 — 23.09.2026
- [x] 4.2 Окно `wev` на столе 8 с временным правилом `center`, `size = { 1920, 1080 }`: 1125,540; сдвиг в 400,300 и диспетчеры Alt+Super+Enter — 1125,540 1920×1080; сдвиг и `workspaced arrange` — 1125,540 (до исправления демон дал бы 960,540); `place center` — 1215,10 1740×2140; `maximize` — 340,10 3490×2140; maximized композитора — 331,1 3508×2158; fullscreen — 0,0 3840×2160; `wev` закрыт по PID, временное правило снято `hyprctl reload config-only` — 23.09.2026
- [x] 4.3 `qs -c panel ipc call keys open` — `state` сообщает `card=1125,10 1920x2140` — 23.09.2026

## 5. Проверка пользователем

- [x] 5.1 Окно, принятое в `work` без места (например, новое окно программы, которой нет в конфиге), встаёт по центру области справа от панели (проверяет пользователь) — подтвердил пользователь 23.09.2026
- [x] 5.2 Alt+Super+Enter и окно подсказки клавиш (Shift+Super+/) — по центру справа от панели (проверяет пользователь) — подтвердил пользователь 23.09.2026

## 6. Архивация

- [ ] 6.1 Архивировать после `live-layout` и `keys-help`: дельты ws-daemon и hyprland-binds взяты из них
