# Tasks: Боковая панель на Quickshell

Перед каждой группой сверяться со спецификациями изменения (`specs/qs-*`, `specs/hyprland-*`) и design.md. Панель во время разработки запускается из терминала (`qs -c panel`), юнит включается в группе 5. Результаты проверок записывать в строку задачи после слова «Результат:».

## 1. Каркас панели (спецификация qs-shell, design D1, D2, D3, D14)

- [x] 1.1 Создать `dot_config/quickshell/panel/` с `qmldir`, синглтоном `Theme.qml` (цвета Tokyo Night, шрифты, размеры из eww.scss) и `shell.qml`: один `PanelWindow` на DP-2 с привязкой к левому краю, шириной и зоной 330 px, пространством имён `panel`, слоем Top, прозрачным фоном и непрозрачным `surfaceFormat` выключенным, фокусом по требованию; внутри — заглушки плиток нужной геометрии (чёрные, альфа 0.9, рамка, скругление). Проверка: `chezmoi apply`, `qs -c panel` стартует без ошибок в stdout, `hyprctl monitors -j` показывает `reserved: [330, 0, 0, 0]`, координаты плиток совпадают с eww.yuck по снимку `grim`. Результат: Сделано 2026-09-10: созданы `qmldir`, `Theme.qml` (синглтон), `Tile.qml` (базовая плитка с заголовком) и `shell.qml` с 21 заглушкой по координатам eww.yuck; `chezmoi apply` без расхождений; `qs -c panel` стартует без ошибок QML (единственное предупреждение — регистрация app ID у портала, безобидное); `hyprctl monitors -j` даёт `reserved: [330, 0, 0, 0]`, `hyprctl layers` показывает слой `panel` 0,0 330×2160 на уровне Top; снимок `grim` показывает все плитки на местах. Шрифты IosevkaTermSlab NF и MesloLGMDZ Nerd Font установлены. Процесс панели называется `qs`.
- [x] 1.2 Добавить в `dot_config/hypr/hyprland.lua` правило слоя `panel` (размытие, `ignore_alpha = 0.2`) по design D2 и обновить сценарий эффектов; `chezmoi apply`, `hyprctl reload`. Проверка сценария «Размытие панели» и «Размытие только под плитками»: на снимке `grim` фон под плитками размыт, промежутки чистые, `hyprctl configerrors` пуст. Результат: Сделано 2026-09-10: в `hyprland.lua` добавлен раздел «Правила слоёв» с `hl.layer_rule` для `^panel$` (blur, `ignore_alpha = 0.2`), `luac -p` и `hyprctl configerrors` без ошибок; на снимке `grim` блики обоев под плиткой часов размыты, точка в промежутке между плитками резкая, промежутки не затемнены. Ореола на скруглённых углах не видно, порог 0.2 оставлен.
- [x] 1.3 Проверить обёртку диспетчеров (design D4): из панели вызвать `Hyprland.dispatch("hl.dsp.focus({ workspace = 2 })")` и убедиться по `hyprctl activeworkspace`, что стол переключился; если нет — реализовать обёртку `Hypr.qml` через `Process` с `hyprctl eval` и записать это здесь. Проверка: оба сценария переключения стола проходят через выбранную обёртку. Результат: Проверено 2026-09-10 пробным файлом через `qs -p`: `Hyprland.dispatch("hl.dsp.focus({ workspace = 2 })")` переключил стол (подтверждено `Hyprland.focusedWorkspace` и `hyprctl activeworkspace`), а `Hyprland.dispatch("workspace 1")` отвергнут с ошибкой обёртки Lua («dispatch in lua is a shorthand for hl.dispatch(...)»). Решение D4 подтверждено, обёртка через `hyprctl eval` не нужна; команды панели — выражения Lua. Замечание: `Hyprland.focusedWorkspace` через 300 мс после старта ещё null, модель заполняется чуть позже.
- [x] 1.4 Проверить фокус по требованию (сценарии «Панель не перехватывает клавиатуру», «Поле фильтра принимает ввод по клику»): при фокусе в терминале клик по плитке не забирает ввод, клик в тестовое поле ввода даёт ввод, клик по окну возвращает его. Результат: Проверено пользователем 2026-09-11 после трёх итераций: панель по умолчанию клавиатуру не просит (`keyboardFocus: None`), режим OnDemand включает только поле фильтра при наведении; панель сама отпускает клавиатуру по событию `activewindowv2` и при уходе указателя с лаунчера, `misc.layers_hog_keyboard_focus = false` в конфиге. Клик по иконке терминала в плитке стола даёт ввод сразу; поле фильтра принимает текст, клик по окну wezterm возвращает ввод окну.

## 2. Плитки без внешних данных (спецификации qs-shell, qs-clock-calendar)

- [x] 2.1 `tiles/Power.qml`: пять кнопок с глифами и командами по спецификации (блокировка, выход диспетчером `exit`, `hyprctl reload`, `systemctl reboot`, `systemctl poweroff`), подсказки при наведении. Проверка: размер глифов и зазоры по снимку совпадают с eww; кнопка блокировки показывает hyprlock; остальные проверяются в группе 6. Результат: Реализовано в `tiles/Power.qml` 2026-09-10: пять плиток с глифами и смещениями из power.scss, цвета наведения, подсказки; глифы перенастроены 2026-09-11 по замечанию пользователя (замер снимка скриптом scratchpad/measure-power.py): замок 29×38 и выход 38×38 по центру плитки, у трёх круглых глифов кольцо 35–36 px с центром в центре плитки с точностью до полпикселя, стрелки перезапуска и перезагрузки в центровке не участвуют, зазор до рамки не меньше 5 px. Затем по уточнению пользователя: наружный диаметр колец перезапуска и перезагрузки приведён к кольцу выключения (35 px, центры в пределах полпикселя от центра плитки по scratchpad/measure-rings.py), замок и выход уменьшены до 24×32 и 32×32. Отступление от eww-значений (44 px) принято пользователем. Клики (блокировка) за пользователем. Проверено пользователем 2026-09-16, работает. Подсказки, блокировка hyprlock, `hyprctl reload` без потери окон; выход проверен последним, сессия завершилась и показан greeter (см. 6.1).
- [x] 2.2 `tiles/Favorites.qml`: три кнопки, каждая открывает yazi в wezterm в своём каталоге командой из favorites.yuck. Проверка: клик по каждой открывает окно класса `yazi` в нужном каталоге (`hyprctl clients -j`). Результат: Реализовано в `tiles/Favorites.qml`: три кнопки с командой wezterm из favorites.yuck (класс `yazi`). Клики за пользователем. Проверено пользователем 2026-09-16, работает. Три кнопки открывают yazi в wezterm в своих каталогах.
- [x] 2.3 `tiles/Clock.qml`: часы с обновлением раз в секунду, дата в ru_RU, календарь на JavaScript по design D10 с навигацией стрелками, возвратом к сегодняшнему дню по клику на время или дату, выделением сегодняшнего и выбранного дня. Проверка по сценариям qs-clock-calendar: перелистывание вперёд и назад, первое число месяца понедельник или нет, выбор дня, возврат; размеры шрифтов по снимку совпадают с eww. Результат: Реализовано в `tiles/Clock.qml`: часы по SystemClock раз в секунду, дата в ru_RU с заглавной буквой дня, календарь на JavaScript (понедельник первый, 6×7, соседние месяцы приглушены, сегодняшний день в круге, выбор кликом, стрелки ±1 и ±12, возврат кликом по времени или дате). На снимке сентябрь 2026 начинается со вторника: первая строка «31 1 2 3 4 5 6». Дробные размеры шрифтов заданы в пунктах (`Theme.pt`, 1 px = 0.75 pt), потому что `font.pixelSize` принимает только целые. Навигация кликами за пользователем. Проверено пользователем 2026-09-16, работает. Листание месяцев стрелками, выбор дня, возврат к сегодняшнему кликом по времени или дате.

## 3. Столы, трей, раскладка, лаунчер (спецификации qs-workspaces, qs-tray-lang, qs-launcher; design D4–D7, D9, D12)

- [x] 3.1 `tiles/Workspaces.qml` и модель окон: восемь плиток по `Hyprland.workspaces`, иконки окон по классу через `DesktopEntries` с правилом префикса заголовка wezterm, обновление по событиям (`refreshToplevels` по `openwindow`, `movewindow`, `closewindow`, `windowtitle`), активный стол выделен, ограничение 10 иконок и «+N», исключения окон панели и слоёв. Проверка: открыть три окна на разных столах, плитки показывают иконки в нужных столах не позднее чем через секунду; окно wezterm с `yazi · …` в заголовке получает иконку yazi. Результат: Реализовано в `tiles/Workspaces.qml`: восемь плиток 274×36 по Hyprland.toplevels с пересборкой по событиям (openwindow, closewindow, movewindowv2, windowtitlev2, activewindowv2, workspacev2) и по загрузке DesktopEntries; иконки — `AppIcon` с перебором кандидатов (запись .desktop, тема breeze-dark по `//@ pragma IconTheme`, файлы в `~/.local/share/icons`, generic). Проверено 2026-09-10 снимками: окна браузеров, Telegram и wezterm в своих столах; wezterm с заголовком «yazi · …» получил иконку yazi из `~/.local/share/icons/yazi/yazi.png`; переключение стола диспетчером переносит жёлтый блок номера. Замечание: `Quickshell.iconPath(name, true)` без pragma темы всегда пуст, так как qt6ct не сообщает тему значков. Проверено пользователем 2026-09-16, работает. Активный стол выделен, иконки соответствуют классам окон.
- [x] 3.2 Скрытые окна: модель «адрес → прежний стол» по событию переноса на `special:hidden`, приглушённая иконка на прежнем столе, левый клик возвращает окно на стол без показа специального стола; окно, скрытое до старта панели, закрепляется за столом, активным при первом появлении. Проверка сценариев qs-workspaces про скрытые окна: Super+Z прячет окно, иконка гаснет на том же столе, клик возвращает. Результат: Реализовано: модель `origins` (адрес → прежний стол) обновляется при каждой пересборке, окна на special:hidden показываются приглушёнными (opacity 0.4) в плитке прежнего стола, при неизвестном происхождении — стола, активного при первом появлении; левый клик возвращает окно `window.move({ follow = false })` и даёт фокус. Диспетчеры по адресу проверены через `hyprctl eval`. Сценарии кликами за пользователем. Проверено пользователем 2026-09-16, работает. Super+Z гасит иконку на прежнем столе, клик по ней возвращает окно.
- [x] 3.3 Действия над окном: клик по свободному месту плитки переключает стол, левый клик по иконке активирует окно (с переходом на его стол), правый открывает `popups/WindowMenu.qml` (активировать, скрыть, закрыть, перенести на стол 1…8; для скрытого окна без пункта «Скрыть»), наведение показывает `popups/HoverInfo.qml` с заголовком при x = 340 на уровне иконки, подсказка не следует за указателем и скрывается при уходе. Проверка сценариев «Действия над окном» и «Подсказка при наведении». Результат: Дополнено 2026-09-11 по просьбе пользователя: повторный левый клик по иконке окна в фокусе скрывает его (сценарий «Повторный клик скрывает окно» добавлен в qs-workspaces). Реализовано: клик по плитке — `hl.dsp.focus({ workspace })`, левый клик по иконке — фокус окна, правый — `MenuPopup` (Показать, Скрыть без скрытых, Закрыть, На стол N), наведение — `HoverInfoPopup` (название, заголовок, класс, стол, признак «Скрыто») на уровне иконки, без переоткрытия внутри одной плитки. Проверка кликами за пользователем. Проверено пользователем 2026-09-16, работает. Повторный клик по иконке окна в фокусе скрывает его, правый клик открывает меню, наведение показывает подсказку с заголовком.
- [x] 3.4 `tiles/Tray.qml`: значки `SystemTray.items` (новые сверху, 20 px, шаг 6 px), левый клик — `activate()`, колесо — `scroll()`, правый клик — меню DBusMenu собственной отрисовкой через `QsMenuOpener` в `popups/TrayMenu.qml` (подменю, флажки, разделители, прозрачный фон Tokyo Night, зазор 10 px от панели). Проверка: значок nm-applet виден, его меню открывается со всеми пунктами и подменю, пункт «Соединения» срабатывает, меню закрывается при уходе указателя и после действия. Результат: Реализовано в `tiles/Tray.qml` и `TrayMenuPopup.qml`: значки SystemTray.items новыми сверху, activate/secondaryActivate/scroll, меню DBusMenu через QsMenuOpener с подменю (Loader, так как рекурсивное вложение типа запрещено), маркерами и разделителями. На снимке значки Telegram и nm-applet. 2026-09-11 по замечанию пользователя содержимое трея сдвинуто к рамке (координаты внутри Tile отсчитываются от внутреннего отступа 10 px): значки в 8 px от рамки, панель громкости и кнопка на своих местах; кнопка «⋮» плитки сети перенесена в строку заголовка. Меню кликами за пользователем. Далее по замечаниям пользователя: динамик по центру плитки (сдвиг 2 px вправо), значки на 2 px ниже, ползунок 230 px, панель громкости 276 px высотой с непрозрачным чёрным фоном, выезжает от верхнего края кнопки динамика. Замечание: наблюдатель трея при старте панели пишет «Ignoring invalid StatusNotifierItem registration» о собственном хосте, на элементы не влияет. Проверено пользователем 2026-09-16, работает. Значок nm-applet, меню с подменю по правому клику.
- [x] 3.5 Кнопка громкости в трее по design D7: устройство вывода PipeWire по умолчанию, глиф с уровнем и признаком «выключен», колесо ±5 % с потолком 100 %, левый клик — pavucontrol, правый — переключение выключения звука, ползунок 0…100 при наведении с задержкой скрытия. Проверка сценариев «Громкость в трее»: изменение громкости колесом отражается в `wpctl get-volume @DEFAULT_AUDIO_SINK@`, изменение из pavucontrol отражается на кнопке без опроса. Результат: Реализовано: PwObjectTracker на Pipewire.defaultAudioSink, глиф по уровню и muted, колесо ±5 %, левый клик pavucontrol, правый — muted, панель с ползунком выезжает за 150 мс и скрывается через 500 мс. Проверка кликами за пользователем. Проверено пользователем 2026-09-16, работает. Колесо меняет громкость, левый клик открывает pavucontrol, правый выключает звук, ползунок при наведении.
- [x] 3.6 `tiles/Lang.qml`: код активной раскладки по событиям `activelayout` из `Hyprland.rawEvent`, начальное состояние из `hyprctl devices -j`, клик — `hyprctl switchxkblayout <клавиатура> next`, подсказка «Переключить раскладку». Проверка: Alt+R и Alt+E меняют код на плитке без опроса, клик переключает по кругу. Результат: Реализовано в `tiles/Lang.qml`. Проверено 2026-09-10: `hyprctl switchxkblayout dh747-w-corne next` меняет подпись на «RU» и обратно на «US» по событию activelayout без опроса; начальное состояние из `hyprctl devices -j` (основная клавиатура dh747-w-corne). Клик по плитке за пользователем. Проверено пользователем 2026-09-16, работает. Клик переключает раскладку по кругу.
- [x] 3.7 `tiles/Launcher.qml` по design D9: сетка 7 × 32 px в ячейках 38 px с прокруткой, список из `DesktopEntries.applications`, разделы и ряд частых приложений, статистика в `~/.local/share/quickshell-panel/usage.json`, фильтр по подстроке без учёта регистра, поле принимает ввод после клика и отдаёт фокус по Escape и после запуска, запуск по клику (`Terminal=true` — через wezterm с `apps.lua`), подсказка с названием при наведении, иконки из темы с фолбэком. Проверка сценариев qs-launcher: фильтр «fire» оставляет Firefox, запуск увеличивает счётчик в файле, установка и удаление .desktop-файла в `~/.local/share/applications` меняет сетку без перезапуска панели. Результат: Реализовано в `tiles/Launcher.qml`: сетка 7×38 px с иконками 32 px в ListView с прокруткой, разделы по Categories с русскими заголовками, ряд частых по `~/.local/share/quickshell-panel/usage.json` (FileView), фильтр по подстроке, Escape очищает и отдаёт фокус (`releaseKeyboard` окна панели: keyboardFocus None → OnDemand), Enter запускает первый результат, Terminal=true — через wezterm с apps.lua. На снимке разделы «Стандартные», «Разработка», «Игры» с иконками. Отклонение: каталог `/var/lib/snapd/desktop/applications` модуль DesktopEntries не читает (только XDG_DATA_HOME и XDG_DATA_DIRS); snapd в системе нет. Проверка фильтра, запуска и фокуса за пользователем. Проверено пользователем 2026-09-16, работает. Ввод после клика в поле, фильтр, Escape возвращает фокус окну, запуск по Enter и клику, ряд частых приложений.

## 4. Сеть, шкалы, диски, погода (спецификации qs-network, qs-monitors, qs-weather; design D8, D11)

- [x] 4.1 Скрипты `scripts/cpu`, `ram`, `gpu`, `cputemp`, `gputemp`, `disks`, `weather` по design D11: копии скриптов eww с выводом одной строки JSON, состояние cpu в `$XDG_RUNTIME_DIR/quickshell-panel/`, кэш погоды в `$XDG_CACHE_HOME/quickshell-weather.json`. Проверка: `sh -n` для каждого, запуск из `~/.config/quickshell/panel/` даёт валидный JSON (`jq .`), повторный запуск cpu даёт ненулевое значение. Результат: Сделано 2026-09-10: скрипты `cpu`, `ram` (с полем total), `gpu`, `cputemp`, `gputemp`, `disks`, `weather`, `netstate`, `netmenu` в `scripts/`, все с `sh -n` без ошибок и валидным JSON (`jq`); `cpu` под нагрузкой всех ядер даёт `{"value":100,"class":"t-crit"}`, при `yes` на одном ядре 3 %. Отступление от design D11: cpu измеряет разность двух чтений /proc/stat с паузой 1 с внутри скрипта, как в eww и как требует спецификация, файл состояния не нужен. Пути к скриптам в QML абсолютные (`Quickshell.shellDir + "/scripts/…"`): Process не ищет их относительно каталога конфигурации.
- [x] 4.2 `tiles/Gauges.qml`: три круговые шкалы 70×70 с дугой 10 px в плитках 100×100, температурные классы и цвета по qs-monitors, интервалы 3 с и 5 с, объём RAM раз в час. Проверка: значения совпадают с `top`/`free`/`nvidia-smi` с точностью до округления, цвет дуги меняется при нагрузке (например, `stress-ng` на минуту). Результат: Реализовано в `tiles/Gauges.qml` (Shape с PathAngleArc). Проверено 2026-09-10: значения CPU 0 %/RAM 19 %/GPU 18 % совпадают со скриптами, под нагрузкой `yes` на всех ядрах дуга CPU красная (#FF073A, t-crit) при 100 %; температуры 60°/54° классом t-mid жёлтые. 2026-09-11 по замечанию пользователя содержимое плиток поднято на 12 px (координаты внутри Tile отсчитываются от внутреннего отступа, из-за чего заголовок и круг сидели ниже, чем в eww).
- [x] 4.3 `tiles/Disks.qml`: строки устройств из скрипта `disks` с полосами заполнения, обновление 30 с. Проверка: набор устройств совпадает с плиткой eww, подключение флешки добавляет строку в течение 30 с. Результат: Реализовано в `tiles/Disks.qml`: на снимке те же устройства, что в плитке eww (ESP, linux 91 % 93G, WINDOWS, nvme2n1p3, limine 2,6G, Data, GhostSpec…, ISOCachyOS), полосы и типы ФС. Подключение флешки не проверялось (нет носителя под рукой), обновление раз в 30 с по таймеру. Проверено пользователем 2026-09-16, работает. Флешка появляется в списке в течение 30 с.
- [x] 4.4 `tiles/Weather.qml`: текущая погода с глифом и цветом по коду WWO, прогноз на 3 дня по частям суток, обновление 30 мин, фолбэк из кэша, заполнитель до первого ответа. Проверка сценариев qs-weather: при отключённой сети панель показывает данные из кэша, при отсутствии кэша — «нет данных». Результат: Реализовано в `tiles/Weather.qml`, скрипт `weather` выдаёт JSON (город из nearest_area, температура, код WWO, три дня по четыре части). Проверено 2026-09-10: сводка «15° Minsk» с глифом пасмурности, прогноз Чт/Пт/Сб с температурами и цветными глифами; с подменённым `curl` (выход 1) скрипт вернул кэш `~/.cache/quickshell-weather.json`, без кэша — `{"error":"нет данных"}`, плитка показывает «нет данных». 2026-09-11 по замечанию пользователя сетка прогноза расширена: отступы справа и снизу до содержимого приведены к отступу слева (24 px от рамки, замер scratchpad/measure-weather.py), шаг строк 12.67 px.
- [x] 4.5 `tiles/Network.qml` и `popups/NetMenu.qml` по design D8: строки ETH, WIFI, VPN из `Quickshell.Networking` и `nmcli` без опросов (поток `nmcli monitor`), клик по плитке — nm-connection-editor, кнопка «⋮» — меню с пунктами из qs-network, закрытие после действия и при уходе указателя. Проверка сценариев qs-network: отключение кабеля меняет строку ETH в течение двух секунд, пункт VPN поднимает и опускает `kozloff-de`, `axata` не трогается. Записать, отражает ли модуль Networking VPN-соединения. Результат: Реализовано в `tiles/Network.qml` со скриптами `netstate` и `netmenu`: строки ETH 192.168.1.11, WIFI отключено, VPN axata на снимке; поток `nmcli monitor` с задержкой 300 мс, меню через MenuPopup с пунктами по состоянию (Wi-Fi, axata, kozloff-de, enp43s0). Модуль Networking 0.3.1 не даёт IPv4 и VPN, спецификация и design D8 переписаны: чтение через nmcli только по событиям. `kozloff-de` имеет тип wireguard, скрипты учитывают типы vpn и wireguard. Отключение кабеля и действия меню за пользователем. Проверено пользователем 2026-09-16, работает. Клик открывает nm-connection-editor, меню «⋮» с пунктами Wi-Fi, VPN и кабеля; при вытащенном кабеле строка ETH меняется за две секунды.

## 5. Автозапуск и удаление пробной панели (спецификация hyprland-autostart, design D13)

- [x] 5.1 Создать `dot_config/systemd/user/quickshell-panel.service` по design D13, `chezmoi apply`, `systemctl --user daemon-reload`, `systemctl --user enable quickshell-panel.service`; закрыть панель из терминала и запустить юнитом. Проверка: `systemctl --user is-active quickshell-panel` даёт `active`, панель видна, `journalctl --user -u quickshell-panel -b` без ошибок QML. Результат: Сделано 2026-09-10: `dot_config/systemd/user/quickshell-panel.service` (ExecStart=/usr/bin/qs -c panel, Restart=on-failure, RestartSec=1, PartOf/After/WantedBy hyprland-session.target), `chezmoi apply`, `daemon-reload`, `enable` создал ссылку в `hyprland-session.target.wants/`; панель из терминала остановлена, юнит запущен: `is-active` даёт `active`, один процесс `qs`, слой panel на месте, в журнале только «Started».
- [x] 5.2 Сценарий «Панель упала»: `kill -SEGV $(pidof quickshell)`; панель возвращается не позднее чем через 5 с. Результат: Проверено 2026-09-10: после `kill -SEGV` юнит через 4 с снова `active` с новым pid, слой panel восстановлен, `NRestarts=1`.
- [x] 5.3 Перезайти в сессию (выход и вход через greeter): сценарий «Сессия запущена» с панелью — через 10 с `pgrep -c quickshell` даёт 1, панель видна, остальные процессы автозапуска на месте; сценарий «Выход из сессии» — после выхода `pgrep quickshell` пуст (проверить из Openbox или tty). Результат: Сделано 2026-09-16: перезаход в сессию в 01:46 (проверка 8.1 workspace-daemon): через минуту после входа один процесс панели (`qs`), слой panel виден, остальные процессы автозапуска по одному; при выходе в 01:46:39 systemd остановил `quickshell-panel.service` вместе с `hyprland-session.target`, процессов не осталось.
- [x] 5.4 Удалить `~/.config/quickshell/nvcheck/` и убедиться, что `qs -c panel` и юнит не ссылаются на него; `chezmoi status` пуст. Результат: Сделано 2026-09-16: `~/.config/quickshell/nvcheck/` удалён, ссылок на него в юните и `hyprland.lua` не было, панель работает, `chezmoi status` пуст.

## 6. Чек-лист сценариев и завершение

- [x] 6.1 Пройти сценарии qs-shell, не покрытые выше: «Окна не залезают под колонку», состав и порядок плиток по снимку рядом со снимком eww из Openbox, кнопки питания «перезапуск» (`hyprctl reload` без потери окон) и «выход» (сессия завершается, greeter показан) — выход проверяется последним. Результат: Сделано 2026-09-16: окон с x < 330 на обычных столах нет, зона панели 330 px (проверка из оболочки); состав и порядок плиток по снимку совпадают с eww; кнопки «перезапуск» и «выход» проверены пользователем, выход последним — сессия завершилась, greeter показан.
- [x] 6.2 Сценарий «Эффекты не дают рывков» с панелью: четыре окна, перемещение окна и переключение столов, `nvidia-smi dmon` без насыщения, оценка плавности пользователем. Результат: Проверено пользователем 2026-09-16, работает. Четыре окна, перемещение и переключение столов без рывков.
- [x] 6.3 Полноэкранное окно (видео в Firefox по F11 или игра): панель не перекрывает его, после выхода из полноэкранного режима панель на месте; записать, нужно ли скрывать панель дополнительно (открытый вопрос design). Результат: Проверено пользователем 2026-09-16, работает. Полноэкранное видео в Firefox по F11: панель его не перекрывает, после выхода панель на месте; дополнительно скрывать панель не нужно, открытый вопрос design закрыт.
- [x] 6.5 Сценарий «Монитор пропадал и вернулся»: выключить и включить монитор кнопкой питания (или пересоединить кабель DisplayPort), после возврата изображения панель на месте без перезапуска юнита, `hyprctl layers` показывает слой panel, `reserved` 330. Результат: Реализовано 2026-09-11 (окно через `Variants` по экранам) после того, как ночью панель исчезла при живом процессе; проверено пользователем 2026-09-11: выключение и включение монитора кнопкой питания возвращает панель без перезапуска.
- [x] 6.6 Сценарии «Наведение не меняет фокус», «Активация из панели без переноса курсора», «Цвет рамки» (design D16): проверить наведением на окно без клика, кликом по иконке окна в плитке стола и по цвету рамки окна в фокусе. Результат: Конфиг изменён 2026-09-11 (`follow_mouse = 2`, `float_switch_override_focus = 0`, `cursor.no_warps = true`, `active_border = rgba(e0af68ee)`), `hyprctl reload` без ошибок, значения подтверждены `hyprctl getoption`; подтверждено пользователем 2026-09-11: фокус и рамка в порядке.
- [x] 6.4 Свести результаты: все сценарии спецификаций пройдены или перечислены отклонения с решением по каждому; обновить `summary.md`; записать здесь. Результат: Сделано 2026-09-16: все сценарии спецификаций qs-* пройдены (кликами пользователя 16.09, из оболочки и по снимкам 10–11.09), отклонений нет. Замечания вне спецификаций: панель при старте пишет предупреждения о недостающих файлах значков (chromium-browser, wine, protontricks, herdr), значки берутся из следующих кандидатов; правки панели для демона (Wsd.qml, SessionsPopup.qml, иконки workspace) вошли в изменение workspace-daemon. `summary.md` обновлён.
