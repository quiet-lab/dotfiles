## 1. Демон workspaced (`~/work/pets/workspaced`)

- [x] 1.1 `config.rs`: поле `mode` у workspace (`swap` по умолчанию, `stack`), проверка значения с указанием workspace и допустимых; таблица `apps` workspace читается с сохранением порядка записи в файле (`IndexMap`). Проверка: модульный тест `workspace_mode_and_app_order` (разбор обоих значений, отказ при опечатке, порядок приложений), `cargo test`. Одного `IndexMap` не хватило: крата `toml` собирала таблицу файла в `BTreeMap` до разбора в наши типы, поэтому в `Cargo.toml` включена её возможность `preserve_order`
- [x] 1.2 `state.rs`: запись цикла (`Cycle`: приложение и окно возврата) по workspace, последнее окно workspace с фокусом (`focus`), запомненные прямоугольники режима `stack` (`geom`). Проверка: `cargo test`
- [x] 1.3 `daemon.rs`: чистая логика цикла — `cycle_step` (шаг по активному окну и порядку экземпляров), `anchor_window` (окно возврата в обоих режимах), `next_app_window` (следующее приложение по порядку записи). Проверка: модульные тесты `cycle_walks_instances_and_returns_to_prev`, `anchor_is_main_window_in_swap_and_previous_in_stack`, `next_app_follows_config_order`
- [x] 1.4 `daemon.rs`: цепочка приложения ведёт цикл вместо однократного обмена ячеек; «выбрать экземпляр» — одна операция с двумя режимами (`select_window`, `swap_to_main`); конец цикла возвращает к prev, иначе переходит к следующему приложению workspace, иначе начинает цикл сначала; только что поднятый workspace начинает цикл заново; приложение вне workspace перебирает свои окна по кругу. Проверка: `cargo test`, `cargo clippy --all-targets -- -D warnings`
- [x] 1.5 `daemon.rs`: событие `activewindow` записывает последнее окно workspace с фокусом; снимок сессии на это событие не пишется. Проверка вживую: два переключения фокуса подряд, время изменения `~/.local/state/workspaced/sessions/default.toml` осталось прежним
- [x] 1.6 `daemon.rs`: поднятие workspace в режиме `stack` не двигает окна, уже стоящие на столе, возвращает вернувшимся окнам запомненный прямоугольник и запоминает прямоугольники перед парковкой на `special:pool` (`remember_geometry`). Проверка: `cargo test`; вживую — три вызова `workspaced app chrome` на `surf` положений окон не изменили
- [x] 1.7 `save.rs`: команда записи workspace в конфиг пишет `mode`, если он не `swap`. Проверка кодом; вживую проверяется задачей 4.7, чтобы не переписывать конфиг пользователя ради проверки
- [x] 1.8 Сборка и проверки: `cargo test` (58 тестов), `cargo clippy --all-targets -- -D warnings`, `cargo build --release` — чисто
- [x] 1.9 `daemon.rs`: поднятие workspace снимает ленивое поднятие сессии для целевого стола (design D15) — иначе первый после перезапуска демона вызов клавиши приложения перекрывался вторым поднятием того же workspace. Проверка вживую: после перезапуска демона первый `workspaced app chrome` со стола 1 выбирает окно `chrome`, строки о ленивом поднятии в журнале нет

## 2. Конфиг сессии

- [x] 2.1 `dot_config/workspaced/config.toml`: `mode = "stack"` у `[workspaces.surf]` с пояснением в комментарии; `work` остаётся в режиме обмена ячеек. Проверка: `chezmoi apply --force` (файл в `$HOME` расходился с источником после команды записи workspace от 22.09.2026), `workspaced check` без ошибок, прежний демон перечитал конфиг по событию одной строкой и предупредил о неизвестном ключе `workspaces.surf.mode`, как и ожидалось до обновления бинарника
- [x] 2.2 Перезапустить `workspaced.service` с новым бинарником. Проверка: `systemctl --user is-active` — active, журнал без ошибок (сессия восстановлена, стартовый `work` поднят), `ss -xp` показывает подключение панели `qs -c panel` к `workspaced/sock`

## 3. Проверка командами (агент)

- [x] 3.1 Со стола 1 `workspaced app chrome` переводит на стол 2, поднимает `surf` и выбирает окно `chrome`: фокус и верхнее место у него, положения всех трёх окон не изменились. До исправления 1.9 первый такой вызов после перезапуска демона перекрывался ленивым поднятием `surf`
- [x] 3.2 Конец цикла в режиме `stack`: третий вызов вернул фокус окну Яндекс.Браузера, в журнале «цикл chrome закончен, возврат к окну 0x…», положения окон прежние
- [x] 3.3 Конец цикла без prev: `workspaced app yandex-browser` при активном окне Яндекс.Браузера выбрал `chrome-ai` — следующее приложение `surf` по порядку записи в разделе (`chrome`, `yandex-browser`, `chrome-ai`), в журнале строка «prev нет — следующее приложение workspace»
- [x] 3.4 Стол и активный workspace возвращены в исходное состояние: активен стол 1, фокус у окна herdr, окна столов 1 и 2 стоят там же, где до проверки

## 4. Проверка вживую (пользователь)

- [ ] 4.1 На столе 1 (`work`, режим обмена ячеек): Super+C ставит окна Chromium в центральную ячейку, повторное нажатие при двух окнах Chromium переводит фокус на второй экземпляр, следующее нажатие возвращает Chromium в левую ячейку, herdr — в центральную и фокус herdr
- [ ] 4.2 На столе 1: Super+E при одном окне neovide работает переключателем — первое нажатие выбирает neovide, второе возвращает прежнюю расстановку и фокус прежнему главному окну
- [ ] 4.3 На столе 1: Super+T при уже главном herdr с фокусом заканчивает цикл — в главную ячейку встаёт следующее приложение `work` по порядку записи (Chromium). Если такое поведение неудобно, сказать об этом: правило «prev нет → следующее приложение workspace» принято 22.09.2026 и меняется отдельным решением
- [ ] 4.4 На столе 2 (`surf`, режим `stack`): Super+B, Super+V и Super+Y поднимают окно нужного браузера наверх и дают ему фокус, ни одно окно не переезжает в другую ячейку
- [ ] 4.5 На столе 2: второе окно Chrome, открытое по Ctrl+N, перебирается той же клавишей, а следующее нажатие возвращает наверх окно, с которого цикл начался
- [ ] 4.6 На столе 2: окно, сдвинутое мышью, остаётся на новом месте после Super+Tab s, а после ухода `surf` на другой стол и обратно возвращается туда, куда его сдвинули
- [ ] 4.7 Ctrl+Super+S, Ctrl+Super+W при активном `surf`: в `config.toml` у `surf` остаётся `mode = "stack"` (после проверки правку можно отменить `chezmoi apply --force`)
- [ ] 4.8 Super+Tab w и Super+Tab s поднимают workspace, как раньше; Super+Tab Tab переключает workspace стола

## 5. Спецификации и документация

- [x] 5.1 `openspec validate key-cycle-stack --strict` проходит
- [x] 5.2 `AGENTS.md`: цикл по клавише и поле `mode` у workspace (кратко, подробности в спецификации)
- [x] 5.3 `docs/window-model.md`: состояние шага 2 в разделе 5 и уточнения раздела 2.3 по итогам реализации
- [x] 5.4 Отдельный проход по формулировкам: артефакты изменения, комментарии в `config.toml` и в изменённом коде демона
