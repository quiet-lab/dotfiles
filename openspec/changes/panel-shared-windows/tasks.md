Архивировать только после `shared-windows` и `workspace-overrides`: изменение опирается на состав workspace тегами и следование общего окна за столом из шага 4 и идёт в серии за шагом 5 (см. `proposal.md`, «Порядок архивации»).

## 1. Демон workspaced

- [ ] 1.1 Изменений нет (решение D10). Проверка: `workspaced status --json` при общем окне `chrome-ai` показывает его адрес в `windows` и `surf`, и `work` — состав для панели уже есть

## 2. Панель (`dot_config/quickshell/panel/tiles/Workspaces.qml`)

- [ ] 2.1 `rebuild`: кроме `byWs` строить `byAddr` (нормализованный адрес → запись окна с полями `at` — стол, где окно стоит, пусто для специальных столов, кроме скрытого; `hidden`; `origin`) и `order` — все окна в порядке `Hyprland.toplevels`, включая окна на `special:pool` (решение D8). Функцию нормализации адреса (`norm`) вынести на уровень `root`
- [ ] 2.2 Свойство `members`: адрес → список workspace по `Wsd.desktops` (все workspace всех столов), пересчитывается привязкой при новом состоянии демона (D6, D8)
- [ ] 2.3 `itemsFor(tile, wsList, withWs)`: окна активного workspace — по его `windows` в порядке `order`, где бы окно ни стояло, с полями `ws` (стол плитки), `at`, `away` (не скрыто и стоит не на столе плитки), `workspaces`; адреса, которых нет в `byAddr`, пропускаются; затем свободные окна `byWs[tile]` — те, чьих адресов нет в `members`; окна, входящие только в неактивные workspace, не показываются (D1, D2). Скрытое окно из состава показывается по составу, а не по столу происхождения (D5)
- [ ] 2.4 Сворачивание неактивных workspace в плитке неактивного стола (D7): при сумме иконок больше 10 и двух и более неактивных workspace — элемент `kind: "fold"` со списком свёрнутых после иконки активного workspace; кнопка 24×24 с жёлтым «+k» в `Repeater` ряда, клики открывают меню `Поднять <имя>` через `Popups.menu`, наведение — подсказку с именами; лимит 10 и серая метка «+N» считаются после сворачивания
- [ ] 2.5 `activate(w)` (D4): для `w.away` — запомнить `pendingFocus = { address, desktop }` и перейти на стол плитки (`focusWs`), проверку повторного клика не выполнять; в `rebuild` — если окно ожидания стоит на столе ожидания, выполнить фокус и подъём наверх одним `chain` и снять ожидание; снять ожидание на `closewindow` этого окна, на `workspacev2` со столом, отличным от стола ожидания, при новом клике и при `Wsd.connected == false`
- [ ] 2.6 `menuFor(w)`: «На стол N» — все столы, кроме `w.at` (для скрытого — кроме стола плитки); «Показать» — через `activate` (D4)
- [ ] 2.7 `infoFor(w)`: строка workspace окна (`workspace: surf, work`), строка стола — стол, где окно стоит, с пометкой «придёт при переходе» для `w.away` (D6)
- [ ] 2.8 `IpcHandler` с целью `workspaces` (D9): `row(desktop: string): string` — ряд плитки JSON-строкой (элементы `ws`, `fold`, `win`, метка `more`), `activate(desktop: string, address: string): void` — левый клик по иконке окна в плитке этого стола
- [ ] 2.9 Комментарий в начале файла и у `itemsFor` описывает ряд по составу, свободные окна, сворачивание и ожидание фокуса; `qmllint` (если доступен) и журнал панели после `chezmoi apply` — без ошибок и предупреждений QML

## 3. Проверка командами (агент)

- [ ] 3.1 `chezmoi apply ~/.config/quickshell/panel`; в журнале панели (`journalctl --user -u quickshell-panel.service`) нет ошибок QML после перечитывания конфигурации
- [ ] 3.2 Исходное состояние: скопировать `~/.local/state/workspaced/sessions/default.toml` в scratchpad; `qs -c panel ipc call workspaces row 1` и `row 2` совпадают по составу окон с `windows` активных `work` и `surf` в `workspaced status --json`, свободные окна — в конце ряда своего стола
- [ ] 3.3 На столе 1: `workspaced key SUPER+SHIFT+V` (окно `chrome-ai` становится общим для `work` и `surf`); `row 1` и `row 2` содержат адрес `chrome-ai`, в `row 2` у него признак `away`, в `row 1` — нет
- [ ] 3.4 `qs -c panel ipc call workspaces activate 2 <адрес chrome-ai>`: по `.socket2.sock` экземпляра Hyprland — `workspacev2` на стол 2, затем `movewindowv2` окна на стол 2, затем `activewindowv2` с его адресом; `hyprctl activewindow -j` — `chrome-ai` на столе 2, окно не на `special:hidden`
- [ ] 3.5 Ожидание снимается: на столе 1 окно `chrome-ai` в `work`; подряд, одной командной строкой, `qs -c panel ipc call workspaces activate 2 <адрес>` и `hyprctl eval 'hl.dispatch(hl.dsp.focus({ workspace = 3 }))'` — композитор остаётся на столе 3, окно `chrome-ai` фокус не получило
- [ ] 3.6 Скрытое общее окно: `hl.dsp.window.move` окна `chrome-ai` на `special:hidden`; `row 1` и `row 2` отдают его с признаком `hidden`; `activate 1 <адрес>` возвращает его на стол 1 с фокусом
- [ ] 3.7 Свободное окно: на столе 1 открыть окно класса из `ignore_classes` (например, `zenity --info`); `row 1` отдаёт его последним элементом, в других рядах его нет
- [ ] 3.8 Сворачивание: поднять на столе 3 три workspace (`workspaced raise <имя> --desktop 3`) так, чтобы в составе активного было не меньше девяти окон (при нехватке окон — перетащить приложения командой `workspaced app <имя> --pull`); `row 3` отдаёт `ws` активного, `fold` с числом свёрнутых и окна, метку `more` — только если элементов после сворачивания больше 10; вернуть столы в исходное состояние (`workspaced remove`, `workspaced detach`)
- [ ] 3.9 Без демона: `systemctl --user stop workspaced`; `row 1` — только окна стола 1 по расположению, общих окон с других столов нет; `systemctl --user start workspaced` — `row 1` снова по составу сразу после появления сокета
- [ ] 3.10 Вернуть состояние: `workspaced detach` над `chrome-ai` на столе 1, окна в своих workspace и ячейках, `default.toml` совпадает с копией из 3.2

## 4. Проверка вживую (пользователь)

- [ ] 4.1 Общее окно Chrome AI (после Shift+Super+V на столе 1) видно в плитке «1» и в плитке «2» одинаковым значком
- [ ] 4.2 Клик по значку Chrome AI в плитке «2», когда вы на столе 1: переход на стол 2, окно на своём месте в `surf` и в фокусе
- [ ] 4.3 Подсказка у значка Chrome AI в плитке неактивного стола называет оба workspace и стол, где окно стоит
- [ ] 4.4 Кнопка свёрнутых workspace в переполненной плитке неактивного стола выглядит уместно и открывает меню поднятия

## 5. Спецификации и документация

- [ ] 5.1 Дельта qs-workspaces («Иконки workspace в плитке стола», «Ограничение числа иконок», «Отображение скрытых окон», «Действия над окном из плитки», «Подсказка при наведении»); после архивации `shared-windows` и `workspace-overrides` сверить, что `openspec/specs/qs-workspaces/spec.md` не менялся
- [ ] 5.2 `openspec validate panel-shared-windows --strict` проходит
- [ ] 5.3 `docs/window-model.md`: раздел 2.6 (общее окно в плитках обоих столов выполнено шагом 6, клик по значку общего окна, свободные окна по-прежнему показываются), раздел 3 (qs-workspaces: «Иконки workspace в плитке стола» — ряд свободных окон остаётся для окон вне состава, а не исчезает; «Отображение скрытых окон», «Действия над окном из плитки», «Подсказка при наведении» — изменены шагом 6; ws-daemon «Состояние для панели» — шаг 6 его не меняет), раздел 4 (предложение о клике выполнено шагом 6: workspace уже активен, фокус после прихода окна), раздел 5 (шаг 6 выполнен; из списка спецификаций шага убрать ws-daemon, добавить три требования qs-workspaces)
- [ ] 5.4 `AGENTS.md`, раздел о сессии Hyprland: нижний ряд плитки — окна активного workspace по составу демона, общее окно в плитках обоих столов, клик по нему переводит на стол плитки; IPC `workspaces row|activate` для проверок
- [ ] 5.5 Отдельный проход по формулировкам: артефакты изменения, комментарии в `Workspaces.qml`, правки `AGENTS.md` и документа
