## 1. Демон workspaced (`~/work/pets/workspaced`)

- [x] 1.1 `state.rs`: поля записи окна в снимке (решение D1) — `SessionWindow::instance: Option<u32>`, `workspaces: Vec<String>`, `rects: BTreeMap<String, PxRect>` (все необязательные, пустые не пишутся); признак нового формата — `instance`. План восстановления в памяти: `RestoreEntry { app, instance, workspaces, desktop, rect, rects, cmd, cwd, state }` с состояниями «ждёт запуска», «запущена (pid)», «ждёт окна процесса приложения», «исполнена», и поле `State::restore` (плюс набор приложений, которые демон запустил при восстановлении, — решение D10). Проверка: модульный тест `session_window_instance_roundtrip` (запись и чтение нового формата; снимок прежнего формата читается, `instance` пуст; неизвестные поля не мешают разбору).
- [x] 1.2 `session.rs`: `snapshot` пишет окна приложений экземплярами (решения D1, D2) — номер из `Client::app_instance`, состав `Client::workspaces()`, `rects` для workspace режима `stack` из `st.geom[W][адрес]`, а для окна на столе, где `W` активен, нынешний прямоугольник; `cmd`/`cwd` по `proc_info` только у экземпляра, чей pid не встречается у других клиентов и чей номер не наименьший среди живых окон приложения. Чистая функция `instance_record(c, clients, st, cfg, proc)` для теста. Проверка: модульный тест `snapshot_records_instances` (два окна одного pid без команды, второй neovide своего процесса с командой, первый экземпляр без команды, `rects` только у `stack`, нынешний прямоугольник на столе активного `W`).
- [x] 1.3 `session.rs`: план восстановления при старте — `restore_plan(entries, clients, cfg) -> Vec<RestoreEntry>`: запись с живым окном того же тега исполнена, её `rects` переходят в `st.geom` для workspace, в которые окно входит сейчас (решение D8); запись без команды при живых окнах приложения снимается со строкой в журнале; снимок прежнего формата плана не даёт (решение D12). Вызов в `restore_default` после `adopt_live_foreign`. Проверка: модульные тесты `restore_plan_skips_live_instances`, `restore_plan_old_format_is_empty`.
- [x] 1.4 `daemon.rs`: запуск записей с командой — `Daemon::spawn_instance(entry)` через `start` и `watch_child`, запись переходит в «запущена (pid)»; в `raise_on` до `adopt_untagged` запускаются записи с командой, у которых в `workspaces` есть поднимаемый workspace; для приложения без окон в `W` — выбор по трём пунктам решения D11 (первый экземпляр ждёт запуска — `spawn`; запущенная запись с командой в `W` — ни отдачи окон, ни запуска; иначе как сейчас). Записи без workspace запускаются в `restore_default` после поднятия активного workspace (решение D4). Чистая функция `raise_restore_step(entries, app, ws, has_windows) -> RaiseStep` для теста. Проверка: модульный тест `raise_waits_for_restored_instance` (neovide с номером 2 в `surf` не получает окно из `work`; первый экземпляр запускается командой приложения; запись без команды не мешает решению D3 шага 4).
- [x] 1.5 `daemon.rs`: сопоставление в `on_open` по порядку решения D5 — `Pending` по pid, запись с командой по pid (предки окна), свободное окно снимка по команде и каталогу, `Pending` по классу, запись без команды приложения окна (`app_for_window`), принятие в workspace стола. Окно, принятое `Pending`, получает запись этого приложения без команды: наименьший номер среди записей с `Pending.workspace`, иначе наименьший вообще. Чистая функция `match_restore(entries, app, pid_hit, workspace) -> Option<usize>` для теста. Проверка: модульные тесты `restore_match_prefers_pid`, `restore_match_by_number_and_workspace`.
- [x] 1.6 `daemon.rs`: окно, сопоставленное с записью (решения D6, D7, D13), — `Daemon::restore_window(c, entry, focus)`: номер записи, если свободен (`free_instance` иначе), теги состава тех workspace записи, которые описывают приложение в эффективном конфиге (`ws_has_app`), место по `window_home` для нынешних активных workspace (ячейка в режиме обмена, `rects[W]` или `rect_for` в режиме `stack`, `special:pool` без активного workspace, свой стол и `rect` у записи без workspace), все `rects` в `st.geom`; `adopt` для окна с записью вызывает его вместо собственной постановки, фокус `Pending.focus` сохраняется. Чистая функция `restore_place(st, cfg, entry, actives, current, mon) -> (Home, Option<PxRect>)` для теста. Проверка: модульные тесты `restored_window_keeps_number_and_membership`, `restored_window_goes_home` (окно из `surf` при активном `work` на текущем столе уходит на стол `surf`; скрытое окно восстанавливается видимым).
- [x] 1.7 `daemon.rs`: снятие ожиданий (решение D10) — `on_child_exit` для записи с командой: предупреждение и снятие; клавиша приложения (`Daemon::app`, `Daemon::key` — по приложению, на котором остановился путь клавиши) снимает ожидание записей без команды этого приложения со строкой в журнале; `save.rs::save_session` и `session::load` снимают весь план. Проверка: модульный тест `restore_wait_ends_on_key_and_save` (над чистой функцией снятия); проверка 3.5.
- [x] 1.8 `session.rs`: `load` по экземплярам (решение D9) — для каждого приложения живые окна по возрастанию номера против записей по возрастанию номера; переиспользованное окно сохраняет номер, получает теги состава из записи (лишние снимаются), встаёт по `restore_place`; окна сверх записей и окна приложений, которых в снимке нет, закрываются штатно; недостающие записи — в план восстановления, первый экземпляр без живых окон запускается командой приложения, приложение отмечается запущенным при восстановлении. `apps_to_start` заменяется планом. Чистая функция `load_pairs(live, entries) -> (Vec<(адрес, запись)>, Vec<адрес>, Vec<запись>)` для теста. Проверка: модульный тест `load_pairs_by_instance_order` (три живых окна против двух записей; одно живое против двух).
- [x] 1.9 Устаревшие тексты в коде: заголовок `session.rs` («`default` по событию») и сообщение `save_named` («сессия default пишется демоном автоматически») приводятся к правилу «снимок пишет только команда сохранения». Проверка: `rg -n 'по событию|автоматически' src/session.rs` не находит устаревших фраз.
- [x] 1.10 Сборка и проверки: `cargo test`, `cargo clippy --all-targets -- -D warnings`, `cargo build --release` — чисто. Выполнено 23.09.2026: 127 тестов, clippy и сборка без замечаний, коммит демона `b3770d0`; отклонения и уточнения — решения D16–D19 в `design.md`.

## 2. Конфиг сессии

- [x] 2.1 `dot_config/workspaced/config.toml` не меняется. Проверка: `workspaced check` новым бинарником — конфиг корректен, число привязок прежнее. Выполнено 23.09.2026: конфиг корректен, привязок 89.
- [x] 2.2 Подменить бинарник и перезапустить `workspaced.service`. Проверка: `systemctl --user is-active` — active; снимок прежнего формата восстановлен без ошибок в журнале, ни одно приложение не запущено повторно. Выполнено 23.09.2026: active; снимок прежнего формата прочитан, в журнале нет запусков приложений.

## 3. Проверка командами (агент)

- [x] 3.1 Скопировать `~/.local/state/workspaced/sessions/default.toml` в scratchpad. На столе 1 (`work`) открыть второе окно Chromium (`chromium --new-window`), в каталоге scratchpad запустить `neovide test.md`, перетащить `chrome-ai` в `work` (`workspaced app chrome-ai --pull`), на столе 2 сдвинуть `chrome-ai` в `surf` (`hl.dsp.window.move` с адресом) и вернуться на стол 1; выполнить `workspaced save-session`. Проверка: в `default.toml` записи `chromium` с номерами 1 и 2 без команды, `neovide` 1 без команды, `neovide` 2 с командой `neovide test.md` и каталогом scratchpad, `chrome-ai` с `workspaces = ["surf", "work"]` и `rects.surf` — сдвинутый прямоугольник; у `rects` нет `work`. Выполнено 23.09.2026: записи `chromium` 1 и 2 и `neovide` 1 без команды, `neovide` 2 с `["neovide", "test.md"]` и каталогом scratchpad, `chrome-ai` (номер 2 — такой был у живого окна) с `workspaces = ["surf", "work"]` и `rects.surf` = 1700,60 1920×2140, `rects.work` нет.
- [x] 3.2 Перезапуск демона без перезагрузки: `systemctl --user restart workspaced`. Проверка: в журнале нет строк о запуске приложений и экземпляров; теги окон (`hyprctl clients -j`) совпадают с тегами до перезапуска; `workspaced status --json` показывает те же окна с теми же номерами; копий окон нет. Выполнено 23.09.2026: в журнале только строка «записей экземпляров 9, ждут восстановления 0», теги окон до и после совпали, план — все записи исполнены, ожиданий запуска нет.
- [x] 3.3 Прямоугольник `stack` пережил перезапуск: перейти на стол 2 — окно `chrome-ai` стоит в сдвинутом прямоугольнике из снимка (до этого шага после перезапуска демона прямоугольник терялся). Выполнено 23.09.2026: переход на стол 2 (ленивое поднятие `surf`) поставил окно в 1700,60 1920×2140.
- [x] 3.4 Имитация перезагрузки для экземпляров отдельными процессами: закрыть оба окна neovide (`hl.dsp.window.close` с адресом), перезапустить демон. Проверка: демон запустил `neovide` командой из конфига и `neovide test.md` в каталоге scratchpad; окна несут `app:neovide#1` и `app:neovide#2` и `ws:work`, стоят стопкой в ячейке `right`; журнал показывает сопоставление по pid. Выполнено 23.09.2026: запущены `neovide` командой конфига и `neovide test.md` в scratchpad; окна `app:neovide#1` и `app:neovide#2` с `ws:work` в 3055,10 1920×2140; второе сопоставлено «по pid запущенного процесса».
- [x] 3.5 Имитация перезагрузки для браузера: закрыть все окна Chromium (процесс браузера завершается), перезапустить демон. Проверка: `chromium` запущен один раз; если браузер открыл оба окна — второе получило `app:chromium#2` и состав из снимка; если одно — в журнале строка об ожидании второго окна, `workspaced app chromium` снимает ожидание (строка в журнале), и окно, открытое после этого `chromium --new-window`, принимается в `work` по общим правилам. Записать, какое значение `session.restore_on_startup` у профиля Chromium и восстановил ли браузер окна. Выполнено 23.09.2026: у профиля Chromium `Default` раздела `session` в `Preferences` нет (значение по умолчанию), браузер открыл одно окно; `chromium` запущен один раз, окно — `app:chromium#1`, в журнале строка «ждём окон … chromium#2»; `workspaced app chromium` снял ожидание (строка «вызов приложения: ожидание окон снимка снято — chromium#2»), окно `chromium --new-window` принято в `work` как `app:chromium#2` по общим правилам.
- [x] 3.6 Загрузка именованной сессии: `workspaced session save t7`; открыть третье окно Chromium; снять `chrome-ai` с `work` (`workspaced detach` на столе 1 при активном окне `chrome-ai`); `workspaced session load t7`. Проверка: третье окно Chromium получило запрос закрытия и закрылось, окна с номерами 1 и 2 не перезапускались; у `chrome-ai` снова теги `ws:surf` и `ws:work`, второй `chrome-ai` не запущен. Выполнено 23.09.2026: третье окно Chromium получило запрос закрытия и закрылось, окна 1 и 2 переиспользованы без перезапуска, у `chrome-ai` снова `ws:surf` и `ws:work`, второй `chrome-ai` не запущен.
- [x] 3.7 Снимок прежнего формата: вернуть копию из 3.1, перезапустить демон. Проверка: снимок прочитан без ошибок, окна живы и не перезапущены, ожиданий вторых окон в журнале нет. Выполнено 23.09.2026: снимок прочитан без ошибок, pid и адреса всех окон прежние, план пуст, строк об ожидании нет; сверка сняла с `chrome-ai` тег `ws:work` (в прежнем снимке записи о месте нет).
- [x] 3.8 Вернуть исходное состояние: закрыть окна проверки (второй Chromium, второй neovide), убрать `chrome-ai` из `work`, удалить `t7.toml`, вернуть копию `default.toml` из 3.1 и перезапустить демон. Проверка: `default.toml` совпадает с копией, окна `work` в своих ячейках на столе 1, окна `surf` на столе 2, фокус у herdr. Выполнено 23.09.2026: второй Chromium и второй neovide закрыты, `t7.toml` удалён, `default.toml` совпадает с копией, окна `work` в ячейках на столе 1, окна `surf` на столе 2, фокус у herdr.

## 4. Проверка вживую (пользователь)

- [ ] 4.1 Перезагрузка: перед ней — два окна Chromium в `work`, второй neovide, открытый из yazi, окно `chrome-ai` в `work` и `surf`, сдвинутое в `surf`; сохранение сессии (Ctrl+Super+S дважды); перезагрузка. После входа: на столе 1 два окна Chromium в ячейке `left`, два окна neovide в ячейке `right`, `chrome-ai` на месте из записи `work`; при переходе на стол 2 окно `chrome-ai` встаёт туда, где его оставили в `surf`. Если второе окно Chromium не вернулось — это настройка браузера «При запуске: продолжить с того же места» (решение D3); сказать, включать ли её. Состояние после входа агент затем проверяет командами (`hyprctl clients -j`, `workspaced status --json`, журнал демона).

## 5. Спецификации и документация

- [x] 5.1 Дельты: ws-sessions («Файлы сессий», «Загрузка сессии», «Восстановление при старте»), ws-daemon («Состав workspace», «Идентификация окон» — текст из дельты `shared-windows`). Проверка: `openspec validate session-instances --strict` проходит. Выполнено 23.09.2026: текст дельт сверен с действующими спецификациями (после архивации шагов 4–6 дельты только добавляют правила шага 7), `openspec validate session-instances --strict` проходит.
- [x] 5.2 Порядок архивации: после `shared-windows`, `workspace-overrides` и `panel-shared-windows` (`proposal.md`, раздел «Порядок архивации»). Проверка: к архивации этого изменения три названных изменения в `openspec/changes/archive/`. Выполнено 23.09.2026: `shared-windows`, `workspace-overrides` и `panel-shared-windows` лежат в `openspec/changes/archive/2026-09-23-*`, раздел «Порядок архивации» и ссылки `design.md` исправлены под архив.
- [x] 5.3 `AGENTS.md`, раздел о сессии Hyprland: снимок сессии хранит экземпляры с составом и прямоугольниками `stack`; после перезагрузки экземпляры отдельных процессов запускаются по записанной команде, вторые окна браузера открывает сам браузер, а демон раскладывает их по записям; ожидание снимается клавишей приложения или сохранением. Выполнено 23.09.2026: абзац о снимке сессии дополнен, память прямоугольников `stack` описана как хранимая в снимке.
- [x] 5.4 `docs/host-state.md`: условие восстановления вторых окон браузеров — «При запуске: продолжить с того же места» (`session.restore_on_startup = 1` в `Preferences` профилей Chromium, Chrome в двух каталогах данных и Яндекс.Браузера), нынешнее значение по итогам проверок 3.5 и 4.1. Выполнено 23.09.2026: запись «`Preferences` профилей браузеров» в разделе «Профили и настройки программ» — ключа нет ни в одном профиле, Chromium по проверке 3.5 открыл одно окно; итог проверки 4.1 дописывается после неё. Заодно исправлена устаревшая строка о `default.toml` («переписывается при каждом изменении»).
- [x] 5.5 `docs/window-model.md`: раздел 2.4 (прямоугольники `stack` в снимке — выполнено шагом 7), 2.7 (способ сохранения экземпляров — решение, а не предложение; ссылка на это изменение), раздел 3 (строки ws-sessions «Файлы сессий», «Загрузка сессии», «Восстановление при старте», «Дополнительные приложения сессии» и ws-daemon «Состав workspace», «Идентификация окон», «Развёртывание окна»), раздел 4 (строка предложения к шагу 7 — выполнено), раздел 5 (шаг 7 выполнен, решения, уточнения замысла; оговорка про сессии переписывается под нынешнее состояние). Выполнено 23.09.2026: разделы 2.4, 2.7, 3 (в том числе строки «Состав workspace» и «Развёртывание окна»), 4 и 5 переписаны; во вводной части документа и раздела 5 отмечено, что серия реализована целиком.
- [x] 5.6 `README.md` демона: формат снимка (поля `instance`, `workspaces`, `rects`, `cmd`/`cwd` у экземпляра) и восстановление экземпляров. Выполнено 23.09.2026: раздел «Сессии и снимок» с примером записи и описанием плана восстановления; исправлены устаревшие строки о записи `default` по каждому событию (описание `session.rs` и пункт о SIGTERM).
- [x] 5.7 Отдельный проход по формулировкам: артефакты изменения, изменённые комментарии в коде демона, правки `AGENTS.md`, `docs/host-state.md`, `docs/window-model.md`, README. Выполнено 23.09.2026: в дельте ws-sessions «это дело приложений» заменено нейтральной формулировкой, в `design.md` «Что есть сейчас» и «Сейчас» в D15 — на «до шага»; комментарии кода демона просмотрены, расхождение одно: комментарий к запуску записей без workspace в `daemon.rs` ссылается на решение D16, а правило о скрытом окне записано в D17 (код в этой задаче не правился).
