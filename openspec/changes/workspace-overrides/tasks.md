Архивировать только после `shared-windows`: дельты «Цепочки клавиш и привязки», «Запись по команде сохранения» и «Цепочка приложения» написаны по тексту его дельт (см. `proposal.md`, «Порядок архивации»).

## 1. Демон workspaced (`~/work/pets/workspaced`)

- [x] 1.1 `config.rs`: запись приложения в workspace — тип `WsApp { place: Placement, chain: Option<String>, mode: Option<String> }` вместо `Placement` в `Workspace::apps` (`IndexMap` сохраняется); разбор трёх форм: строка (ячейка), `{ rect = … }`, таблица с `cell` или `rect` и необязательными `chain`, `mode` (решение D1). Неизвестные ключи записи — предупреждение, как везде. Проверка: модульный тест `ws_app_entry_forms` (все формы, прежний `config.toml` читается без правки) Тип `WsApp` с четвёртым полем `place_error` (решение D15). Сделано 23.09.2026.
- [x] 1.2 `config.rs`: проверки записи в `validate` — ровно одно из `cell` и `rect` (ошибка называет workspace и приложение), `mode` из `MODES` (ошибка называет workspace, приложение, значение и допустимые), ячейка есть в шаблоне, как раньше (решение D8). Проверка: модульный тест `ws_app_entry_errors` Сделано 23.09.2026.
- [x] 1.3 `config.rs`: помощники `Config::ws_entry(ws, app)` (запись приложения или его семейства), `Config::app_key(ws, app)` (ключ приложения в workspace, D2), `Config::app_mode(ws, app) -> Mode` (поле записи; без него режим workspace, а без ячейки в workspace режима `swap` — `Stack`, D7), `Config::responds(ws, chain)` (приложение, отзывающееся в workspace на цепочку; при двух откликах — запись, стоящая раньше в разделе, D8). Проверка: модульный тест `app_key_and_mode_in_workspace` Сделано 23.09.2026.
- [x] 1.4 `state.rs`: `State::cells_of` и остальные места, где читается `Placement` записи (`merge_extra` в `daemon.rs` — записи сессии получают `WsApp` без `chain` и `mode`), переходят на `WsApp::place`. Проверка: прежние тесты расстановки и сессий проходят Сделано 23.09.2026.
- [x] 1.5 `keys.rs`: `collect` собирает отклики на цепочку — собственные клавиши приложений (`apps.<имя>`) и ключи записей workspace (`workspaces.<ws>.apps.<имя>`) — в одну привязку на цепочку с командой `workspaced key '<цепочка>'` (цепочка в каноническом виде, в кавычках оболочки); источник — перечень откликов через запятую (решение D3). Проверка внутри workspace идёт по ключам (`Config::app_key`), ошибка называет workspace, оба приложения и цепочку (D8). Проверка: модульные тесты `override_keys_collected`, `override_frees_own_key`, `override_conflicts_in_workspace`, `override_conflicts_with_workspace_chain`; прежние тесты `keys.rs` с поправкой команды `app` → `key` Сделано 23.09.2026.
- [x] 1.6 `main.rs`, `client.rs`, разбор запроса в `daemon.rs`: подкоманда `workspaced key <цепочка…>` (слова склеиваются пробелом), запрос сокета `{"cmd": "key", "chain": …}`; неизвестная цепочка — строка в журнале и успешный ответ без действия. Проверка: `workspaced key SUPER+V` на временном конфиге Сделано 23.09.2026.
- [x] 1.7 `daemon.rs`: чистая функция `key_route(cfg, desktops, current, chain) -> AppRoute` — пять шагов решения D4 (отклик `W`; собственная клавиша приложения, описанного в `W`; отклик запущенного `V`; случай «в» — `Pull`, без `W` — `Raise`; открыть или цикл вне workspace); `Daemon::key(chain)` выбирает путь и выполняет его теми же ветвями, что `Daemon::app`. «Описано» — через `ws_has_app` (запись приложения или семейства, D6). Проверка: модульный тест `key_route_cases` на все шаги, на смешанный случай (один `V` переопределяет, другой нет), на клавишу переопределения без запущенного `V` и на стол без активного workspace Шаг 3 учитывает описание через семейство (решение D13). Сделано 23.09.2026.
- [x] 1.8 `daemon.rs`: `app_route` для `workspaced app` — описано через `ws_has_app`, запущенный `V` поднимается, только если не переопределяет клавишу приложения, иначе случай «в» (решение D5). Проверка: модульный тест `app_route_cases` дополнен случаем «в» от команды без `--pull` Случай «в» — в тесте `app_route_follows_own_key`. Сделано 23.09.2026.
- [x] 1.9 `daemon.rs`: режим цикла по приложению (решение D7) — `Daemon::cycle` берёт `Config::app_mode(ws, app)` для prev (`main_window` только в режиме `swap`), для запуска без окон (обмен перед запуском только в `swap`) и передаёт его в `select_window` вместо режима workspace для выбора, конца цикла и следующего приложения; `Start::Joined` по-прежнему выбирает без обмена. Режим workspace остаётся в `raise_on`, `raise_target`, `remember_geometry`, `leave_geom`, `follow_plan`, `arrange`, `capture_new`. Проверка: модульные тесты `cycle_mode_of_app` (приложение без ячейки в `swap` не обменивается, явный `mode = "swap"` у записи с `rect` обменивается, `mode = "stack"` у записи с ячейкой не обменивается) Сделано 23.09.2026.
- [x] 1.10 `save.rs`: `save_workspace` правит таблицу `apps` раздела на месте (решение D9) — у существующей записи меняется только место с сохранением формы (строка, `{ rect }`, таблица с `cell`/`rect`, `chain`, `mode`), комментарии и порядок остаются; новые записи (дополнительные приложения сессии, свободные окна стола) добавляются в конец по порядку эффективного конфига. Проверка: модульные тесты `save_keeps_overrides`, `save_keeps_entry_order` Запись без переопределений пишется короткой формой (решение D14). Сделано 23.09.2026.
- [x] 1.11 Сборка и проверки: `cargo test`, `cargo clippy --all-targets -- -D warnings`, `cargo build --release` — чисто Тестов 114. Сделано 23.09.2026.
- [x] 1.12 README демона: раздел о записи приложения в workspace, команде `key` и режиме приложения Сделано 23.09.2026.
- [x] 1.13 `daemon.rs`, `config.rs` (решение D16): первый шаг `key_route` — клавиша ведёт к окну там, где оно сейчас есть: кандидаты цепочки (`Config::key_candidates` — собственная клавиша и ключи записей во всех workspace), входящие в активный workspace описанием или тегом состава окна (`present_candidate`, имена приложений окон с тегом собирает `Daemon::tagged_in_active`), ведут цикл в нём; прежние шаги — только без таких кандидатов. `app_route` тоже учитывает окно с тегом. Проверка: модульные тесты `key_route_follows_window_in_active_ws` (клавиша переопределения `surf` при общем окне в `work` — цикл в `work`; без окна — поднятие `surf`; собственная клавиша; два кандидата) и дополнение `app_route_follows_own_key`; `cargo test` (115), `cargo clippy --all-targets -- -D warnings`, `cargo build --release` — чисто; вживую на столе 1 `workspaced key SUPER+V` при общем окне Chrome AI дал фокус окну и вернул его herdr без смены стола, после `workspaced detach` поднял `surf` на столе 2. Коммиты демона `46372be`, `cd1acc4`. Сделано 23.09.2026.

## 2. Конфиг сессии

- [x] 2.1 `dot_config/workspaced/config.toml` (решение D10): у `chrome`, `chrome-ai`, `yandex-browser` — `chain` Shift+Super+B, Shift+Super+V, Shift+Super+Y; раздел `surf.apps` — `chrome = { cell = "left", chain = "SUPER+B" }`, `yandex-browser = { cell = "center", chain = "SUPER+Y" }`, `chrome-ai = { cell = "right", chain = "SUPER+V" }` в прежнем порядке; комментарий над разделом `surf` объясняет две клавиши браузера. Проверка: `workspaced check` новым бинарником — конфиг корректен, число привязок больше прежнего на три; `workspaced keys --list` показывает `SUPER+B` → `key SUPER+B` с источником `workspaces.surf.apps.chrome` и `SUPER+SHIFT+B` → `key SUPER+SHIFT+B` с источником `apps.chrome` Привязок 89 вместо 86. Сделано 23.09.2026.
- [x] 2.2 `dot_config/hypr/hyprland.lua`: убрать из двух комментариев упоминание мозаики «через Super+Shift+V» — такой привязки нет, а сочетание теперь занято `chrome-ai` Сделано 23.09.2026.
- [x] 2.3 Подменить бинарник, `chezmoi apply` конфигов, перезапустить `workspaced.service`. Проверка: `systemctl --user is-active` — active; `hyprctl binds` содержит Super+Shift+V с командой `workspaced key`; в журнале демона нет ошибок разбора Вместо `hyprctl binds` (там у привязок ссылки на функции Lua) проверена копия `~/.local/state/workspaced/keys.lua`: `hl.bind("SUPER + SHIFT + V", hl.dsp.exec_cmd("workspaced key 'SUPER+SHIFT+V'"))`; в `hyprctl binds -j` есть сочетания 65 B/V/Y. Сделано 23.09.2026.

## 3. Проверка командами (агент)

- [x] 3.1 Скопировать `~/.local/state/workspaced/sessions/default.toml` в scratchpad. На столе 2 с активным `surf`: `workspaced key SUPER+V` при фокусе у Яндекс.Браузера — фокус у `chrome-ai`, положения окон не изменились; `workspaced key SUPER+SHIFT+V` — фокус вернулся к Яндекс.Браузеру (конец цикла) Сделано 23.09.2026.
- [x] 3.2 На столе 1 с активным `work`: `workspaced key SUPER+V` — активен стол 2, фокус у `chrome-ai`, у окна нет тега `ws:work` Сделано 23.09.2026.
- [x] 3.3 На столе 1: `workspaced key SUPER+SHIFT+V` — окно `chrome-ai` несёт теги `ws:surf` и `ws:work`, стоит на столе 1 в центре экрана со своим размером, фокус у него, `surf` активен на столе 2, второй Chrome AI не запущен, ячейки `work` прежние Сделано 23.09.2026.
- [x] 3.4 На столе 1, фокус у herdr: `workspaced key SUPER+SHIFT+V` — окно `chrome-ai` поднято и в фокусе на своём месте, herdr в ячейке `center`; ещё раз — фокус у herdr, ячейки прежние (режим `stack` приложения без ячейки, D7). Затем `workspaced detach` над `chrome-ai` — окно ушло на стол 2 Сделано 23.09.2026.
- [x] 3.5 `workspaced remove surf --desktop 2`, на столе 1 `workspaced key SUPER+V` — `surf` не поднят, окно `chrome-ai` стало общим для `work` и встало в центр экрана; вернуть `surf` на стол 2 (`workspaced raise surf --desktop 2`) и убрать окно из `work` (`workspaced detach`) Сделано 23.09.2026.
- [x] 3.6 Временный конфиг в scratchpad (семейство и режимы, решения D6, D7): `chrome` с `chain = "SUPER+B"`, `chrome-ai` с `family = "chrome"`, workspace `mix` с записью только `chrome`, workspace `pair` с `chrome` и `chrome-ai` по своим ячейкам, запись с `mode = "stack"` в workspace режима `swap`. `XDG_CONFIG_HOME=<каталог scratchpad> workspaced check` (демон берёт путь конфига из `dirs::config_dir`) и модульные тесты на тех же данных: в `mix` Super+B перебирает окна обоих профилей, Super+V — только `chrome-ai`; в `pair` Super+B — только `chrome` Модульные тесты `key_route_cases` (часть о семействе) и `family_key_cycles_variant_windows`; предупреждение о неизвестном ключе записи выводится с путём. Сделано 23.09.2026.
- [x] 3.7 На столе 2 сдвинуть окно `chrome-ai` (`hl.dsp.window.move` с адресом) и выполнить `workspaced save-workspace`: запись `chrome-ai` в `surf` — `{ rect = …, chain = "SUPER+V" }`, порядок `chrome`, `yandex-browser`, `chrome-ai` сохранён, комментарии раздела на месте; вернуть исходный `config.toml` (`chezmoi apply --force ~/.config/workspaced/config.toml`) и проверить `chezmoi status` — пусто Сделано 23.09.2026.
- [x] 3.8 Состояние после проверок вернулось к исходному: активен стол 1, фокус у окна herdr, окна `work` в своих ячейках, окна `surf` на столе 2, у `work` нет записей дополнительных приложений, оставшихся от проверок, `default.toml` совпадает с копией, сделанной до 3.1 Сделано 23.09.2026.

## 4. Проверка вживую (пользователь)

- [ ] 4.1 В `surf` Super+B, Super+V, Super+Y ведут себя как до изменения
- [x] 4.2 На столе 1 с `work` Super+V переводит к `surf` на стол 2, а Shift+Super+V приносит окно Chrome AI на стол 1; на столе 2 оно по-прежнему на своём месте в `surf` — проверено пользователем 23.09.2026: Shift+Super+V принёс окно; Super+V со стола 1 при окне Chrome AI в `work` переводил на стол 2 — пользователь счёл это неверным, правило уточнено (решение D16)
- [ ] 4.3 После 4.2 Shift+Super+V в `work` поднимает окно Chrome AI и возвращает фокус прежнему окну, окна `work` из ячеек не уходят
- [x] 4.4 Super+Backspace над окном Chrome AI в `work` убирает его со стола 1, окно остаётся в `surf` — пользователь сделал это 23.09.2026 при проверке шага 4: окно ушло в `surf`, не закрылось

## 5. Спецификации и документация

- [ ] 5.1 Дельты: ws-config («Файл конфига и его разделы», «Цепочки клавиш и привязки», «Запись по команде сохранения»), ws-daemon («Цепочка приложения»), hyprland-binds («Раскладка клавиш сессии»); после архивации `shared-windows` сверить три требования, взятые из его дельт Дельты написаны и прошли проход по формулировкам 23.09.2026; сверка — после архивации shared-windows.
- [x] 5.2 `openspec validate workspace-overrides --strict` проходит Сделано 23.09.2026.
- [x] 5.3 `AGENTS.md`, раздел о сессии Hyprland: запись приложения в workspace с `cell`/`rect`, `chain` и `mode`; команда `workspaced key`; таблица клавиши приложения целиком (Super+B/V/Y — клавиши `surf`, Shift+Super+B/V/Y — собственные клавиши браузеров, перетаскивание окна в текущий workspace); режим `stack` у приложения без ячейки; фраза «клавиши для этого в конфиге пока нет» снимается Сделано 23.09.2026; добавлено и о правке записей на месте командой записи workspace в конфиг.
- [x] 5.4 `docs/window-model.md`: раздел 2.1 (имена полей переопределения выбраны: `cell`, `rect`, `chain`, `mode` в записи приложения), раздел 2.5 (случай «в» выполнен шагом 5, пример с Shift+Super+V), раздел 3 (ws-config «Файл конфига и его разделы», «Цепочки клавиш и привязки» — изменено шагом 5; «Запись по команде сохранения» — изменено и шагом 5; ws-daemon «Цепочка приложения»), раздел 4 (D11 и D12 `app-families` — пересмотрены шагом 5: семейство Chrome по-прежнему не заводится, клавиши браузеров разделены на клавиши `surf` и собственные), раздел 5 — шаг 5 выполнен со ссылкой на архив, в списке спецификаций шага добавлены «Запись по команде сохранения» и hyprland-binds Сделано 23.09.2026: раздел 5 ссылается на активное изменение, ссылку сменить на архивную при архивации; в разделе 3 заведена таблица hyprland-binds, в разделе 2.2 — абзац о клавишах семейства и варианта.
- [x] 5.5 `docs/host-state.md`: упоминания Super+B, Super+V, Super+Y сверить с новой раскладкой Сделано 23.09.2026; то же в строке о ярлыках в `todo.md`.
- [x] 5.6 Отдельный проход по формулировкам: артефакты изменения, комментарии в коде демона и в `config.toml`, правки `AGENTS.md` и документов Сделано 23.09.2026; правок в коде и `config.toml` не потребовалось.
