## 1. Демон workspaced

- [x] 1.1 `src/config.rs`: поля `desc`, `group` у записи `[[binds]]`, таблица `reserved_desc` в `[keys]` (решение D3) — 23.09.2026
- [x] 1.2 `src/keys.rs`: действие записи вынесено в `bind_act`, подстановка номера серии — в `substitute` (с `desc`), поле `origin` у привязки — 23.09.2026
- [x] 1.3 `src/keys_help.rs`: подписи цепочек, группы и описания по умолчанию, серия одной строкой, зарезервированные сочетания, JSON; `src/main.rs`: ключ `keys --json` (решения D1, D2, D4, D5) — 23.09.2026
- [x] 1.4 Тесты `keys_help` (подписи, группы и описания, порядок групп, ошибка конфига); `cargo test` — 131 тест, `cargo clippy --all-targets -- -D warnings` без замечаний, `cargo build --release` — 23.09.2026
- [x] 1.5 `README.md` демона; коммит `3c2558d`, push — 23.09.2026

## 2. Конфиг демона

- [x] 2.1 `dot_config/workspaced/config.toml`: привязка `SUPER+SHIFT+slash` → `qs -c panel ipc call keys toggle` (решение D8), описания записей `exec`, `dispatch`, `lua`, группы лаунчера и Alt+Tab, `reserved_desc`, пояснение полей в шапке раздела привязок — 23.09.2026
- [x] 2.2 Применение в `$HOME`; `workspaced check` проходит; работающий демон перечитал конфиг, `hyprctl binds` содержит `slash` с `modmask` 65 — 23.09.2026

## 3. Панель

- [x] 3.1 `KeysHelp.qml`: состояние, загрузка `workspaced keys --json` при открытии, разбор по трём событиям процесса, IPC-цель `keys` (`toggle`, `open`, `close`, `state`) (решения D7, D9) — 23.09.2026
- [x] 3.2 `KeysHelpWindow.qml`: окно layer-shell на весь экран, затемнение, карточка по центру, Escape и клик мимо карточки (решение D6); `shell.qml`, `qmldir` — 23.09.2026
- [x] 3.4 По замечанию пользователя: карточка шириной 1920 px от верхнего до нижнего отступа экрана, перечень одной колонкой с прокруткой (колесо, перетаскивание, стрелки, PageUp/PageDown, Home/End), полоса прокрутки у правого края, прокрутка в начало при открытии (решение D6); `chezmoi apply`, перезапуск панели, `keys open` — слой `keys-help` 3840×2160, `state` «shown groups=9 rows=73», журнал без ошибок QML — 23.09.2026
- [x] 3.3 `chezmoi apply` файлов панели, перезапуск `quickshell-panel.service`: журнал без ошибок QML — 23.09.2026

- [x] 3.5 По требованию пользователя (центрирование с учётом панели, изменение work-area-center): карточка по центру рабочей области, x = 1125 (решение D6) — 23.09.2026
- [x] 3.6 По требованию пользователя: плавная прокрутка анимацией 150 мс, колесо по три строки с анимацией, инерция перетаскивания; Пробел и Shift+Пробел — на полокна; клавиши в духе Vim j/k, Ctrl+D/U, Ctrl+F/B, g g, G, буква по клавише или по коду клавиши при русской раскладке (решение D12) — 23.09.2026
- [x] 3.7 По требованию пользователя: перенос подписи и описания в своих колонках, высота строки по содержимому; шаг «на строку» — 52 px (решение D11); `state` сообщает `card`, `scroll`, `wrapExtra` (решение D9) — 23.09.2026
- [x] 3.8 `chezmoi apply`, перезапуск панели: журнал без ошибок QML (остаются прежние предупреждения о значках лаунчера и портале) — 23.09.2026
- [x] 3.9 По замечанию пользователя «ещё более плавно»: анимация 320 мс `Easing.OutCubic`, тачпад через ту же копящуюся цель, повтор цели у края не перезапускает анимацию, инерция `flickDeceleration: 1500`, `maximumFlickVelocity: 4000` (решение D12) — 23.09.2026

## 4. Проверка командами (агент)

- [x] 4.1 `qs -c panel ipc call keys toggle` дважды: `state` — «shown groups=9 rows=73», слой `keys-help` 3840×2160 в `hyprctl layers`; затем «hidden», слоя нет — 23.09.2026
- [x] 4.2 `open` и `close`: активное окно композитора до и после то же — 23.09.2026
- [x] 4.3 Escape, поданный `wtype -k Escape` в слой с монопольной клавиатурой, скрывает окно (`state` — «hidden»), активное окно прежнее — 23.09.2026
- [x] 4.4 `workspaced keys --json` с конфигом, где привязка занимает зарезервированное сочетание: код 1, stdout пуст, в stderr запись и сочетание — 23.09.2026

- [x] 4.5 `keys open`: `state` — «shown groups=9 rows=73 card=1125,10 1920x2140 scroll=0/2477 wrapExtra=0»: карточка в центре рабочей области, ни одна строка не перенесена — 23.09.2026
- [x] 4.6 Клавиши из `wtype`, цель прокрутки по `state`: Пробел → 1020, Shift+Пробел → 0, j → 52, j → 104, k → 52, End → 2477, Home → 0, PageDown → 1987, PageUp → 0, ↓ → 52, ↑ → 0, Shift+g → 2477, g → 2477, j → 2477, g → 2477, g → 0; Escape — «hidden» — 23.09.2026

## 5. Документация

- [x] 5.1 `AGENTS.md`: фраза о Shift+Super+/ в разделе «Сессия Hyprland» — 23.09.2026

## 6. Проверка пользователем

- [x] 6.1 Shift+Super+/ при английской и при русской раскладке показывает окно подсказки, повторное Shift+Super+/ скрывает его (проверяет пользователь) — подтвердил пользователь 23.09.2026; шрифт по его замечанию увеличен вдвое
- [x] 6.2 Клик по затемнённой части экрана скрывает окно, клик по карточке не скрывает; после закрытия ввод с клавиатуры снова идёт в прежнее окно (проверяет пользователь) — подтвердил пользователь 23.09.2026
- [x] 6.3 Вид окна: карточка 1920 px, группы одной колонкой читаются, подписи цепочек и описания не обрезаны, прокрутка колесом, перетаскиванием, стрелками, PageUp/PageDown и Home/End работает (проверяет пользователь) — подтвердил пользователь 23.09.2026 (клавиши и прокрутка работают; прокрутку попросил сделать ещё плавнее)

- [ ] 6.4 Плавность прокрутки колесом, клавишами и перетаскиванием; Ctrl+D/U/F/B (из wtype сочетания с Ctrl до клиентов не доходят); j, k, g g, G, Ctrl+D при русской раскладке; карточка по центру справа от панели (проверяет пользователь)
- [ ] 6.5 Прокрутка после замедления до 320 мс заметно плавнее: колесо при быстром вращении едет без остановок между щелчками, тачпад без рывков, инерция перетаскивания мягкая (проверяет пользователь)

## 7. После проверки

- [ ] 7.1 Перезапуск демона workspaced при удобном случае, чтобы ушли предупреждения «неизвестный ключ конфига: binds.N.desc» старого бинарника (решение D10)
