## 1. Код панели (сделано коммитом `5f1e4ff`, 22.09.2026)

- [x] 1.1 `Wsd.qml`: переподключение к демону по разрыву соединения
  (`onConnectionStateChanged`) и по появлению файла сокета (`FileView` с
  `watchChanges`), с наблюдением за `$XDG_RUNTIME_DIR` на случай запуска
  панели раньше демона; объект `Socket` пересоздаётся при каждой попытке.
  Таймер `reconnect` (5 с) убран
- [x] 1.2 `HoverInfoPopup.qml`, `TooltipPopup.qml`, `MenuPopup.qml`,
  `PanelPopup.qml`: скрытие и показ подсказок и меню — через `Qt.callLater`
  и наведение на объединённую область вместо таймеров задержки (120, 300,
  350 мс)
  - [x] 1.2.1 Отдельная задача каждого фрагмента этого пункта не заводилась:
    задача сохраняет разбивку по файлам, выполнена одним коммитом
- [x] 1.3 `tiles/Launcher.qml`: список строится прямо в обработчике события
  (`onQueryChanged`, `onValuesChanged`), таймер-дебаунсер (30 мс) убран
- [x] 1.4 `tiles/Network.qml`: чтение состояния по каждой строке
  `nmcli monitor` с откладыванием при уже идущем чтении (флаг
  `statePending`) вместо таймера-дебаунсера (300 мс); перезапуск монитора
  по его выходу (`onExited`) вместо таймера (3 с), со счётчиком подряд
  идущих мгновенных выходов и остановкой попыток после пяти
- [x] 1.5 `tiles/Tray.qml`: панель громкости скрывается по `HoverHandler` на
  объединённой области кнопки и панели вместо таймера (500 мс)
- [x] 1.6 `tiles/Gauges.qml`: общий объём памяти берётся из каждого ответа
  скрипта `ram`, отдельный часовой таймер убран
- [x] 1.7 `tiles/Disks.qml`, `tiles/Gauges.qml` (нагрузка, температуры),
  `tiles/Weather.qml`: три оставшихся таймера помечены комментарием
  `WARNING` с объяснением, почему источника событий нет
- [x] 1.8 `docs/decisions/0007-no-timers.md`: дополнена положением о метках
  `FIXME` и `WARNING`

## 2. Спецификации (это изменение)

- [x] 2.1 `openspec/specs/qs-workspaces/spec.md`: требование «Иконки
  workspace в плитке стола» и сценарий «Панель без демона» переписаны под
  переподключение по событиям
- [x] 2.2 `openspec/specs/qs-tray-lang/spec.md`: требование «Громкость
  в трее» и сценарий «Панель громкости по наведению» переписаны под
  скрытие без задержки
- [x] 2.3 `openspec/specs/qs-monitors/spec.md`: требование «Использование
  RAM» переписано — общий объём памяти из ответа скрипта `ram`
- [x] 2.4 `openspec/specs/qs-network/spec.md`: требование «Состояние из
  NetworkManager по событиям» дополнено перезапуском монитора по выходу
  процесса с защитой от перезапуска по кругу, добавлены сценарии
- [x] 2.5 `openspec validate panel-no-timers --strict` проходит
- [x] 2.6 `todo.md`: убрана строка о расхождении трёх требований
  спецификаций с кодом панели (расхождение закрыто этим изменением)
- [x] 2.7 Отдельный проход по формулировкам артефактов изменения
  и дельт спецификаций
