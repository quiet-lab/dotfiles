## 1. Проверка предпосылок

- [x] 1.1 Убедиться на живой системе, что сворачивание и разворачивание окна меняют `_NET_CLIENT_LIST_STACKING` (запустить `xprop -root -spy _NET_CLIENT_LIST_STACKING`, свернуть окно, увидеть событие); при отсутствии события зафиксировать в design.md запасной триггер `_NET_ACTIVE_WINDOW`.

## 2. Скрипт состояния

- [x] 2.1 Переписать `collect` в `widgets/workspaces/scripts/executable_workspace`: разбор `_NET_WM_STATE` на признак свёрнутости, табуляция как разделитель, замена табуляции в значениях пробелом; проверить `sh -n` и вывод записей для текущих окон.
- [x] 2.2 Реализовать `render` с выводом JSON (поля `desktops[].n`, `active`, `windows[]` с `id`, `icon`, `glyph`, `min`, `name`, поле `more`), обрезкой до 10 окон и функцией экранирования `jesc`; проверить корректность разбора вывода командой `python3 -m json.tool`.
- [x] 2.3 Добавить подавление одинаковых состояний и триггер `_NET_CLIENT_LIST_STACKING` в `xprop -spy`; проверить, что переключение фокуса между окнами одного стола не даёт новых строк вывода, а сворачивание даёт.
- [x] 2.4 Обновить комментарий-заголовок скрипта под новый формат вывода и триггеры.

## 3. Разметка и стили

- [x] 3.1 Переписать `workspaces.yuck`: `deflisten` с начальным значением `{"desktops":[]}`, `defwidget workspaces` с внешним `for` по столам и внутренним по окнам, `eventbox` плитки, кнопки-иконки с `:tooltip`, метка «+N» по `more`, классы `ws-fo` и `ws-min` через тернарные выражения; проверить `eww reload` без ошибок в `eww logs`.
- [x] 3.2 Переписать `workspaces.scss`: удалить `ws-name`, `ws-win*`, `ws-icons` старой раскладки; задать `.ws` с `min-width: 300px`, `min-height: 20px`, `padding: 8px 10px`, номер `$gray` и `$foreground` для активного, рамку `ws-fo`, полупрозрачность `ws-min`, отступ кнопок-иконок 2px; проверить, что ширина и высота всех плиток совпадают (пустой стол, стол с окнами, активный стол).

## 4. Проверка поведения

- [x] 4.1 Проверить клики: по свободному месту плитки переключается стол, по иконке окна другого стола активируется это окно на его столе.
- [x] 4.2 Проверить свёрнутые окна: после сворачивания иконка становится полупрозрачной без смены стола, после разворачивания возвращается к обычному виду.
- [x] 4.3 Проверить ограничение: открыть более 10 окон на одном столе (например, несколько wezterm) и убедиться, что показаны 10 иконок и метка «+N», ширина плитки не изменилась.
- [x] 4.4 Проверить экранирование: подать в скрипт запись с кавычкой в названии или пути (временно через переменную окружения или тестовую функцию) и убедиться, что JSON остаётся корректным и колонка отрисовывается.

## 5. Завершение

- [x] 5.1 `chezmoi apply`, повторный `eww reload`, `chezmoi status` без расхождений по файлам виджета; сверить итоговое поведение со спецификацией `eww-workspaces` по каждому сценарию дельты.
