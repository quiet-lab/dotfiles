## 1. Причина (сделано 23.09.2026)

- [x] 1.1 Разобрана причина по исходникам Hyprland 0.56.2
      (`InputManager.cpp`, `Seat.cpp`) и по событиям `wev`: после выхода
      `wtype` окно получает полную карту us+ru без события `modifiers`
      с номером группы
- [x] 1.2 Отвергнуты `switchxkblayout` на ту же группу и смена фокуса окна:
      ни один не порождает событие `modifiers` с нужной группой (разбор —
      в `design.md`)

## 2. Исправление (сделано 23.09.2026)

- [x] 2.1 Скрипт `dot_local/bin/handmade-scripts/executable_voxtype-restore-layout`:
      по каждой физической клавиатуре с ненулевой группой переключает
      группу на 0 и обратно одним `hyprctl --batch`
- [x] 2.2 Ключ `post_output_command` в `dot_config/voxtype/config.toml`
      вызывает скрипт по событию «вывод текста закончен»
- [x] 2.3 Подраздел «Раскладка окна после диктовки» в
      `docs/media-and-portals.md`: разбор причины, исправление, проверка
      без нажатий через `wev`

## 3. Спецификация (это изменение)

- [x] 3.1 `openspec/changes/voxtype-restore-layout/specs/voxtype-dictation/spec.md`:
      добавлено требование «Раскладка окна после диктовки» со сценариями
- [x] 3.2 `AGENTS.md`: абзац о VoxType дополнен фразой о возврате раскладки
      со ссылкой на `docs/media-and-portals.md`
- [x] 3.3 `todo.md`: в разделе «Открытые вопросы к пользователю» отмечена
      проверка настоящей диктовкой
- [x] 3.4 `docs/open-questions/hyprland-keymap-group-after-virtual-keyboard.md`
      заведён с условием пересмотра, строка добавлена в
      `docs/open-questions/README.md`
- [x] 3.5 `openspec validate voxtype-restore-layout --strict` проходит

## 4. Проверка (не заархивировано до её итогов)

- [x] 4.1 Проверка настоящей диктовкой при русской раскладке: после вставки — пользователь проверил 23.09.2026: после диктовки при RU кириллица печатается сразу; в журнале четыре вызова перехвата с возвратом группы 1
      текста первый же напечатанный символ — кириллица, без задержки
      и без лишнего нажатия — **проверяет пользователь**
