## 1. Демон workspaced (`~/work/pets/workspaced`)

- [x] 1.1 `daemon.rs`: чистая функция `raise_target` — место окна при поднятии workspace с учётом переноса и режима (решение D2)
- [x] 1.2 `daemon.rs`: `Daemon::raise` ставит окна, которые переносятся с прежнего стола вместе с workspace, в их прямоугольник у композитора до переноса в обоих режимах (решение D1); `Daemon::move_desktop` больше не вызывает `remember_geometry`
- [x] 1.3 Модульный тест `raise_target_keeps_rect_on_move_in_both_modes`: перенос в обоих режимах, поднятие в режиме обмена ячеек, режим `stack` с запомненным прямоугольником и без него
- [x] 1.4 Сборка и проверки: `cargo test` — 73 теста проходят, `cargo clippy` без предупреждений, `cargo build --release`
- [x] 1.5 Коммит `5ebe3c8` в ветвь master, отправлен в `origin`

## 2. Проверка командами (агент)

- [x] 2.1 Перезапуск `workspaced.service` с новым бинарником; `work` активен на столе 1
- [x] 2.2 Окно neovide сдвинуто и растянуто диспетчерами композитора: из ячейки `right` (3055,10, 1920×2140) в 2000,300, 1400×1200
- [x] 2.3 `workspaced move-desktop 5`: все окна `work` на столе 5, прямоугольники совпали с прежними (neovide 2000,300 1400×1200; два окна chromium −805,10 1920×2140; herdr 1125,10 1920×2140)
- [x] 2.4 `workspaced move-desktop 1`: окна вернулись на стол 1 в тех же прямоугольниках, фокус у herdr (главная ячейка), назначения ячеек `work` прежние
- [x] 2.5 `workspaced arrange` вернул neovide в ячейку `right`; журнал демона без ошибок, состояние стола 1 исходное

## 3. Спецификация и документация

- [x] 3.1 Дельта `ws-daemon`: требования «Перенос workspace на стол» и «Расстановка окон»; `openspec validate move-desktop-keeps-geometry --strict` проходит
- [x] 3.2 `AGENTS.md` и `docs/window-model.md` описывают новое поведение переноса; ссылка на заархивированное изменение `desktop-move-and-arrange` исправлена
