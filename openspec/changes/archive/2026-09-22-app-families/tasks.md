## 1. Демон workspaced (`~/work/pets/workspaced`)

- [x] 1.1 `config.rs`: поля `family` и `rect` у `App`, `cmd` становится необязательным; проверка требует хотя бы одно из `cmd` и `class`, отклоняет `family`, называющий отсутствующее приложение, вариант другого варианта и само приложение, и отклоняет имя приложения с двоеточием или `#`; прежние проверки (`title` без `class`, разбор выражений) сохраняются. Проверка: тесты на принятие правильного конфига и на каждую ошибку, `cargo test`
- [x] 1.2 `daemon.rs`: тег экземпляра `app:<имя>#<номер>` — разбор тега (номер, тег без номера от прежней версии, звёздочка тега из правила композитора) и выбор наименьшего свободного номера среди живых окон приложения. Проверка: модульные тесты разбора и выбора номера; на живом окне `hyprctl dispatch tagwindow +app:probe#1 address:…` и `hyprctl clients -j` подтверждают, что композитор принимает такой тег (тег после проверки снять); при отказе взять разделитель `-` и записать это в `design.md`
- [x] 1.3 `daemon.rs`: вместо поиска одного окна (`find_app_window`) — набор окон приложения по возрастанию номера экземпляра, где окна вариантов входят и в набор своего семейства. Проверка: модульные тесты на порядок экземпляров и на окно варианта в наборе семейства
- [x] 1.4 `daemon.rs`: захват (`adopt_untagged`, `pick_candidate`) берёт все подходящие окна, выполняется и для приложения, у которого окна уже есть, сопоставляет сначала варианты, затем семейства, а при нескольких подходящих вариантах одного семейства отдаёт окно первому по имени с предупреждением в журнале. Проверка: модульные тесты (три окна Chromium получают номера 1—3; окно, подходящее варианту и семейству, достаётся варианту; окно на `special:hidden` не берётся)
- [x] 1.5 `daemon.rs`: место окна по правилу «ячейка или `rect` приложения в workspace → то же у его семейства → `rect` приложения → центр экрана»; расставляются все окна приложения одним прямоугольником, порядок по глубине не меняется, наверх поднимается только окно, получающее фокус. Проверка: модульный тест правила места, `cargo test`. Дополнено независимой проверкой: если workspace описывает и семейство, и его вариант, окно варианта расставляет запись варианта, а запись семейства его не трогает (тест `variant_window_is_placed_by_its_own_entry`); прежде место такого окна зависело от порядка имён приложений
- [x] 1.6 `daemon.rs`: обмен ячеек (`make_main`) переставляет стопки целиком, фокус получает первый экземпляр, второй экземпляр по цепочке не запускается; приложение без `cmd` и без окон оставляет место пустым и пишет строку в журнал вместо запуска. Проверка: модульные тесты, `cargo test`, `cargo build --release`
- [x] 1.7 `save.rs`: у приложения с несколькими окнами в конфиг записывается `rect` первого экземпляра, остальные окна отдельными записями не появляются. Проверка: модульный тест записи, `cargo test`
- [x] 1.8 `session.rs`: приложение из снимка запускается один раз, сколько бы его окон в снимке ни было (design D14). Проверка: модульный тест `snapshot_with_two_windows_starts_one_instance`, `cargo test`

## 2. Переезд профиля Chrome `AI.dev2026` (до задач, зависящих от класса варианта)

- [x] 2.1 Сверить размещение профиля только чтением: каталог данных Chrome — `~/.config/google-chrome`, профилю `AI.dev2026` отвечает подкаталог `Profile 1` (по `profile.info_cache` в `Local State`), объём около 338 МиБ; `$HOME` общий с контейнером `fedora-box`, поэтому путь одинаков на хосте и в контейнере. Проверка: вывод `du -sh` и записи `info_cache` приложены к задаче, ничего не изменено
- [x] 2.2 Закрыть все окна Chrome обоих профилей и убедиться, что процессов `google-chrome` в контейнере и на хосте нет. Проверка: `pgrep -af google-chrome` ничего не выводит
- [x] 2.3 Скопировать `~/.config/google-chrome/Profile 1` в `~/.config/google-chrome-ai/Default` без изменения исходного каталога. Проверка: `du -sh` обоих каталогов совпадает, в копии есть `Preferences`, `Login Data`, `Extensions`, `History`
- [x] 2.4 Запустить экземпляр вручную из контейнера командой с `--user-data-dir=$HOME/.config/google-chrome-ai --profile-directory=Default --class=google-chrome-ai --ozone-platform=wayland --enable-features=AcceleratedVideoDecodeLinuxGL,VaapiOnNvidiaGPUs,VaapiIgnoreDriverChecks`. Проверка: `hyprctl clients -j` показывает класс `google-chrome-ai` у окна и у второго окна, открытого пользователем по Ctrl+N. Сделано: экземпляр запущен, `hyprctl clients -j` подтверждает class и initialClass `google-chrome-ai` у первого окна; осталось: проверить класс второго окна, открытого пользователем по Ctrl+N
- [x] 2.5 Пользователь проверяет в новом экземпляре вход в аккаунты, расширения, закладки и историю. Проверка: подтверждение пользователя
- [x] 2.6 Ярлык варианта: обновить (или создать по образцу экспорта distrobox) `~/.local/share/applications/fedora-box-google-chrome-ai.desktop` с новыми ключами запуска; файл вне chezmoi, поэтому упомянуть его в `docs/host-state.md`. Проверка: запуск из лаунчера rofi открывает нужный экземпляр, `hyprctl clients -j` показывает класс `google-chrome-ai`. Сделано: ярлык создан, `desktop-file-validate` проходит, упомянут в `docs/host-state.md`; осталось: проверить запуск именно из rofi
- [x] 2.7 Только после подтверждения пользователя (задача 2.5) удалить прежний профиль `AI.dev2026` из каталога `~/.config/google-chrome` средствами Chrome (диспетчер профилей), чтобы `Local State` остался согласованным. Проверка: в диспетчере профилей экземпляра по умолчанию остался один профиль, каталог `Profile 1` исчез. Сделано 22.09.2026 иначе: при закрытом Chrome запись `Profile 1` убрана из `Local State` (`profile.info_cache`, `profile.profiles_order`), каталог удалён; после запуска Chrome профиль заново не появился, в `info_cache` остался только `Default`

## 3. Конфиг сессии и правила композитора

- [x] 3.1 `dot_config/workspaced/config.toml`: семейство `[apps.wezterm]` (`cmd = "wezterm-gui"`, `class = "^org\\.wezfurlong\\.wezterm$"`, без `chain`, ни в одном workspace); `herdr` получает `family = "wezterm"`, аргумент `--class wezterm-herdr` в запуске и `class = "^wezterm-herdr$"` вместо `title`. Проверка: `workspaced check` без ошибок, `workspaced keys --list` показывает Super+T по-прежнему у `herdr`
- [x] 3.2 `dot_config/workspaced/config.toml`: у `chrome-ai` команда запуска с `--user-data-dir` и `--class` из задачи 2.4 и `class = "(?i)^google-chrome-ai$"`; у `chrome` класс остаётся `(?i)^google-chrome$`. Каталог в аргументе писать как `--user-data-dir=$HOME/.config/google-chrome-ai`: `~` демон подставляет только в начале строки, внутри аргумента он остался бы знаком тильды. Проверка: `workspaced check` без ошибок, в журнале демона после запуска `chrome-ai` виден путь с раскрытым `$HOME`. Конфиг и `class` внесены, `workspaced check` проходит новым демоном. Раскрытие `$HOME` внутри аргумента подтверждено кодом и тестом, а не журналом (запускать `chrome-ai` не понадобилось): `expand` (`config.rs`) берёт значение из сложенного окружения, а при его отсутствии из окружения процесса, и тест `expand_forms` проверяет, что `--user-data-dir=$HOME/.config/x` раскрывается в абсолютный путь, а тильда внутри аргумента — нет
- [x] 3.3 `dot_config/hypr/hyprland.lua`: класс `google-chrome-ai` добавлен в правило непрозрачности браузеров `opaque-media`, класс `wezterm-herdr` — в правило размытия терминалов `blur-terminal-focused` (иначе окно herdr теряет размытие вместе с прежним классом). Проверка: после `chezmoi apply` и `hyprctl reload config-only` окно herdr размывается в фокусе, окно нового экземпляра Chrome непрозрачно. Сделано: оба класса внесены, `chezmoi apply` выполнен, Hyprland перечитал конфиг (`hyprctl configerrors` пуст); осталось: глазами убедиться, что окно herdr с новым классом размывается в фокусе, — нужен экран и перезапущенный herdr
- [x] 3.4 Применить конфиг (`chezmoi apply`), перезапустить `workspaced.service`, выполнить `hyprctl reload config-only`, поднять `work` и `surf`. Проверка: `hyprctl clients -j` показывает теги с номерами экземпляров у всех окон приложений, окна стоят по ячейкам `thirds` и `overlay`, повторных запусков в журнале демона нет. Сделано: собран новый демон (`cargo test`, `cargo clippy --all-targets`, `cargo build --release`), конфиг и `hyprland.lua` применены, `hyprctl configerrors` пуст. Осталось: перезапустить демон и проверить окна. Перезапуск 22.09.2026 в 03:11 не удался по внешней причине: за минуту до него монитор DP-2 отключился (в журнале Hyprland «DP-2 is not connected, clearing stale crtc», остался выход FALLBACK), столы 1…4 остались без монитора, и демон при восстановлении сессии вышел с ошибкой «Workspace has no monitor ← hl.dsp.focus({ workspace = 2 })»; юнит исчерпал попытки перезапуска. Состояние юнита сброшено (`systemctl --user reset-failed`), окна сессии целы. Когда монитор включён, выполнить `systemctl --user start workspaced.service` и проверки. Тег экземпляра с номером получат только новые и захваченные окна: у уже открытых окон остаются теги прежнего вида `app:<имя>`, и это правильно — они читаются как экземпляр 1 Проверено 22.09.2026 после включения монитора: демон поднялся с первой попытки, окна `work` и `surf` стоят по ячейкам, повторных запусков и ошибок в журнале нет; у окон, открытых до перезапуска, теги прежнего вида без номера (экземпляр 1).

## 4. Проверка вживую (пользователь)

- [x] 4.1 Super+T, Super+C, Super+E, Super+B, Super+V делают главным те же приложения, что и раньше, Super+Tab w и Super+Tab s поднимают `work` и `surf`
- [x] 4.2 Второе окно Chromium, открытое по Ctrl+N: после Super+Tab w оба окна стоят в ячейке `left` одно поверх другого, Super+C выводит стопку в главную ячейку с фокусом у первого экземпляра, перенос `work` на другой стол уводит оба окна
- [x] 4.3 Окна двух профилей Chrome не путаются: после перезапуска браузеров Super+B поднимает окно профиля Default, Super+V — окно `AI.dev2026`, и каждое стоит в своей ячейке `overlay`
- [x] 4.4 Обычное окно wezterm (терминал агента) остаётся свободным: поднятие `work` его не переносит и в ячейку `center` не ставит

## 5. Спецификации и документация

- [x] 5.1 `openspec validate app-families --strict` проходит
- [x] 5.2 `AGENTS.md`: семейства и варианты в конфиге, теги экземпляров, несколько окон у приложения, класс окна herdr, отдельный каталог данных и класс окна профиля `AI.dev2026`; оговорка о неотличимых окнах двух профилей Chrome снимается
- [x] 5.3 `docs/host-state.md`: каталог данных `~/.config/google-chrome-ai` и ярлык варианта вне chezmoi
- [x] 5.4 Отдельный проход по формулировкам: артефакты изменения, комментарии в `config.toml`, `hyprland.lua` и в изменённом коде демона
