## 1. Сервер уведомлений в панели

- [x] 1.1 `dot_config/quickshell/panel/NotificationService.qml`: синглтон с `NotificationServer` (объявленные возможности, `keepOnReload`), историей записей за сутки с состоянием «просмотрено»/«не просмотрено», списком показанных на экране, счётчиком непросмотренных, режимом «не беспокоить», файлом состояния `notifications.json`, отбрасыванием устаревших записей по обращению, меткой стопки и `IpcHandler` с целью `notifications`. Проверка: панель запускается без ошибок QML, `busctl --user list` показывает владельцем `org.freedesktop.Notifications` процесс `qs`. Сделано и проверено 22.09.2026
- [x] 1.2 `dot_config/quickshell/panel/NotificationCard.qml`: карточка записи истории — значок, заголовок, тело с разметкой, полоса по подсказке `value`, кнопки действий, клики (левый — действие по умолчанию и пометка просмотренным, правый — действие и удаление из истории, средний — пометить просмотренными все), растворение при уходе (`NumberAnimation` по `opacity`, смена состояния по сигналу окончания). Сроков показа и таймеров в карточке нет. Проверка: `notify-send` с каждым из вариантов. Сделано 22.09.2026
- [x] 1.3 `dot_config/quickshell/panel/NotificationStack.qml`: окно layer-shell постоянного размера справа от колонки (10 px от колонки, от нижнего края экрана до отступа 10 px сверху), слой `Overlay`, пространство имён `panel`, область ввода (`mask`) по занятой карточками части, без зоны и без фокуса клавиатуры; карточки создаются и уничтожаются по одной на непросмотренную запись, расстановка снизу вверх с анимированным смещением, выезд новой снизу, опускание после растворения, пометка верхней просмотренной при переполнении экрана. Проверка: `hyprctl layers`, серии `notify-send`. Сделано 22.09.2026: `hyprctl layers` показывает слой `panel` с xywh `340 10 444 2150`, нижняя карточка нижним краем в 2150
- [x] 1.4 `dot_config/quickshell/panel/NotificationHistoryPopup.qml`: всплывающее окно истории от плитки — записи за сутки новыми сверху, отметка состояния, подчёркивание записи с живым действием, левый клик переключает состояние, правый выполняет действие и удаляет запись, кнопка очистки, строка о пустой истории. Проверка: вид окна и работа кнопок. Сделано и проверено 22.09.2026
- [x] 1.5 `dot_config/quickshell/panel/tiles/Notifications.qml`: плитка 36×36 в стиле переключателя раскладки — глиф колокольчика, перечёркнутый в режиме «не беспокоить», и число непросмотренных уведомлений в кружке поверх глифа; левый клик — история, правый — режим, подсказка при наведении. Проверка: вид плитки, счётчик после серии уведомлений. Сделано 22.09.2026
- [x] 1.6 `qmldir`, `shell.qml` и `tiles/Tray.qml`: регистрация новых компонентов, плитка уведомлений в правой колонке блока столов (x = 294, y = 1462), трей сдвинут на y = 1508 и укорочен до 266 px (внутренние размеры считаются от высоты плитки), средняя группа вернулась к прежним координатам (шкалы 704, сеть 816, диски 933, избранное 1178). Проверка: `chezmoi apply`, перезапуск юнита, журнал панели без ошибок QML. Сделано и проверено 22.09.2026

## 2. Переход с dunst

- [x] 2.1 `dot_config/systemd/user/symlink_dunst.service` — маска юнита ссылкой на `/dev/null`; `systemctl --user stop dunst.service` и маска в живой системе. Проверка: `systemctl --user status dunst.service` показывает `masked`, процесса `dunst` нет. Сделано и проверено 22.09.2026
- [x] 2.2 `dot_config/dunst/dunstrc` снять с управления chezmoi (`chezmoi forget --force ~/.config/dunst/dunstrc`) и удалить из источника (`git rm`). Проверка: `chezmoi status` пуст, каталога `dot_config/dunst` в источнике нет. Сделано 22.09.2026
- [x] 2.3 `dot_config/workspaced/config.toml`: записи `[[binds]]` уведомлений переводятся с `dunstctl` на `qs -c panel ipc call notifications …`; `Ctrl+Enter` освобождается, добавляются `Super+Enter` (`dismissOldest`), `Shift+Super+Enter` (`invokeOldest`), `Super+Ctrl+Enter` (`closeAll`) и `Super+Ctrl+Alt+Enter` (`clear`). Проверка: `workspaced check`, `workspaced keys --list`, перезапуск `workspaced.service`, `hyprctl configerrors` пуст. Сделано 22.09.2026: `workspaced keys --list` показывает четыре вызова панели, `hyprctl configerrors` пуст
- [x] 2.4 Пакет `dunst` в системе оставлен: его `dunstify` вызывают скрипты громкости, яркости и снимков экрана. Команда удаления пакета названа в отчёте, но не выполняется

## 3. Проверка агентом

- [x] 3.1 `notify-send "Заголовок" "Текст"` — карточка появляется справа от колонки у нижнего края и сама не исчезает. Проверено 22.09.2026: карточка на месте и через минуту
- [x] 3.2 `notify-send -i dialog-information "Заголовок" "Текст"` — слева значок из темы. Проверено 22.09.2026
- [x] 3.3 `notify-send -u critical "Внимание" "Текст"` — красная рамка. Проверено 22.09.2026
- [x] 3.4 `notify-send -h int:value:40 "Громкость" ""` — полоса заполнена на 40 %. Проверено 22.09.2026
- [x] 3.5 `notify-send -A ok=Ок -A no=Нет "Вопрос" "Текст"` — в карточке две кнопки. Проверено 22.09.2026. Замечание: `notify-send -A` ждёт закрытия уведомления, а сроков показа больше нет, поэтому команда не возвращает управление, пока карточку не закроют
- [x] 3.6 `notify-send -p` и `notify-send -r <id>` — вторая карточка заменяет первую на месте. Проверено 22.09.2026: остаётся одна карточка
- [x] 3.7 Пять уведомлений подряд — столбик новыми снизу, зазор 10 px. Проверено 22.09.2026
- [x] 3.7b Закрытие нижней карточки (`close`) — остальные опускаются ровно на 76 px (карточка 66 px и зазор 10 px); закрытие карточки в середине (`CloseNotification` по D-Bus) — карточки под ней остались на прежних местах, карточки над ней опустились. Проверено 22.09.2026 по снимкам до и после
- [x] 3.7a Несколько десятков уведомлений подряд — столбик не выходит за верхний край экрана, самые старые растворяются и попадают в историю. Проверено 22.09.2026: после 35 уведомлений подряд на экране 28 карточек, верхняя стоит в 10 px от верха экрана, вытесненные видны в счётчике истории
- [x] 3.8 Режим «не беспокоить» включён через IPC: обычное уведомление на экране не появляется и попадает в историю, критическое показывается. Проверено 22.09.2026
- [x] 3.9 `qs -c panel ipc call notifications close`, `closeAll`, `dismissOldest`, `invokeOldest`, `restore`, `clear`, `dnd`, `history` — каждая команда делает своё. Проверено 22.09.2026
- [x] 3.21 Значок из подсказки `image-data` перерисовывается в файл `notification-icons/<номер>.png` и переживает перезапуск панели; значок по пути к файлу и по имени темы хранятся как есть и файла не заводят. Проверено 22.09.2026: уведомление с `image-data` послано через `gdbus`, после перезапуска панели значок виден и в окне истории, и на возвращённой карточке; журнал без «Requested image from unknown handle»
- [x] 3.22 `dismissOldest` при уведомлении Telegram без действия по умолчанию переводит фокус на окно Telegram с переходом на его стол. Проверено 22.09.2026: уведомление с подсказкой `desktop-entry` послано через `gdbus`, `hyprctl activewindow` показал `org.telegram.desktop` на столе 3, фокус возвращён на прежнее окно
- [x] 3.20 `closeAll` при трёх непросмотренных — все три `seen`, экран пуст (один слой `panel`); следом `clear` — записей в файле состояния нет, на плитке остался один колокольчик. Проверено 22.09.2026
- [x] 3.9a `dismissOldest` при трёх уведомлениях убирает верхнее, а два нижних остаются на тех же местах экрана. Проверено 22.09.2026: снимки до и после совпадают в нижней части столбика
- [x] 3.10 Режим «не беспокоить» переживает `systemctl --user restart quickshell-panel.service`. Проверено 22.09.2026
- [x] 3.13 На экране только непросмотренные: `dismissOldest` убирает верхнюю карточку, а её запись остаётся в файле состояния с `seen: true`. Проверено 22.09.2026
- [x] 3.14 Режим «не беспокоить» скрывает карточки, не помечая их просмотренными; пришедшее в режиме уведомление тоже остаётся непросмотренным; выключение режима возвращает все четыре на экран. Проверено 22.09.2026
- [x] 3.15 `restore` возвращает последнее просмотренное уведомление на экран (запись снова `seen: false`). Проверено 22.09.2026
- [x] 3.16 Перезапуск панели сохраняет историю, состояния и счётчик: после `systemctl --user restart` четыре непросмотренные записи снова на экране, у восстановленных записей действия по умолчанию нет (в окне истории заголовок без подчёркивания). Проверено 22.09.2026
- [x] 3.17 Записи старше суток отбрасываются: время двух записей в `notifications.json` сдвинуто на 25 часов назад, после старта панели в истории и в файле остались только свежие. Проверено 22.09.2026
- [x] 3.18 `invokeOldest` выполняет действие по умолчанию верхнего уведомления и удаляет его запись из истории; у записи без действия только удаляет. Проверено 22.09.2026: `notify-send -A default=Открыть` завершился по вызову действия, записи в файле не осталось
- [x] 3.19 Окно истории показывает записи за сутки новыми сверху, отличает просмотренные (пустой кружок, приглушённый текст) от непросмотренных (залитый кружок, яркий текст) и подчёркивает заголовок записи с живым действием. Проверено 22.09.2026
- [x] 3.11 `hyprctl layers` показывает два слоя в пространстве имён `panel`; зону резервирует только колонка. Проверено 22.09.2026
- [x] 3.12 `ss -xp | rg workspaced/sock` — панель подключена к демону; журнал панели без ошибок QML. Проверено 22.09.2026: плитки столов показывают workspace демона

## 4. Проверка вживую (пользователь)

- [ ] 4.1 Левый клик по карточке Telegram: выполняется действие по умолчанию (открывается топик сообщения), карточка растворяется и уходит с экрана, запись остаётся в истории с отметкой «просмотрено»
- [ ] 4.1a Левый клик по карточке уведомления без действия по умолчанию: композитор переходит на стол окна приложения-отправителя и отдаёт ему фокус
- [ ] 4.2 Правый клик по карточке Telegram: выполняется действие по умолчанию, композитор переходит на стол окна Telegram, окно получает фокус, а записи об этом уведомлении в истории не остаётся
- [ ] 4.3 Средний клик по карточке при трёх на экране: все три уходят с экрана и остаются в истории
- [ ] 4.4 Клик по кнопке действия в карточке (например, «Пометить как прочитанное» у Telegram) доходит до приложения, после чего карточка уходит с экрана
- [ ] 4.5 Левый клик по плитке уведомлений открывает окно истории, повторный клик закрывает его; правый клик включает и выключает режим «не беспокоить», глиф меняется
- [ ] 4.5a Плитка уведомлений стоит над треем вровень с плиткой первого стола, выглядит как переключатель раскладки, число непросмотренных видно в кружке поверх колокольчика; трей и раскладка на своих местах, выдвижная панель громкости не разъехалась
- [ ] 4.6 В окне истории левый клик по просмотренной записи возвращает её на экран, левый клик по непросмотренной убирает с экрана; правый клик выполняет действие и удаляет запись; кнопка очистки опустошает список и убирает кружок с плитки
- [ ] 4.7 Запись, восстановленная после перезапуска панели, в истории не подчёркнута, правый клик по ней только удаляет её из списка, а левый переводит фокус на окно приложения-отправителя
- [ ] 4.7a Значок уведомления Telegram (аватар собеседника) виден и после перезапуска панели — и в окне истории, и на карточке, возвращённой на экран
- [ ] 4.8 Сочетания Ctrl+Escape, Ctrl+Пробел, Ctrl+Shift+Пробел работают: окно истории, пометить просмотренным последнее, пометить просмотренными все
- [ ] 4.8e Super+Ctrl+Enter помечает просмотренными все уведомления, Super+Ctrl+Alt+Enter очищает историю целиком: экран пустеет, кружок с плитки исчезает
- [ ] 4.8a Super+Enter убирает верхнюю карточку с экрана, оставляя её в истории; Shift+Super+Enter выполняет действие верхней карточки и удаляет её из истории; нижние карточки при этом не сдвигаются
- [ ] 4.8b Ctrl+Enter достаётся приложению: в поле ввода Telegram он отправляет сообщение
- [ ] 4.8c Движение столбика глазом: новое уведомление выезжает снизу и поднимает остальные одним движением; уходящая карточка растворяется, и только после этого вышележащие опускаются; уведомление, пришедшее во время движения, не сбивает его рывком
- [ ] 4.8d Клик по окну в той части экрана, где столбик уведомлений пуст (выше карточек, правее колонки), попадает в окно, а не в панель: область ввода окна столбика ограничена карточками
- [ ] 4.9 Клавиши громкости и яркости: на экране всё время одна карточка с полосой, а не столбик одинаковых, и в истории одна запись о громкости
- [ ] 4.10 Print и Shift+Print: уведомление о снимке читается, перевод строки на месте

## 5. Спецификации и документация

- [x] 5.1 `openspec validate qs-notifications --strict` проходит. Проверено 22.09.2026
- [x] 5.2 `AGENTS.md`, раздел «Сессия Hyprland»: уведомления показывает панель, dunst убран; перечень опросов с `WARNING` не меняется — таймеров в уведомлениях нет. Сделано 22.09.2026
- [x] 5.3 `docs/agent-session-tips.md`: упоминание `dunstrc` в перечне мест, где задана тема значков, заменено на конфиг панели. Сделано 22.09.2026
- [x] 5.4 `docs/host-state.md`: упоминание dunst в строке о шрифтах поправлено. Сделано 22.09.2026
- [x] 5.5 Проход по формулировкам всех написанных документов. Сделано 22.09.2026
