## 1. Демон workspaced (`~/work/pets/workspaced`)

- [x] 1.1 `state.rs`, `daemon.rs`: поле `workspace` убрано из `Foreign` и `ExpectedForeign`; `raise` паркует только окна живых приложений других workspace, `remove` — только окна приложений workspace, состав окон workspace для панели — только окна с тегом, в `status` признак `foreign` вместо `attached` (`main.rs`)
- [x] 1.2 `session.rs`: снимок записывает постороннее окно без `workspace`, восстановление не привязывает окна; поле прежних снимков читается и отбрасывается
- [x] 1.3 `save.rs`: отбор посторонних окон текущего стола, приложение конфига под своим именем, тег `app:<имя>` новым приложениям, удаление из `foreign`, сброс `cells[ws]` перед перечитыванием; `cargo test` проходит, `cargo build --release`

## 2. Запуск

- [x] 2.1 Перезапустить `workspaced.service`, убедиться по журналу, что новые окна помечаются как свободные

## 3. Проверка вживую (пользователь)

- [ ] 3.1 Открыть терминал из лаунчера при поднятом `work`; поднять другой workspace или убрать `work` командой `remove` — терминал остаётся на столе; вернуть `work` — терминал не переставляется
- [ ] 3.2 При открытом терминале нажать `Super+Tab Super+S`: в `config.toml` у `work` появляется приложение с `cmd` терминала и `rect`, окно несёт тег (`hyprctl clients -j`), следующее поднятие `work` на другом столе переносит терминал вместе с остальными окнами

## 4. Спецификации и документация

- [x] 4.1 Дельты ws-daemon, ws-config и ws-sessions, `openspec validate free-foreign-windows --strict`
- [x] 4.2 AGENTS.md: свободные окна и сохранение
- [x] 4.3 Проход по формулировкам артефактов и комментариев
