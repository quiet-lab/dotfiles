## 1. Демон workspaced (`~/work/pets/workspaced`)

- [x] 1.1 `state.rs`: запись `ExtraApp` (класс, командная строка, каталог, прямоугольник) и таблица `extra_apps` у `SessionWorkspace`; поле необязательное, снимок прежнего формата читается без ошибки. `State` получает карту «workspace → имя → запись». Проверка: модульные тесты чтения снимка прежнего формата и кругового обхода (запись и чтение снимка с дополнительным приложением), `cargo test`
- [x] 1.2 `config.rs`: поле `save_session` в `[keys]`, действие `"save-session"` в `[[binds]]`, `Workspace` сравнивается на равенство (нужно для пересборки ячеек). Проверка: модульные тесты разбора и отклонения опечатки, `cargo test`
- [x] 1.3 `keys.rs` и `main.rs`: цепочка `keys.save_session` даёт команду `workspaced save-session`, подкоманда `save-session` у клиента, действие `"save-session"` в `[[binds]]`. Проверка: модульный тест генерации Lua и списка привязок, `workspaced keys --list`
- [x] 1.4 `daemon.rs`: эффективный конфиг (design D1) — `cfg_file` из файла и `cfg` с дополнительными приложениями сессии; пересборка при чтении, перечитывании и восстановлении сессии; правила снятия записей (design D4) с предупреждением в журнале; синхронизация назначений ячеек уже собранных workspace. Проверка: модульные тесты пересборки (запись добавляет приложение и место; имя, занятое конфигом, снимается; запись без `cmd` без приложения конфига снимается), `cargo test`
- [x] 1.5 `daemon.rs`: `reload_config` снимает назначения ячеек workspace, чей раздел в конфиге изменился или исчез, и сохраняет назначения остальных (design D8). Проверка: модульный тест на изменённый и неизменённый workspace, `cargo test`
- [x] 1.6 `daemon.rs`: поиск приложения конфига по окну (`class` и `title`, сначала варианты, затем семейства, внутри группы по именам, design D9). Проверка: модульный тест (окно `google-chrome-ai` достаётся `chrome-ai`, окно Gmail — варианту, а не семейству)
- [x] 1.7 `save.rs`: команда `save-session` — принятие посторонних окон текущего стола в активный workspace (design D3, D5, D6, D7); конфиг не читается и не пишется, места описанных приложений не меняются, окно получает тег экземпляра. Проверка: модульный тест плана принятия (окно без подходящего приложения даёт новую запись; окно, подходящее приложению конфига, — запись только о месте; приложение, уже входящее в workspace, записи не даёт; окно без командной строки пропускается), `cargo test`
- [x] 1.8 `save.rs`: `save-workspace` пишет дополнительные приложения сессии в конфиг и снимает их, а для постороннего окна сначала ищет подходящее приложение конфига и пишет его под своим именем. Проверка: модульный тест, `cargo test`
- [x] 1.9 `session.rs`: снимок хранит дополнительные приложения при своём workspace; восстановление при старте и загрузка сессии заменяют их целиком из снимка. Проверка: модульные тесты, `cargo test`
- [x] 1.10 `daemon.rs`: команда `save-session` в сокете. Проверка: `cargo test`, `cargo clippy --all-targets`, `cargo build --release`
- [x] 1.11 `daemon.rs`: снимок сессии пишет обработчик события или команды, изменивших состояние, до ответа клиенту; отметка `mark_dirty` и отложенная запись убраны (design D12). Проверка: модульный тест `changes_state`, отсутствие `mark_dirty` в коде
- [x] 1.12 `daemon.rs`: выход запущенного процесса становится событием (поток с `child.wait()` и сообщение демону), предельные сроки ожидания окна сняты, переходы ожидания вынесены в `pending_step` (design D12). Проверка: модульный тест переходов, `cargo test`
- [x] 1.13 `daemon.rs`: поток тактов убран целиком, `Instant`, `Duration` и `thread::sleep` из кода демона исчезли. Проверка: поиск по коду не находит их
- [x] 1.14 `daemon.rs`: перечитывание конфига по событиям завершённой записи (закрытие файла на записи, появление файла после переименования), без задержки и подавления (design D13). Проверка: модульный тест `write_finished`; вживую правка конфига через `chezmoi apply`, через `tee` на месте и через временный файл с переименованием даёт ровно одну строку о перечитывании в журнале на каждое сохранение (проверено 22.09.2026; парное событие переименования `Modify(Name(Both))` пришлось исключить, иначе перечитываний было два)

## 2. Конфиг сессии

- [x] 2.1 `dot_config/workspaced/config.toml`: в `[keys]` вместо `save_workspace = "SUPER+TAB SUPER+s"` две строки — `save_session = "CTRL+SUPER+s CTRL+SUPER+s"` и `save_workspace = "CTRL+SUPER+s CTRL+SUPER+w"` — с комментарием о том, что первая меняет только сессию, вторая правит файл; комментарий в шапке файла и в разделе `[keys]` поправить под новые цепочки. Проверка: `workspaced check` новым бинарником, `workspaced keys --list` показывает обе цепочки и не показывает `SUPER+TAB SUPER+s`
- [x] 2.2 Применить конфиг (`chezmoi apply`, при отказе без терминала — `chezmoi apply --force <путь>`), перезапустить `workspaced.service`, убедиться, что `hyprctl configerrors` пуст и в журнале демона ошибок нет. Проверка: `journalctl --user -u workspaced.service`, `hyprctl configerrors`. Сделано 22.09.2026: демон поднялся с первой попытки, восстановил сессию и поднял `work`; `hyprctl configerrors` пуст; `hyprctl binds` показывает подкарту Ctrl+Super+S с двумя действиями, а в подкарте Super+Tab остались только Tab, s, w и Escape

## 3. Проверка вживую (пользователь)

- [ ] 3.1 Ctrl+Super+S, затем Ctrl+Super+S при постороннем окне на столе с активным workspace: `chezmoi status` пуст, `workspaced status --json` показывает окно в составе workspace, плитка панели показывает его значок
- [ ] 3.2 Перенос workspace на другой стол уводит принятое окно вместе с остальными и ставит его на записанное место; возврат workspace возвращает окно
- [ ] 3.3 Ctrl+Super+S, затем Ctrl+Super+W записывает workspace в конфиг вместе с принятыми окнами; `chezmoi diff` показывает ожидаемую правку, дополнительных приложений в снимке сессии больше нет
- [ ] 3.4 Прежняя цепочка Super+Tab Super+S ничего не делает; Super+Tab w, Super+Tab s и Super+Tab Tab работают как раньше
- [ ] 3.5 Откат правки конфига возвращает расстановку workspace без перезапуска демона
- [ ] 3.6 Клавиша приложения после неудачного запуска: если окно так и не появилось, повторное нажатие запускает приложение заново; Super+T, Super+C, Super+E, Super+B, Super+V, Super+Y работают как раньше

## 4. Спецификации и документация

- [x] 4.1 `openspec validate session-save-extra-windows --strict` проходит
- [x] 4.2 `AGENTS.md`: раздел о сессии Hyprland — новые цепочки вместо `Super+s`, дополнительные приложения сессии, разделение сохранения сессии и записи в конфиг
- [x] 4.3 `docs/window-model.md`: решение пользователя от 22.09.2026 в разделе 2.7, строка об этом изменении в плане серии перед шагом 3, строка «Запись по команде сохранения» в таблице раздела 3
- [x] 4.4 `todo.md`: убрать три строки раздела «Демон workspaced» — про `reload_config`, про сохранение, заводящее новое приложение, и про обработчик SIGTERM (с записью снимка по событию она теряет смысл)
- [x] 4.5 `AGENTS.md`: формулировку про «30 с ожидания демона» в абзаце о контейнере `fedora-box` поправить — ожидание окна больше не ограничено сроком, юнит прогрева остаётся ради скорости
- [x] 4.6 Отдельный проход по формулировкам: артефакты изменения, комментарии в `config.toml` и в изменённом коде демона
