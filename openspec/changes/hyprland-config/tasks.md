# Tasks: Рабочий конфиг Hyprland под chezmoi

Результат проверки записывать в конце строки задачи после слова «Результат:». Задачи групп 1–4 выполняются из сессии Openbox, группы 5–7 требуют входа в Hyprland через greeter lightdm.

## 1. Подготовка

- [x] 1.1 Установить пакеты `hyprpaper hypridle hyprlock cliphist wl-clipboard grim slurp` и проверить: `pacman -Q` показывает все семь, `chezmoi status` пуст, сессия Openbox не изменилась.
- [x] 1.2 Переименовать пробный `~/.config/hypr/hyprland.lua` в `hyprland.lua.trial` и убедиться, что `~/.config/hypr/` больше не содержит `hyprland.lua` и никогда не содержал `hyprland.conf` (спецификация hyprland-config, «Конфиг под управлением chezmoi»).

## 2. Конфиг Hyprland (спецификация hyprland-config)

- [x] 2.1 Создать `dot_config/hypr/hyprland.lua` с разделами по design D1: монитор DP-2 `3840x2160@120` и правило по умолчанию для прочих выходов, `hl.env` для NVIDIA и переменных X11-сессии по D7 (`QT_QPA_PLATFORMTHEME`, `LESSHISTFILE`, `MOZ_DISABLE_RDD_SANDBOX`, `PATH` с `~/.local/bin` и `~/.bun/bin`), `input.kb_file`, эффекты и кривые из пробного файла, `cursor.no_hardware_cursors = false`, `debug.disable_logs = false` (D11), Xwayland включён. Проверка: `luac -p` без ошибок, `chezmoi apply` кладёт файл в `~/.config/hypr/hyprland.lua`, `chezmoi status` пуст. Результат: файл создан, `luac -p` без ошибок, `chezmoi apply` положил `~/.config/hypr/hyprland.lua`, `chezmoi status` пуст. Сверх спецификации оставлены служебные привязки из пробы: Super+M (выход, пока нет плиток питания) и Super+Shift+C (переключение курсора).
- [x] 2.2 Объявить восемь рабочих столов «1»…«8» на DP-2 через `hl.workspace_rule` (или эквивалент из `hl.meta.lua`), чтобы стол существовал до первого перехода; проверка после входа в задаче 5.3. Результат: цикл `hl.workspace_rule` для «1»…«8» с `monitor = "DP-2"`, `persistent = true`, `default` для «1»; проверка в 5.3.
- [x] 2.3 Добавить правило D5: все окна плавающие (`match = { class = ".*" }`, `float = true`) с исключением закреплённых и полноэкранных; проверка после входа в задаче 5.4. Результат: правило `float-by-default` (`match = { class = ".*" }`, `float = true`) и правило `fix-xwayland-drags` из примера Hyprland; исключений для закреплённых и полноэкранных окон не нужно: правило применяется при открытии окна. Проверка в 5.4.
- [x] 2.4 Создать `dot_config/hypr/hyprpaper.conf` с файлом обоев из `bg-saved.cfg` для DP-2 (D12), `dot_config/hypr/hypridle.conf` только с `lock_cmd` (D9) и минимальный `dot_config/hypr/hyprlock.conf` с тем же фоном. Проверка: `chezmoi apply`, файлы на месте, `hyprpaper --help`, `hypridle --help`, `hyprlock --help` отвечают; синтаксис конфигов проверяется при запуске в задаче 6.4. Результат: `hyprpaper.conf` (DP-2 и запасной монитор, fit_mode cover, splash выключен), `hypridle.conf` (только `lock_cmd`), `hyprlock.conf` (фон с размытием, поле пароля в цветах Tokyo Night); все три программы отвечают на `--help`.
- [x] 2.5 Выбор файла через yazi (design D15): установить `xdg-desktop-portal-termfilechooser` из AUR, создать `dot_config/xdg-desktop-portal/hyprland-portals.conf` и `dot_config/xdg-desktop-portal-termfilechooser/config`, добавить в `hyprland.lua` `hl.env("GTK_USE_PORTAL", "1")` и правило окна для класса `termfilechooser`; `chezmoi apply`, `hyprctl reload`, `systemctl --user restart xdg-desktop-portal`. Результат: Сделано 2026-09-10 05:20: пакет 1.4.3-1 установлен через paru, файлы созданы, `luac -p` и `hyprctl configerrors` без ошибок, `chezmoi status` пуст, три службы порталов после перезапуска активны.

## 3. Привязки (спецификация hyprland-binds)

- [x] 3.1 Перенести в `hyprland.lua` привязки из таблицы спецификации: запуск программ (Super+Return, Super+R, Super+E, Super+V), окно (Super+C, Super+F, Super+X, Super+Z по D6, Super+Shift+V плавающий режим), фокус Super+стрелки, столы Super+1…8 и Super+Shift+1…8 с переходом за окном, Alt+Tab и Alt+Shift+Tab с подъёмом окна, Super+L, dunstctl на Ctrl+Escape/Return/Пробел/Shift+Пробел, медиаклавиши playerctl, громкость и яркость через скрипты из `~/.scripts/`, мышь Super+кнопки. Проверка: `luac -p` без ошибок, в файле нет привязок на `SUPER + space`, `ALT + E`, `ALT + R` (`rg -n 'ALT \+ [ER]|SUPER \+ space'` пусто). Результат: все привязки таблицы перенесены, `luac -p` без ошибок, поиск `ALT + [ER]`, `SUPER + space` пуст. Alt+Tab и Alt+Shift+Tab реализованы функцией: `cycle_next({ next = true/false })` плюс `bring_to_top()`; форма `next = false` для обратного порядка проверяется в 5.8.
- [x] 3.2 Реализовать функцию Lua для половин рабочей области по D5 (Super+Shift+стрелки): геометрия от размера активного монитора и зарезервированных зон с отступами 10 px, вызов диспетчеров точного размера и положения из `hl.meta.lua`. Проверка после входа: сценарий «Окно в половину рабочей области» (положение 10,10, размер 1905×2140 без панели), задача 5.5. Результат: функция `half(side)` считает геометрию от `hl.get_active_monitor()` (position, width, height, reserved) с отступом 10 px, переводит окно в плавающий режим и вызывает `window.resize` и `window.move` с `relative = false`. Форма поля `reserved` в API не задокументирована, разбираются три варианта; проверка в 5.5 с `hyprctl eval`.
- [x] 3.3 Записать в конфиг комментарий со списком намеренно не перенесённых сочетаний (Alt+Пробел, Alt+Super+Пробел, Super+T, Super+D) и убедиться, что они не заняты: `rg -n 'SUPER \+ [TD]\b|ALT \+ space' hyprland.lua` пусто. Результат: комментарий в разделе «Привязки», поиск `SUPER + [TD]`, `ALT + space` пуст.
- [x] 3.4 Создать скрипты `dot_config/hypr/scripts/executable_screenshot-screen`, `executable_screenshot-countdown`, `executable_screenshot-selection` по D10 (grim, slurp, wl-copy, dunstify, каталог `~/Pictures/Screenshots`, именование как в X11-версиях) и привязать к Print, Ctrl+Print, Shift+Print. Проверка: `sh -n` для трёх файлов, после `chezmoi apply` файлы исполняемые; работа проверяется в задаче 5.7. Результат: три скрипта плюс общий `screenshot-common` (каталог `~/Pictures/Screenshots`, имя `<дата>_<ШxВ>_grim.png`, размер через `magick identify`, копия в буфер `wl-copy`, уведомления как в X11-версиях); `sh -n` для всех, после `chezmoi apply` файлы исполняемые; работа в 5.7.
- [x] 3.5 Создать `dot_config/hypr/scripts/executable_clipboard-menu` по D8 (`cliphist list | rofi -dmenu | cliphist decode | wl-copy`) и привязать к Super+V. Проверка: `sh -n`; работа в задаче 6.3. Результат: скрипт `clipboard-menu` создан, `sh -n` без ошибок; работа в 6.3.

## 4. Автозапуск (спецификация hyprland-autostart)

- [x] 4.1 Создать `dot_config/systemd/user/hyprland-session.target` по D3 (`BindsTo=graphical-session.target`, `Before=graphical-session.target`) и добавить в конфиг обработчики `hl.on("hyprland.start")` со `systemctl --user start hyprland-session.target` и `hl.on("hyprland.shutdown")` со `systemctl --user stop graphical-session.target`, с комментарием об удалении после появления встроенной интеграции. Проверка: `chezmoi apply`, `systemctl --user cat hyprland-session.target` показывает файл, `systemctl --user daemon-reload` без ошибок. Результат: цель с `BindsTo=graphical-session.target`, `Wants=` и `After=graphical-session-pre.target` (рецепт прежней вики); обработчики `hyprland.start` (`systemctl --user start hyprland-session.target`) и `hyprland.shutdown` (`os.execute("systemctl --user stop graphical-session.target")`) с комментарием об удалении; `systemctl --user daemon-reload` и `cat` без ошибок.
- [x] 4.2 Включить юниты `systemctl --user enable voxtype.service hyprpolkitagent.service hyprpaper.service hypridle.service` (у hyprpaper и hypridle юниты есть в пакетах, design D4) и проверить, что в сессии Openbox они не стартовали: `systemctl --user is-active` для четырёх юнитов и `graphical-session.target` даёт `inactive`. Результат: включены четыре юнита, символические ссылки в `~/.config/systemd/user/graphical-session.target.wants/` (вне chezmoi); в сессии Openbox все четыре и `graphical-session.target` `inactive`.
- [x] 4.3 Создать `dot_config/hypr/scripts/executable_vpn-up` (ожидание `nmcli -t -f STATE general` = `connected` до 30 с, затем `nmcli con up id axata`, если не активно; `kozloff-de` не упоминается) и добавить в обработчик `hyprland.start` по D4 вместе с `nm-applet --indicator`, двумя `wl-paste --watch cliphist store` (`--type text` и `--type image`), `wezterm-gui`, `firefox`. Проверка: `sh -n`, запуск скрипта из Openbox при активном `axata` завершается без повторного подключения (`nmcli con show --active` без изменений). Результат: скрипт `vpn-up` создан, `sh -n` без ошибок; запуск из Openbox при активном `axata` завершился с кодом 0, список активных соединений не изменился. В обработчик `hyprland.start` добавлены `nm-applet --indicator`, два `wl-paste --watch cliphist store`, `vpn-up`, `wezterm-gui`, `firefox`.
- [x] 4.4 Добавить в начало `dot_config/openbox/autostart.sh` строку `systemctl --user stop graphical-session.target` с комментарием (риск падения Hyprland при выходе), `sh -n`, `chezmoi apply`; проверить, что в текущей сессии команда завершается без ошибок и ничего не останавливает. Результат: строка `systemctl --user stop graphical-session.target` с комментарием добавлена после `exec >/dev/null`, `sh -n` и `chezmoi apply` без ошибок; в текущей сессии команда завершилась с кодом 0, цель была и осталась `inactive`.

## 5. Вход через lightdm и окна (спецификации hyprland-session, hyprland-config, hyprland-binds)

- [x] 5.1 Выйти из Openbox, в greeter выбрать «Hyprland», войти. Проверка сценария «Вход в Hyprland через greeter»: `loginctl show-session $XDG_SESSION_ID -p Type` даёт `wayland`, `echo $XDG_SESSION_DESKTOP` даёт `Hyprland`, `hyprctl monitors` показывает DP-2 3840x2160 при частоте не ниже 119 Гц, `cat ~/.dmrc` содержит `Session=hyprland`. Результат: Вход через greeter выполнен пользователем 2026-09-10 02:40: `loginctl show-session 5` даёт `Type=wayland`, `Desktop=hyprland`, `Service=lightdm`; `hyprctl monitors` показывает DP-2 3840x2160@119.994, scale 1; `~/.dmrc` содержит `Session=hyprland`; `hyprctl configerrors` пуст. Переменная `XDG_SESSION_DESKTOP` равна `hyprland` в нижнем регистре (значение из hyprland.desktop задаёт lightdm).
- [x] 5.2 В терминале сессии проверить окружение: `env | rg 'LIBVA_DRIVER_NAME|__GLX_VENDOR_LIBRARY_NAME|NVD_BACKEND|ELECTRON_OZONE_PLATFORM_HINT|QT_QPA_PLATFORMTHEME|MOZ_DISABLE_RDD_SANDBOX'` даёт шесть строк, `echo $PATH` содержит `~/.local/bin`, `systemctl --user show-environment | rg WAYLAND_DISPLAY` даёт значение. Результат (в том числе, применил ли lightdm login-оболочку): Результат: В окружении wezterm-gui и firefox, запущенных Hyprland, все шесть переменных с ожидаемыми значениями, `WAYLAND_DISPLAY=wayland-1`, `DISPLAY=:0` (Xwayland). PATH содержит `~/.local/bin` и `~/.bun/bin` дважды: из `hl.env` и из login-оболочки, то есть lightdm применяет login-оболочку и к Wayland-сессии (в PATH самого Hyprland есть пути mise из `~/.profile`). `systemctl --user show-environment` содержит `WAYLAND_DISPLAY`, `DISPLAY`, `HYPRLAND_INSTANCE_SIGNATURE`, `XDG_CURRENT_DESKTOP`.
- [x] 5.3 Рабочие столы: Super+7 на пустой стол, `hyprctl activeworkspace` показывает имя «7» на DP-2; Super+Shift+3 переносит окно и переходит за ним (сценарий «Перенос окна на стол»). Результат: `hyprctl workspaces` показывает восемь столов «1»…«8» на DP-2 до первого перехода (persistent). Переход на стол 7 через диспетчер: `hyprctl activeworkspace` даёт «7» на DP-2 без окон. Перенос `window.move({ workspace = 3, follow = true })` перенёс активное окно на стол 3, стол 3 стал активным, окно осталось в фокусе; окно возвращено на стол 1.
- [x] 5.4 Окна: новый терминал плавающий (`hyprctl activewindow` даёт `floating: 1`), Super+Shift+V переводит в мозаику и обратно, GIMP с `GDK_BACKEND=x11` показывает `xwayland: 1` и отрисован полностью, `hyprctl clients` для wezterm даёт `xwayland: 0` (пересобранный с Wayland). Результат: Новый wezterm-gui из сессии: `floating: true`, `xwayland: false` (пересобранный wezterm нативный), размер по умолчанию 800×700; GIMP с `GDK_BACKEND=x11`: класс `Gimp`, `xwayland: true`, окно отображено (mapped), закрыт после проверки; nvidia-settings пользователя тоже под Xwayland. Super+Shift+V проверяется пользователем.
- [x] 5.5 Половины рабочей области: Super+Shift+Влево даёт положение 10,10 и размер 1905×2140, Super+Shift+Вправо, Вверх, Вниз дают соответствующие половины (`hyprctl activewindow`). Результат: Поле `reserved` монитора в Lua-API — таблица с ключами left, top, right, bottom (первая ветка `reserved_of`), `position` — таблица с x, y. Логика функции `half` прогнана через `hyprctl eval` на окне nvidia-settings: left → 10,10 1905×2140; right → 1925,10 1905×2140; up → 10,10 3820×1065; down → 10,1085 3820×1065. Привязка Super+Shift+Вправо нажата пользователем: wezterm получил 1925,10 1905×2140.
- [x] 5.6 Раскладки: Alt+R, Alt+E, Win+Пробел переключают, `hyprctl devices` показывает активную раскладку; Super+T, Super+D ничего не делают, `hyprctl binds` их не содержит. Результат: Подтверждено пользователем 2026-09-10: Alt+R, Alt+E, Win+Пробел переключают раскладки; `hyprctl binds` не содержит Super+T, Super+D, Super+Пробел (проверено по JSON, 57 привязок), `hyprctl devices` показывает активную раскладку.
- [x] 5.7 Скриншоты: Print, Ctrl+Print, Shift+Print создают файлы в `~/Pictures/Screenshots`, снимок в буфере (`wl-paste --list-types` содержит `image/png`), уведомление показано. Результат: Скрипт `screenshot-screen` из сессии создал `~/Pictures/Screenshots/2026-09-10-024251_3840x2160_grim.png`, `wl-paste --list-types` содержит `image/png`, уведомление показано (dunst активен). Ctrl+Print и Shift+Print используют тот же общий код; выделение области slurp проверяется пользователем.
- [x] 5.8 Фокус и окна: Super+стрелки переносят фокус, Alt+Tab и Alt+Shift+Tab переключают с подъёмом, Super+Z прячет окно в специальный стол, Super+Shift+Z показывает и скрывает этот стол, Super+C закрывает, Super+F и Super+X переключают режимы, Super+левая/правая кнопка мыши перемещает и меняет размер. Результат: Подтверждено пользователем: привязки фокуса и окон работают, мышь с Super перемещает и меняет размер; максимизация и скрытие в специальный стол проверены и через диспетчеры. Отклонение: Alt+Shift+Tab с `cycle_next({ next = false })` шёл вперёд; заменено на `cycle_next({ prev = true })`, конфиг перезагружен без ошибок, повторная проверка за пользователем.

## 6. Автозапуск в сессии (спецификация hyprland-autostart)

- [x] 6.1 Через 10 с после входа: `pgrep -c` по одному для hyprpaper, dunst, hyprpolkitagent, nm-applet, voxtype, hypridle, два процесса `wl-paste`; окна wezterm и firefox открыты; обои на месте; `systemctl --user is-active graphical-session.target hyprland-session.target voxtype.service hyprpolkitagent.service` даёт `active` для всех (сценарий «Сессия запущена»). Результат: Через 2 минуты после входа: по одному процессу hyprpaper, dunst, hyprpolkitagent, nm-applet, voxtype, hypridle, два `wl-paste`, окна wezterm-gui и firefox открыты, `hyprctl hyprpaper listactive` показывает файл обоев на DP-2; `graphical-session.target`, `hyprland-session.target` и четыре юнита `active`, отказавших юнитов нет. В журнале: hypridle сообщает «Config has errors: No rules configured» и продолжает работу (в конфиге намеренно нет listener); hyprpolkitagent пишет «Failed to register with host portal» (регистрация Qt-приложения в портале, на работу агента не влияет); voxtype загрузил модель large-v3 на Vulkan.
- [x] 6.2 Через 30 с после входа: `nmcli -t -f NAME con show --active` содержит `axata` и не содержит `kozloff-de` (сценарий «VPN поднят»); после `hyprctl reload` число процессов автозапуска не изменилось (сценарий «Конфиг перезагружен»). Результат: `nmcli con show --active` содержит `axata` и не содержит `kozloff-de`. После `hyprctl reload` число процессов автозапуска не изменилось (1 1 1 1 1 1 2 1 1 до и после), `hyprctl configerrors` пуст.
- [x] 6.3 Буфер обмена: скопировать текст и изображение, Super+V показывает историю в rofi, выбранная запись попадает в буфер (`wl-paste`). Результат: Подтверждено пользователем: Super+V показывает историю cliphist в rofi, выбор попадает в буфер.
- [x] 6.4 Блокировка: Super+L и `loginctl lock-session` показывают hyprlock, после пароля все окна сохранены; `hyprctl configerrors` пуст, hyprpaper и hypridle не пишут ошибок в журнал (`journalctl --user -b | rg -i 'hyprpaper|hypridle'`). Результат: Подтверждено пользователем: Super+L показывает hyprlock, после пароля окна на месте; `hyprctl configerrors` пуст; в журнале hypridle только «No rules configured» (listener намеренно нет), hyprpaper без ошибок.
- [x] 6.5 Порталы: `systemctl --user is-active xdg-desktop-portal-hyprland` после запроса выбора файла из Firefox даёт `active` (сценарий «Портал доступен»); запрос выбора файла открывает yazi в окне wezterm, выход по q отменяет запрос (сценарий «Выбор файла через yazi»). Результат: Часть по D-Bus пройдена 2026-09-10 05:23: `gdbus call … FileChooser.OpenFile` открыл окно класса `termfilechooser` (плавающее, 2200×1400 на момент проверки, затем уменьшено до 1100×700 по просьбе пользователя, по центру), `xdg-desktop-portal-termfilechooser` и `xdg-desktop-portal-hyprland` активны, `graphical-session.target` активен, после q окно закрылось, в журнале портала ошибок нет. До установки termfilechooser тот же запрос открывал нативное окно gtk-портала. Firefox не принимает сочетания от `send_shortcut` и `wtype`, поэтому Ctrl+O проверил пользователь 2026-09-10 05:45 после перезапуска Firefox (переменная `GTK_USE_PORTAL` действует только на процессы, запущенные после `hyprctl reload`): Ctrl+O открыл yazi, выбранный файл открылся в Firefox; `xdg-desktop-portal-hyprland`, `xdg-desktop-portal-termfilechooser` и `graphical-session.target` активны, предупреждений в журнале портала нет. Сохранение (сценарий «Сохранение файла через yazi») 05:46: при `create_help_file=0` пользователь не понял, как сохранить, так как файла для выбора не было; после `create_help_file=1` и `save_mode=suggested` проба `zenity --file-selection --save` показала файл-подсказку в нужном каталоге, Enter вернул его путь приложению, файл перезаписан.
- [x] 6.6 Голосовой ввод: при английской раскладке удерживать клавишу voxtype и произнести русскую фразу, текст напечатан в окно (сценарий «Диктовка русского текста»); `journalctl --user -u voxtype -b` без ошибок wtype. Результат: Подтверждено пользователем: диктовка русской фразы при английской раскладке печатает русский текст; voxtype загрузил модель large-v3 на Vulkan, ошибок wtype в журнале нет.
- [x] 6.7 Эффекты: четыре окна, перемещение и переключение столов без рывков, `nvidia-smi` без насыщения (сценарий «Эффекты не дают рывков»). Результат: Прогон 2026-09-10 05:50 с четырьмя окнами (Firefox, Яндекс.Браузер с видео YouTube, два wezterm): 120 шагов перемещения плавающего окна через `hl.dsp.window.move` и 16 переключений столов 1↔2 через `hl.dsp.focus`, замер `nvidia-smi -lms 200` (66 выборок): загрузка GPU максимум 35%, в среднем 26%, выборок ≥90% нет, частота ядра до 1350 МГц. Насыщения нет. В журнале за это время два coredump вспомогательных процессов Яндекс.Браузера (ExternalMediaService и demuxer), но такие пары падают весь день с 03:06 (16 за сутки) независимо от нагрузки, к сессии не относятся. Пользователь рывков при перемещении окна и переключении столов не увидел (2026-09-10 05:55).
- [x] 6.8 Качество изображения при 120 Гц (design D14): установить `/etc/modprobe.d/nvidia-vrr.conf` с `options nvidia_modeset conceal_vrr_caps=1`, пересобрать initramfs (`limine-mkinitcpio`), перезагрузиться; проверка сценария «VRR скрыт от композитора»: `modetest -M nvidia-drm -c | rg -A3 vrr_capable` даёт `value: 0`, `hyprctl monitors` даёт `vrr: false`, картинка при 120 и 60 Гц одинакова. Результат: Файл установлен 2026-09-10 05:02, initramfs собран для ядер `7.2.3-1-cachyos`, `bore-lto`, `lts`; после перезагрузки 05:06 `vrr_capable` равен 0 у всех коннекторов, `vrr: false`; пользователь сравнил 120 Гц с 60 Гц (переключение через `hyprctl eval 'hl.monitor({...})'`) и разницы не увидел. Отвергнутые до этого кандидаты перечислены в D14. Комментарий в файле переписан из экспериментального в постоянный.

## 7. Возврат в Openbox и завершение

- [x] 7.1 Выйти из Hyprland (Super+M или `hyprctl dispatch exit`), в greeter войти в Openbox: дашборд eww и автозапуск на месте, `systemctl --user is-active graphical-session.target voxtype.service hyprpolkitagent.service` даёт `inactive` (строка из 4.4 отработала), `chezmoi status` пуст (сценарии «Выход в greeter», «Возврат в Openbox»). Результат (в том числе, было ли падение при выходе): 2026-09-10 03:33: выход по Super+M, вход в Openbox через greeter. Сессия 7 `Type=x11`, `Desktop=openbox`; дашборд eww (4 процесса eww, 3 eww-daemon) и picom на месте; `systemctl --user is-active` для graphical-session.target, hyprland-session.target и четырёх юнитов даёт `inactive` (строка из 4.4 отработала); `chezmoi status` пуст; VPN axata поднят блоком autostart.sh. Падение при выходе было: coredump Hyprland SIGSEGV 03:33:06 (известная ошибка aquamarine), на возврат в Openbox не повлияло.
- [x] 7.2 Удалить `~/.config/hypr/hyprland.lua.trial`, убедиться, что `~/.config/quickshell/nvcheck/` не изменён, обновить `summary.md` состоянием изменения. Результат: 2026-09-10 05:57: перед удалением сверены привязки, ключи `hl.config` и `hl.env` пробного и рабочего конфигов, единственное отличие — правило слоя `nvcheck_blur_rule` из проверки NVIDIA, в рабочем конфиге не нужное; `hyprland.lua.trial` удалён, в `~/.config/hypr/` остались только файлы из chezmoi. `~/.config/quickshell/nvcheck/shell.qml` не изменён с 2026-09-10 00:47. `summary.md` обновлён.
- [x] 7.3 Свести результаты: все сценарии спецификаций пройдены или перечислены отклонения с решением по каждому; записать в этой строке. Результат: Сводка 2026-09-10 06:00 по 29 сценариям четырёх спецификаций. hyprland-session: «Вход в Hyprland через greeter» — 5.1; «Выход в greeter», «Возврат в Openbox» — 7.1 (с падением Hyprland при выходе, известная ошибка aquamarine, на возврат не влияет); «Клиент видит переменные» — 5.2; «Видео декодируется аппаратно» — ОТКЛОНЕНИЕ: в основном профиле Firefox (`~/.config/mozilla/firefox/3e5c3xuj.default-release/`) нет ни одной настройки VA-API из требования (они были только в удалённом пробном профиле `~/.cache/ff-nvcheck`), `nvidia-smi dmon` при видео YouTube в Яндекс.Браузере показывает декодер 0%; решение см. ниже. hyprland-config: «Конфиг применён» — 2.1 и `chezmoi status` пуст после каждой правки; «Конфиг перезагружен в работающей сессии» — многократные `hyprctl reload` с пустым `configerrors` (5.8, 6.2, правки D13–D15); «Монитор в родном режиме» — 5.1; «VRR скрыт от композитора» — 6.8; «Раскладки переключаются картой» — 5.6; «Прозрачность как в Openbox» — правила D13 применены после замечания пользователя, на снимках экрана 6.5 терминал в фокусе полупрозрачный с размытием, браузеры без фокуса прозрачнее; «Эффекты не дают рывков» — 6.7; «Переход на пустой стол» — 5.3; «Новое окно плавающее», «Окно Xwayland» — 5.4. hyprland-binds: «Окно в половину рабочей области» — 5.5; «Перенос окна на стол» — 5.3; «Блокировка» — 6.4; «Сочетание без действия» — 5.6; «Снимок области» — 5.7 частично: общий код и Print проверены, выделение slurp по Shift+Print за пользователем. hyprland-autostart: «Сессия запущена», «Конфиг перезагружен» — 6.1, 6.2; «VPN поднят» — 6.2; «VPN уже активен» — по коду `vpn-up` (при активном `axata` `nmcli con up` не вызывается), отдельно не прогонялся; «Блокировка по сигналу logind» — 6.4; «Портал доступен», «Выбор файла через yazi», «Сохранение файла через yazi» — 6.5; «Диктовка русского текста» — 6.6. Проверки 5.4 (Super+Shift+V), 5.7 (Shift+Print со slurp) и 5.8 (Alt+Shift+Tab после исправления) подтверждены пользователем 2026-09-10 06:03. Решение по отклонению VA-API: по решению пользователя в основной профиль Firefox добавлен `user.js` с настройками из спецификации (плюс `media.ffmpeg.vaapi.enabled`, как в пробном профиле); файл вне chezmoi, описан в design D16. Проверка декодера 2026-09-10 06:10: Firefox закрыт и запущен из Hyprland (`hl.exec_cmd`) с тестовым роликом H.264 1080p30 от ffmpeg; `prefs.js` содержит настройки из `user.js`; `nvidia-smi dmon -s u` за 5 с показывает декодер 5–6% на каждой выборке (до этого 0%). Сценарий «Видео декодируется аппаратно» пройден, отклонений не осталось.
