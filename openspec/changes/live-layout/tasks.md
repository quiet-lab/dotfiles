## 1. Демон workspaced (`~/work/pets/workspaced`)

- [ ] 1.1 `state.rs`: раскладка в памяти (решения D1, D5) — `State::moved: BTreeMap<String, BTreeMap<String, Moved>>` (`Moved { rect: PxRect, owner: Option<String> }`), `State::main: BTreeMap<String, String>`; `State::rect_for` сначала берёт изменённое место записи (приложения, затем семейства), `State::main_app` — `State::main`, затем поле `main` раздела, затем приложение главной ячейки (с записью в `State::main` при первом обращении). `SessionWorkspace` получает необязательные `main: Option<String>` и `moved: BTreeMap<String, PxRect>` (пустые не пишутся; решения D13, D14). Проверка: модульные тесты `rect_for_prefers_moved_place` (изменённое место побеждает ячейку; вариант без записи берёт изменённое место семейства), `main_app_is_explicit` (после записи `main` с `rect` главное приложение — из поля, а не из главной ячейки), `session_workspace_layout_roundtrip` (запись и чтение `main` и `moved`; снимок прежнего формата читается без них).
- [ ] 1.2 `daemon.rs`: снятие раскладки (решения D2, D3) — чистая функция `absorb_plan(cfg, st, ws, n, clients, mon) -> Absorb` (изменённые места записей с владельцем и прямоугольники окон для `State::geom`) и метод `Daemon::absorb(ws, n, clients)`. Берётся первое окно записи на столе `n` в порядке `placed_windows`, скрытые и полноэкранные (`fullscreen != 0`) пропускаются, прямоугольник, равный исходному месту, изменённого места не даёт, записи без окон на столе не меняются. Проверка: модульный тест `absorb_takes_first_instance_on_its_desktop` (первый экземпляр из двух; окно на другом столе и окно на `special:pool` не учитываются; полноэкранное пропускается; равный ячейке прямоугольник снимает изменённое место; окно варианта даёт место записи семейства).
- [ ] 1.3 `daemon.rs`: вызовы снятия (решение D4) — `cycle` до выбора шага; `raise_on` для workspace, активного на целевом столе (вытесняемого или того же), и для переносимого с прежнего стола — вместо `remember_geometry`; `follow`, `send_to`, `bring`, `detach` (окно уходит к другому workspace) — для workspace, со стола которого уходит окно, вместо `leave_geom`; `remove`; `save.rs::save_session` (все столы с активным workspace) и `save_workspace`. `remember_geometry` и `leave_geom` удаляются. Проверка: `rg -n 'remember_geometry|leave_geom' src/` пусто; модульный тест `follow_plan_absorbs_leaving_workspace` (общее окно, сдвинутое на столе `work`, при уходе на стол `surf` оставляет `work` изменённое место и запомненный прямоугольник).
- [ ] 1.4 `daemon.rs`: память окна в обоих режимах (решение D8) — `raise_target(carried, moving, kept, place, current)` без признака `stack`; `arrive_rect`, `follow_plan`, `send_to`, `bring` запоминают прямоугольник для всех workspace. Проверка: модульный тест `raise_target_keeps_rect_on_move_in_both_modes` переписан: окно на столе не двигается в обоих режимах, приходящее встаёт в запомненный прямоугольник, без него — на место, переносимое — в прежний; `follow_plan_moves_shared_windows` — окно, пришедшее в workspace режима `swap`, встаёт в запомненный прямоугольник.
- [ ] 1.5 `daemon.rs`: обмен мест (решение D5) — `swap_cells` сменяется функцией `swap_places(st, ws, main, app, has_windows)`: места (`State::cells` и `State::moved`) меняются целиком, `State::main` становится вызванным приложением, изменённое место, доставшееся приложению без окон в `ws`, снимается (решение D9); `swap_to_main` берёт главное приложение из `State::main_app`, ставит окна обоих приложений в прямоугольники новых мест, обновляет `State::geom` и владельцев. Проверка: модульные тесты `swap_places_exchanges_moved_rects` (пример пользователя: `chrome-ai` с записью сессии и изменённым местом 400,300 1400×1000 и herdr в `center` — после обмена `chrome-ai` главное и в `center`, herdr в 400,300 1400×1000; обратный обмен возвращает), `swap_places_drops_moved_of_app_without_windows`; тест `swap_cells_exchanges_stacks` переписан под `swap_places`.
- [ ] 1.6 `config.rs`: `Config::app_mode` без исключения для записи без ячейки (решение D6). Проверка: модульный тест `cycle_mode_of_app` — запись с `rect` в workspace режима `swap` даёт `Swap`, с `mode = "stack"` — `Stack`; `workspaced check` на конфиге сессии проходит.
- [ ] 1.7 `daemon.rs`: шаг цикла в режиме `swap` (решение D7) — в `cycle` при режиме `swap` и неглавном вызванном приложении нажатие начинает цикл заново (новая запись `State::cycle` с prev — первым окном главного приложения) и выбирает активное окно, если оно экземпляр приложения, иначе первый экземпляр; нажатие `Start::Joined` правило не затрагивает. Чистая функция `swap_start(order, active, is_main) -> Option<String>` для теста. Проверка: модульный тест `swap_press_on_non_main_app_selects_active` (одно активное окно неглавного приложения — выбирается оно с обменом; приложение главное — прежний `cycle_step`, конец цикла; три экземпляра главного приложения и клик по второму — третий).
- [ ] 1.8 `daemon.rs`: возврат к исходному (решение D9) — `drop_moved(st, cfg, addr, clients)`: изменённые места с владельцем `addr` снимаются, если в workspace не осталось окон записи, иначе владельцем становится первое оставшееся окно; вызов в обработке `closewindow`, в `detach` и в `sync_membership` для окон, у которых снят тег состава. Владелец назначается при снятии раскладки, при обмене мест и при постановке окна на место без владельца (`adopt`, `capture_new`, `restore_window`). Проверка: модульный тест `closing_last_window_drops_moved_place` (закрытие единственного окна снимает место; при втором окне место остаётся и меняет владельца; место из снимка без владельца переживает закрытие окна другого приложения).
- [ ] 1.9 `daemon.rs`: расстановка по команде (решение D10) — `arrange` снимает `State::moved[ws]`, заменяет `State::geom[ws]` местами плана у окон стола и удаляет остальные, `State::cells[ws]` и `State::main[ws]` не трогает; в обоих режимах. Проверка: модульный тест `arrange_resets_moved_keeps_swap` (после обмена и сдвига окна расстановка ставит окна на исходные места нынешнего назначения, главное приложение прежнее, изменённых мест нет).
- [ ] 1.10 `daemon.rs`: перечитывание (решение D12) — `keep_cells` сменяется `keep_layout(old, new, st) -> Vec<String>`, которая снимает у изменённых и исчезнувших workspace `cells`, `moved`, `main`, `geom` и возвращает их имена; `reload_config` для каждого такого workspace, активного на столе, вызывает `Daemon::apply_layout(ws, n)`: окна `ws` на столе `n` встают на места из новой раскладки (план как у `arrange_plan`, только окна `ws`), фокус и порядок по глубине не меняются, прямоугольники запоминаются. Проверка: модульный тест `keep_layout_resets_changed_workspace` (переписанный `keep_cells_resets_only_changed_workspace`: у соседнего workspace раскладка и изменённые места остаются).
- [ ] 1.11 `save.rs`: запись workspace (решение D11) — снятие раскладки, места из `State::rect_for`; `app_item` сменяется функцией `place_item(template, base, rect)`: имя исходной ячейки, если прямоугольник равен ей, иначе первой равной ячейки шаблона, иначе `{ rect = … }`; `main` — `State::main_app`; `live_rect` удаляется (общее окно, ушедшее на другой стол, даёт место из раскладки). После записи `State::moved[ws]` и `State::main[ws]` снимаются вместе с `cells` (раскладка соберётся из файла при перечитывании). Проверка: модульные тесты `save_writes_layout_places` (после обмена: `main = "chrome-ai"`, `chrome-ai = "center"`, herdr `{ rect = … }`; прямоугольник, равный ячейке `left`, пишется `"left"`), `save_workspace_writes_shared_window` переписан под раскладку; `save_keeps_overrides`, `save_keeps_entry_order`, `rect_of_first_instance_only` проходят.
- [ ] 1.12 `save.rs`, `session.rs`: снимок сессии (решение D13) — `save_session` снимает раскладку всех столов с активным workspace; `update_extra_rects` и `extra_rect` удаляются (прямоугольник записи дополнительного приложения не переписывается); `snapshot` пишет у workspace `main` и `moved`; `instance_record` пишет `rects` для всех workspace окна. Проверка: модульные тесты `snapshot_records_instances` (у окна `rects` для `work` и `surf`), `snapshot_writes_layout` (`main`, `moved` у `work` после обмена; запись дополнительного приложения с прежним прямоугольником).
- [ ] 1.13 `session.rs`, `daemon.rs`: восстановление и загрузка — `apply_lists` восстанавливает `main` и `moved`; `restore_plan` переносит `rects` всех workspace в `State::geom`; `restore_place` одинакова в обоих режимах: прямоугольник записи для workspace, без него — место в раскладке. Проверка: модульные тесты `restored_window_goes_home` (окно workspace режима `swap` встаёт в свой прямоугольник из записи), `restore_plan_skips_live_instances` (прямоугольники режима `swap` тоже становятся запомненными).
- [ ] 1.14 `daemon.rs`: `status_json` отдаёт `main` и `moved` рядом с `cells` (решение D15). Проверка: `workspaced status --json | jq '.main, .moved'` после сборки.
- [ ] 1.15 Комментарии и README демона: тексты о памяти прямоугольников «только в режиме `stack`», о главной ячейке как признаке главного приложения, о режиме приложения без ячейки приводятся к раскладке в памяти; `README.md` демона — раздел о раскладке, обмене мест, снимке (`main`, `moved`, `rects` у окна). Проверка: `rg -n 'в режиме .stack. .*(запомин|помн)|без ячейки.*stack|главн(ой|ую) ячейк' src/ README.md` не находит устаревших утверждений.
- [ ] 1.16 Сборка и проверки: `cargo test`, `cargo clippy --all-targets -- -D warnings`, `cargo build --release` — чисто. Проверка: число тестов больше 131, замечаний нет.

## 2. Конфиг сессии

- [ ] 2.1 `dot_config/workspaced/config.toml` не меняется. Проверка: `workspaced check` новым бинарником — конфиг корректен, число привязок прежнее.
- [ ] 2.2 Подменить бинарник и перезапустить `workspaced.service`. Проверка: `systemctl --user is-active workspaced` — active; окна на столах не сдвинулись (`hyprctl clients -j` до и после совпадают по `at` и `size`), в журнале нет ошибок чтения снимка.

## 3. Проверка командами (агент)

Подпись Hyprland берётся по `ls -t $XDG_RUNTIME_DIR/hypr | head -1` и передаётся явно; окно сдвигается диспетчерами `hyprctl eval 'hl.dispatch(hl.dsp.window.resize({ x = <w>, y = <h>, relative = false, window = "address:<адрес>" }))'` и `hyprctl eval 'hl.dispatch(hl.dsp.window.move({ x = <x>, y = <y>, relative = false, window = "address:<адрес>" }))'` — та же форма, что у `hypr::d_place`.

- [ ] 3.1 Подготовка: скопировать `~/.config/workspaced/config.toml` и `~/.local/state/workspaced/sessions/default.toml` в scratchpad, записать `hyprctl clients -j` и `workspaced status --json`. Проверка: копии есть, на столе 1 активен `work`, herdr на главном месте в ячейке `center` (1125,10 1920×2140).
- [ ] 3.2 Пример пользователя: на столе 1 `workspaced key SUPER+SHIFT+V` (окно `chrome-ai` приходит в `work` в центр экрана), сдвинуть его в 400,300 1400×1000, дать ему фокус (`hl.dsp.focus` с адресом) и выполнить `workspaced key SUPER+V`. Проверка: `chrome-ai` в 1125,10 1920×2140 с фокусом, herdr в 400,300 1400×1000; `workspaced status --json`: `main.work = "chrome-ai"`, `moved.work` содержит herdr с 400,300 1400×1000; `chezmoi status` и `diff` конфига с копией пусты. Повторное `workspaced key SUPER+V`: herdr в 1125,10 1920×2140 с фокусом, `chrome-ai` в 400,300 1400×1000.
- [ ] 3.3 Главное окно тоже сдвинуто: растянуть herdr до 1125,10 2400×2140, выполнить `workspaced key SUPER+V`. Проверка: `chrome-ai` встаёт в 1125,10 2400×2140, herdr — в 400,300 1400×1000; вторым нажатием — обратно.
- [ ] 3.4 Поднятие и вытеснение: сдвинуть `neovide`, выполнить `workspaced raise work` на столе 1. Проверка: ни одно окно `work` не сдвинулось. Затем `workspaced move-desktop 5` и `workspaced move-desktop 1`. Проверка: окна `work` на столе 1 в тех же прямоугольниках. Затем `workspaced raise surf --desktop 1`, `workspaced raise surf --desktop 2`, `workspaced raise work --desktop 1`. Проверка: окна `work` вернулись с `special:pool` в прямоугольники, в которых их оставили, `surf` снова активен на столе 2.
- [ ] 3.5 Новое окно на месте приложения: сдвинуть первое окно Chromium, выполнить `workspaced key SUPER+T` (снятие раскладки) и `chromium --new-window`. Проверка: новое окно получает `app:chromium#2` и встаёт в прямоугольник первого окна.
- [ ] 3.6 Возврат к исходному: закрыть второе окно Chromium (`hl.dsp.window.close` с адресом). Проверка: `moved.work.chromium` остаётся. Сдвинуть `neovide`, выполнить `workspaced key SUPER+T`, закрыть окно `neovide`. Проверка: у `work` нет изменённого места `neovide`; `neovide`, запущенный из оболочки, встаёт в ячейку `right`.
- [ ] 3.7 Расстановка: сдвинуть окна `work`, поменять местами `chrome-ai` и herdr (`workspaced key SUPER+V`), выполнить `workspaced arrange`. Проверка: окна на исходных местах нынешнего назначения (`chrome-ai` в `center`, herdr в прямоугольнике записи сессии `chrome-ai`), `main.work = "chrome-ai"`, изменённых мест у `work` нет; вернуть обмен вторым `workspaced key SUPER+V`.
- [ ] 3.8 Полноэкранное окно: перевести окно Chromium в полноэкранный режим (`hl.dsp.window.fullscreen` с адресом), выполнить `workspaced key SUPER+E`. Проверка: изменённое место `chromium` не стало размером экрана; выйти из полноэкранного режима.
- [ ] 3.9 Общее окно: сдвинуть `chrome-ai` на столе 1, перейти на стол 2 (`hl.dsp.focus` на workspace 2) и обратно. Проверка: на столе 2 окно на месте `surf`, на столе 1 снова в сдвинутом прямоугольнике.
- [ ] 3.10 Снимок сессии: при сдвинутых окнах и обмене `chrome-ai` с herdr выполнить `workspaced save-session`. Проверка: в `default.toml` у `work` есть `main` и `moved`, у окна `chrome-ai` в `rects` есть и `work`, и `surf`, прямоугольник записи дополнительного приложения `chrome-ai` у `work` прежний; `systemctl --user restart workspaced` — окна не сдвинулись, `workspaced status --json` показывает ту же раскладку.
- [ ] 3.11 Запись workspace на время проверки: при обмене `chrome-ai` с herdr и сдвинутом herdr выполнить `workspaced save-workspace`. Проверка: `diff` `~/.config/workspaced/config.toml` с копией показывает в разделе `work` только `main = "chrome-ai"`, `chrome-ai = "center"` и herdr с `rect`; комментарии на месте; в журнале одна строка о перечитывании; окна не сдвинулись; изменённых мест у `work` нет. Вернуть файл: `chezmoi apply --force ~/.config/workspaced/config.toml`. Проверка: после перечитывания окна `work` сразу встали по файлу (herdr в `center`, Chromium в `left`, `neovide` в `right`), фокус прежний; `chezmoi status` пуст.
- [ ] 3.12 Перечитывание изменённого раздела: заменить в `~/.config/workspaced/config.toml` `chromium = "left"` прямоугольником (`sed` на копии и копирование на место). Проверка: окна Chromium на столе 1 сразу в этом прямоугольнике без поднятия `work`; раскладка `surf` не сброшена. Вернуть файл `chezmoi apply --force ~/.config/workspaced/config.toml` — Chromium снова в `left`.
- [ ] 3.13 Режим `stack` не изменился по клавише: на столе 2 сдвинуть окно `chrome`, выполнить `workspaced key SUPER+B`. Проверка: окна `surf` не сдвинулись, `chrome` наверху с фокусом, `moved.surf.chrome` — сдвинутый прямоугольник.
- [ ] 3.14 Вернуть исходное состояние: снять `chrome-ai` с `work` (`workspaced detach` при активном `chrome-ai` на столе 1), закрыть окна проверки, вернуть копию `default.toml`, `workspaced arrange` на столах 1 и 2, перезапустить демон. Проверка: `default.toml` совпадает с копией, `config.toml` — с источником chezmoi, окна `work` в своих ячейках на столе 1, окна `surf` на столе 2, фокус у herdr.

## 4. Проверка вживую (пользователь)

- [ ] 4.1 Мышью и клавишами: на столе 1 Shift+Super+V, перетащить и растянуть окно Chrome AI мышью, Super+V — Chrome AI на главном месте, herdr там, где был Chrome AI; ещё раз Super+V — обратно. Затем растянуть окно herdr мышью за край и снова Super+V. Оценить, что окна встают туда, куда ожидалось, без промежуточных перемещений.
- [ ] 4.2 Сдвинуть окно neovide мышью, нажать Super+T, закрыть neovide (Super+Delete) и открыть neovide из yazi: окно встаёт в ячейку `right`, а не в прежний сдвинутый прямоугольник. Ctrl+Super+Пробел после сдвигов возвращает окна на места из описания, не отменяя обмена.
- [ ] 4.3 Ctrl+Super+S Ctrl+Super+W после сдвигов: сказать, записывать ли результат в `config.toml` под chezmoi или откатить (`chezmoi apply --force ~/.config/workspaced/config.toml`); состояние после выбора агент проверяет командами.

## 5. Спецификации и документация

- [ ] 5.1 Дельты: ws-daemon («Расстановка окон», «Цепочка приложения», «Расстановка по команде», «Следование общего окна за столом»), ws-config («Файл конфига и его разделы», «Слежение за конфигом», «Запись по команде сохранения»), ws-sessions («Файлы сессий», «Загрузка сессии», «Восстановление при старте», «Дополнительные приложения сессии»). Проверка: `openspec validate live-layout --strict` проходит.
- [ ] 5.2 Порядок архивации: после `workspace-overrides` и `session-instances` (`proposal.md`, раздел «Порядок архивации»). Проверка: к архивации этого изменения оба в `openspec/changes/archive/`, и `openspec validate live-layout --strict` проходит против обновлённых спецификаций.
- [ ] 5.3 `AGENTS.md`, раздел о сессии Hyprland: режим `swap` — обмен мест с главным приложением, в том числе у приложения без ячейки; фраза «приложение без ячейки в workspace режима `swap` выбирается в режиме `stack`» и «память о геометрии окон определяется режимом workspace» заменяются раскладкой в памяти (сдвиг окна становится местом приложения, снимается в момент действия, в конфиг — только Ctrl+Super+S Ctrl+Super+W, в снимок — Ctrl+Super+S дважды, закрытие последнего окна возвращает место к описанию); Ctrl+Super+Пробел снимает изменённые места, сохраняя обмен.
- [ ] 5.4 `docs/window-model.md`: раздел 2.1 (исключение для приложения без ячейки снято, ссылка на это изменение), 2.3 (таблица режимов: «выбрать» в `swap` — обмен мест; нажатие клавиши неглавного приложения), 2.4 (переписать: раскладка в памяти, снятие в момент действия, память окна в обоих режимах, возврат к исходному, расстановка), 2.5 (перенос workspace: «вернуть окна в ячейки можно расстановкой» остаётся верным), раздел 3 (строки ws-daemon «Расстановка окон», «Цепочка приложения», «Расстановка по команде», «Следование общего окна за столом»; ws-config «Файл конфига и его разделы», «Слежение за конфигом», «Запись по команде сохранения»; ws-sessions — четыре требования), раздел 5 (изменение вне серии с перечнем решений).
- [ ] 5.5 Отдельный проход по формулировкам: артефакты изменения, изменённые комментарии в коде демона, правки `AGENTS.md`, `docs/window-model.md`, README демона.
