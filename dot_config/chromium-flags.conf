# Флаги Chromium, их читает обёртка /usr/bin/chromium (пакет chromium).
# Нативно под Wayland: под Xwayland voxtype печатает через wtype цифры вместо
# текста, так как виртуальная клавиатура с собственной раскладкой в Xwayland
# толкуется по обычной раскладке.
--ozone-platform=wayland
