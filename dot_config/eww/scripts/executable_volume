#!/bin/sh
# Текущая громкость в % (всегда валидное число для scale, даже если pactl не готов).
v=$(pactl get-sink-volume @DEFAULT_SINK@ 2>/dev/null | head -n1 | awk '{print $5}' | tr -d '%')
case "$v" in
    ''|*[!0-9]*) v=0 ;;
esac
echo "$v"
