#
# These tray will be restarted after switching modes.
# Each line parsed as individual execution processes,
# don't use shell-syntaxis, only `command [options]`.
#
# https://github.com/owl4ce/dotfiles
# ---

# parcellite заменён на clipcat: демон clipcatd запускается из autostart.sh,
# меню — clipcat-menu по W-v (rc.xml). В трее ему иконка не нужна.
nm-applet
#xfce4-power-manager
