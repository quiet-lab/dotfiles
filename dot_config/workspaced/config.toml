# Конфиг демона workspaced (спецификация ws-config).
# Демон пишет сюда только по команде сохранения workspace (SUPER+TAB SUPER+s).
# Координаты — от левого верхнего угла экрана 3840×2160; панель занимает
# 330 px слева, поэтому ячейки начинаются с x = 335. Число — пиксели, строка
# с процентом — доля монитора. Координаты могут быть отрицательными, а размеры
# больше экрана: окно тогда выходит за край, например
# { x = -400, y = 0, w = 4600, h = "100%" }.

# Зазор в пикселях, на который окно отступает внутрь ячейки с каждой стороны.
# Правило: расстояние от края экрана и панели до окна, между окнами и между
# плитками панели — одно число, 2·gap = 10 px. Поэтому ячейки отодвинуты на
# gap от границ рабочей области (x от 335, y от 5, до 3835 и 2155), а панель
# держит те же 10 px (Theme.margin и Theme.gap в quickshell/panel/Theme.qml).
# Команды `half` (Alt+Super+стрелки), `place` и `maximize` считают половины,
# позиции и развёрнутую область по тому же правилу.
gap = 5

# Общее окружение для всех приложений; переопределяется в workspace и приложении.
[env]
EDITOR = "neovide"

# Служебные цепочки: сохранить активный workspace в этот файл, окно выбора
# сессии, следующий workspace из списка текущего стола. Super+Tab — префикс
# всех действий с workspace (после него Super отпускается): d/w/c — поднять,
# 1…8 + буква — на столе, s — сохранить, Tab — следующий workspace стола.
[keys]
save_workspace = "SUPER+TAB SUPER+s"
sessions = "SUPER+S"
next_workspace = "SUPER+TAB Tab"
# Сочетания, которые не может занять ни одна привязка; `workspaced check`
# отклоняет конфиг с такой привязкой (спецификация hyprland-binds).
# Карта XKB (~/.config/X11/xkb_custom): Super+Пробел, Alt+E, Alt+R — иначе
# композитор перехватит их раньше XKB.
# Намеренно свободное: Alt+Super+Пробел.
reserved = [
  "SUPER+space", "ALT+E", "ALT+R",
  "ALT+SUPER+space",
]

# ---- Шаблоны геометрии --------------------------------------------------

# Две колонки, главная справа.
[templates.halves]
main = "right"
[templates.halves.cells]
left = { x = 335, y = 5, w = 1750, h = 2150 }
right = { x = 2085, y = 5, w = 1750, h = 2150 }

# Три окна по 1920 px на всю высоту: главное по центру рабочей области
# (330…3840, центр 2085), боковые слева и справа от него с обычным отступом
# 10 px и уходят за края экрана на 805 px. Ячейка на gap шире окна с каждой
# стороны: окна 1125…3045, −805…1115 и 3055…4975.
[templates.thirds]
main = "center"
[templates.thirds.cells]
left = { x = -810, y = 5, w = 1930, h = 2150 }
center = { x = 1120, y = 5, w = 1930, h = 2150 }
right = { x = 3050, y = 5, w = 1930, h = 2150 }

# Крест: колонки по краям, центр разбит на верх, середину (главная) и низ.
[templates.cross]
main = "center"
[templates.cross.cells]
left = { x = 335, y = 5, w = 700, h = 2150 }
top = { x = 1035, y = 5, w = 2100, h = 600 }
center = { x = 1035, y = 605, w = 2100, h = 950 }
bottom = { x = 1035, y = 1555, w = 2100, h = 600 }
right = { x = 3135, y = 5, w = 700, h = 2150 }

# ---- Приложения ---------------------------------------------------------
# Одно приложение — одно окно. class и title — регулярные выражения: если окна
# с тегом приложения нет, демон захватывает уже открытое окно с таким классом
# (целиком) и заголовком, где бы оно ни было, вместо запуска нового.
# Цепочка из одного сочетания — прямая привязка Super+буква.

# Терминал wezterm с herdr; заголовок окна начинается с «herdr · », что
# отличает его от других окон wezterm. --always-new-process: отдельный процесс,
# иначе окно откроется внутри уже работающего wezterm.
[apps.herdr]
chain = "SUPER+T"
cmd = "wezterm-gui"
args = ["start", "--always-new-process", "--", "herdr"]
cwd = "~"
class = "^org\\.wezfurlong\\.wezterm$"
title = "^herdr · "

# Chromium: под Xwayland класс Chromium-browser, нативно chromium.
[apps.chromium]
chain = "SUPER+C"
cmd = "chromium"
class = "(?i)^chromium(-browser)?$"

[apps.neovide]
chain = "SUPER+E"
cmd = "neovide"
class = "^neovide$"

# Google Chrome из контейнера distrobox ubuntu-box, два профиля одного
# процесса: chrome — профиль Default (Alexandr), chrome-ai — Profile 1
# (AI.dev2026). Окна профилей несут один класс Google-chrome и ничем не
# отличаются, поэтому захват по классу берёт первое свободное окно; пока окна
# живут, их различают теги демона. Запуск открывает окно нужного профиля в уже
# работающем Chrome.
[apps.chrome]
chain = "SUPER+B"
cmd = "distrobox-enter"
args = ["-n", "ubuntu-box", "--", "/usr/bin/google-chrome-stable", "--ozone-platform=x11", "--profile-directory=Default"]
class = "^Google-chrome$"

[apps.chrome-ai]
chain = "SUPER+V"
cmd = "distrobox-enter"
args = ["-n", "ubuntu-box", "--", "/usr/bin/google-chrome-stable", "--ozone-platform=x11", "--profile-directory=Profile 1"]
class = "^Google-chrome$"

# ---- Workspace ----------------------------------------------------------

# Рабочий workspace: herdr по центру (главный), Chromium слева, neovide
# справа на шаблоне thirds.
[workspaces.work]
chain = "SUPER+TAB w"
template = "thirds"
main = "herdr"
icon = "folder-development"
[workspaces.work.apps]
herdr = "center"
chromium = "left"
neovide = "right"

# Два окна Chrome на шаблоне halves: профиль Default справа (главный),
# AI.dev2026 слева. Живёт на столе 2.
[workspaces.surf]
chain = "SUPER+TAB s"
template = "halves"
main = "chrome"
icon = "google-chrome"
[workspaces.surf.apps]
chrome-ai = "left"
chrome = "right"

# Поднимается при старте демона после восстановления сессии (стол 1).
[startup]
workspace = "work"

# ---- Горячие клавиши сессии ---------------------------------------------
# Все привязки Hyprland живут здесь (спецификация hyprland-binds): hyprland.lua
# только загружает код, который печатает `workspaced keys --lua`. Одиночное
# сочетание — прямая привязка, цепочка через пробел — подкарта. Ровно одно
# действие на запись: action (демон), exec (команда), dispatch (диспетчер
# hl.dsp.* без префикса, строка или список по порядку), lua (тело функции).
# Флаги: locked (и при заблокированном экране), repeating (повтор при
# удержании), mouse (кнопка мыши с удержанием), release (по отпусканию).
# range = [от, до] разворачивает запись, $n заменяется числом.
# Список привязок — `workspaced keys --list`.

# Программы.
[[binds]]
chain = "SUPER+R"
exec = "rofi -show drun"

# Меню буфера обмена: история cliphist через rofi. Super+V отдан Chrome,
# Insert — клавиша вставки.
[[binds]]
chain = "SUPER+Insert"
exec = "~/.config/hypr/scripts/clipboard-menu"

# Окно. Закрытие — Super+Delete: Super+C занят Chromium.
[[binds]]
chain = "SUPER+Delete"
dispatch = "window.close()"

# Замена сворачивания: Super+Z прячет окно в специальный стол «hidden»,
# Super+Shift+Z показывает и скрывает этот стол.
[[binds]]
chain = "SUPER+Z"
dispatch = "window.move({ workspace = \"special:hidden\", follow = false })"

[[binds]]
chain = "SUPER+SHIFT+Z"
dispatch = "workspace.toggle_special(\"hidden\")"

# Фокус.
[[binds]]
chain = "SUPER+left"
dispatch = "focus({ direction = \"l\" })"

[[binds]]
chain = "SUPER+right"
dispatch = "focus({ direction = \"r\" })"

[[binds]]
chain = "SUPER+up"
dispatch = "focus({ direction = \"u\" })"

[[binds]]
chain = "SUPER+down"
dispatch = "focus({ direction = \"d\" })"

# Alt+Tab с подъёмом окна наверх, как NextWindow в Openbox. Диспетчер
# cycle_next обходит окна в порядке слоёв, а bring_to_top этот порядок меняет,
# поэтому назад он ходил только между двумя последними окнами. Здесь обход
# идёт по порядку создания окон (stable_id) среди окон текущего стола.
[[binds]]
chain = "ALT+Tab"
lua = """
local cur = hl.get_active_window()
if not cur or not cur.workspace then return end
local list = {}
for _, w in ipairs(hl.get_windows()) do
  if w.mapped and w.workspace and w.workspace.id == cur.workspace.id then table.insert(list, w) end
end
table.sort(list, function(a, b) return a.stable_id < b.stable_id end)
local idx = 1
for i, w in ipairs(list) do if w.address == cur.address then idx = i end end
hl.dispatch(hl.dsp.focus({ window = list[(idx) % #list + 1] }))
hl.dispatch(hl.dsp.window.bring_to_top())
"""

[[binds]]
chain = "ALT+SHIFT+Tab"
lua = """
local cur = hl.get_active_window()
if not cur or not cur.workspace then return end
local list = {}
for _, w in ipairs(hl.get_windows()) do
  if w.mapped and w.workspace and w.workspace.id == cur.workspace.id then table.insert(list, w) end
end
table.sort(list, function(a, b) return a.stable_id < b.stable_id end)
local idx = 1
for i, w in ipairs(list) do if w.address == cur.address then idx = i end end
hl.dispatch(hl.dsp.focus({ window = list[(idx - 2) % #list + 1] }))
hl.dispatch(hl.dsp.window.bring_to_top())
"""

# Геометрия окна — слой Alt+Super. Половины рабочей области: команда демона
# `half`, отступы по правилу gap выше. Super+Shift+стрелки намеренно свободны.
[[binds]]
chain = "ALT+SUPER+left"
action = { half = "left" }

[[binds]]
chain = "ALT+SUPER+right"
action = { half = "right" }

[[binds]]
chain = "ALT+SUPER+up"
action = { half = "up" }

[[binds]]
chain = "ALT+SUPER+down"
action = { half = "down" }

# Позиции окна: команда демона `place`, те же отступы. Верхний ряд (окно в
# половину ширины и высоты): Home — слева, PageDown — по центру, End — справа.
# Нижний ряд с Ctrl: Z — слева, X — по центру, V — справа. C с Ctrl — по центру
# на всю высоту, PageUp — развернуть с возвратом (maximize), Enter — окно 1920×1080 по центру
# рабочей области (размер Full HD), Menu — полноэкранный режим.
[[binds]]
chain = "ALT+SUPER+Home"
action = { place = "top-left" }

[[binds]]
chain = "ALT+SUPER+Page_Down"
action = { place = "top-center" }

[[binds]]
chain = "ALT+SUPER+End"
action = { place = "top-right" }

[[binds]]
chain = "CTRL+ALT+SUPER+Z"
action = { place = "bottom-left" }

[[binds]]
chain = "CTRL+ALT+SUPER+X"
action = { place = "bottom-center" }

[[binds]]
chain = "CTRL+ALT+SUPER+V"
action = { place = "bottom-right" }

[[binds]]
chain = "CTRL+ALT+SUPER+C"
action = { place = "center" }

# Развёртывание — командой демона, а не режимом maximized композитора: Hyprland
# ставит плавающее окно вплотную к панели и краям, демон оставляет те же 10 px,
# что у половин и ячеек (2·gap). Повторное нажатие возвращает прежнюю геометрию.
[[binds]]
chain = "ALT+SUPER+Page_Up"
action = "maximize"

[[binds]]
chain = "ALT+SUPER+Return"
dispatch = [
  "window.float({ action = \"on\" })",
  "window.resize({ x = 1920, y = 1080, relative = false })",
  "window.center()",
]

[[binds]]
chain = "ALT+SUPER+Menu"
dispatch = "window.fullscreen({ mode = \"fullscreen\", action = \"toggle\" })"

# Рабочие столы: переход и перенос окна с переходом за ним (SendToDesktop).
[[binds]]
chain = "SUPER+$n"
range = [1, 8]
dispatch = "focus({ workspace = $n })"

[[binds]]
chain = "SUPER+SHIFT+$n"
range = [1, 8]
dispatch = "window.move({ workspace = $n, follow = true })"

# Мышь: перемещение и изменение размера с удержанием Super.
[[binds]]
chain = "SUPER+mouse:272"
mouse = true
dispatch = "window.drag()"

[[binds]]
chain = "SUPER+mouse:273"
mouse = true
dispatch = "window.resize()"

# Блокировка сессии: hypridle слушает logind и запускает hyprlock.
[[binds]]
chain = "SUPER+L"
exec = "loginctl --no-ask-password lock-session"

# Скриншоты (grim и slurp, см. ~/.config/hypr/scripts).
[[binds]]
chain = "Print"
exec = "~/.config/hypr/scripts/screenshot-screen"

[[binds]]
chain = "CTRL+Print"
exec = "~/.config/hypr/scripts/screenshot-countdown"

[[binds]]
chain = "SHIFT+Print"
exec = "~/.config/hypr/scripts/screenshot-selection"

# Уведомления dunst.
[[binds]]
chain = "CTRL+Escape"
exec = "dunstctl history-pop"

[[binds]]
chain = "CTRL+Return"
exec = "dunstctl context"

[[binds]]
chain = "CTRL+space"
exec = "dunstctl close"

[[binds]]
chain = "CTRL+SHIFT+space"
exec = "dunstctl close-all"

# Мультимедиа: работают и при заблокированном экране.
[[binds]]
chain = "XF86AudioRaiseVolume"
exec = "~/.scripts/change-volume.sh +"
locked = true
repeating = true

[[binds]]
chain = "XF86AudioLowerVolume"
exec = "~/.scripts/change-volume.sh -"
locked = true
repeating = true

[[binds]]
chain = "XF86AudioMute"
exec = "~/.scripts/change-volume.sh 0"
locked = true

[[binds]]
chain = "XF86MonBrightnessUp"
exec = "~/.scripts/change-brightness.sh +"
locked = true
repeating = true

[[binds]]
chain = "XF86MonBrightnessDown"
exec = "~/.scripts/change-brightness.sh -"
locked = true
repeating = true

[[binds]]
chain = "XF86AudioPlay"
exec = "playerctl play-pause"
locked = true

[[binds]]
chain = "XF86AudioStop"
exec = "playerctl stop"
locked = true

[[binds]]
chain = "XF86AudioPrev"
exec = "playerctl previous"
locked = true

[[binds]]
chain = "XF86AudioNext"
exec = "playerctl next"
locked = true

# Служебные привязки, которых в Openbox не было.
# Выход из сессии, пока нет плитки питания: сочетание с тремя модификаторами,
# чтобы не нажать случайно.
[[binds]]
chain = "CTRL+ALT+SUPER+Escape"
dispatch = "exit()"
