# hypridle только слушает logind: `loginctl lock-session` запускает hyprlock.
# Таймаутов бездействия и действий перед сном нет: спящий режим не используется,
# автоблокировки в сессии Openbox тоже не было.

general {
    lock_cmd = pidof hyprlock || hyprlock
}
